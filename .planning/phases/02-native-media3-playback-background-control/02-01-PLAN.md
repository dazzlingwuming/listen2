---
phase: 02-native-media3-playback-background-control
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - android/app/build.gradle
  - android/app/src/main/java/com/dazzlingwuming/listen2/PlaybackCommand.java
  - android/app/src/main/java/com/dazzlingwuming/listen2/PlaybackSnapshot.java
  - android/app/src/main/java/com/dazzlingwuming/listen2/PlaybackBridgePolicy.java
  - android/app/src/test/java/com/dazzlingwuming/listen2/PlaybackDependencyProbeTest.java
  - android/app/src/test/java/com/dazzlingwuming/listen2/PlaybackBridgePolicyTest.java
autonomous: true
requirements: [PLAY-001, PLAY-003, DATA-001]
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-02: compileSdk 35 resolves one aligned Media3 1.9.4 graph, Room 2.8.4, and the approved Java settings dependency without an API-36 upgrade."
    - "D-04/D-05: `prepareSelection` accepts only bounded domain fields, then native mints track/occurrence identities consumed by `selectPrepared`; both operations require page epoch and expected revision."
    - "D-04/D-06: snapshots are versioned and sanitized and cannot carry URLs, headers, cookies, raw MediaItem values, provider bodies, or credentials."
    - "D-17: pure-Java bridge tests fail before implementation on closed schemas, stale revisions, serialization, and sensitive-field boundaries."
  artifacts:
    - path: android/app/build.gradle
      provides: API-35-compatible Media3, Room, testing, and Java annotation-processing graph
    - path: android/app/src/main/java/com/dazzlingwuming/listen2/PlaybackBridgePolicy.java
      provides: Android-free command/snapshot validation boundary
    - path: android/app/src/test/java/com/dazzlingwuming/listen2/PlaybackBridgePolicyTest.java
      provides: closed-schema, stale-revision, and sensitive-field rejection tests
  key_links:
    - from: PlaybackCommand.java
      to: PlaybackBridgePolicy.java
      via: parsed command validation before any coordinator dispatch
    - from: PlaybackSnapshot.java
      to: PlaybackBridgePolicy.java
      via: bounded snapshot serialization contract
---

## Phase Goal

**As a** Listen2 Android user, **I want to** use one native player across the page and Android system controls, **so that** every control, queue transition, background event, and recovery describes the same durable state.

<objective>
Resolve the pinned AndroidX graph and freeze the smallest production playback command/snapshot contract before service, persistence, and UI expansion.

Purpose: remove compile-SDK and wire-shape uncertainty at the start of the phase.
Output: a dependency-resolution tracer plus pure-Java closed playback protocol tests.
</objective>

<execution_context>
@/Users/fluenteng/.codex/gsd-core/workflows/execute-plan.md
@/Users/fluenteng/.codex/gsd-core/templates/summary.md
</execution_context>

<execution_gate>
Before every task commit or push, invoke `$run-local-ci` for the exact worktree and HEAD. Any failure, timeout, missing authoritative command/tool, or coverage mismatch blocks publication. No plan authorizes merge, deploy, signing-secret use, or release publication.
</execution_gate>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-native-media3-playback-background-control/02-CONTEXT.md
@.planning/phases/02-native-media3-playback-background-control/02-RESEARCH.md
@.planning/phases/02-native-media3-playback-background-control/02-PATTERNS.md
@android/app/build.gradle
@android/app/src/main/java/com/dazzlingwuming/listen2/AndroidRpcContract.java
@android/app/src/test/java/com/dazzlingwuming/listen2/AndroidRpcContractTest.java
</context>

<file_ownership>
Plan 02-01 is the sole writer for the six listed files. Later plans may extend build or bridge integration only after 02-01-SUMMARY.md records both verifiers and local CI passing.
</file_ownership>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Resolve the API-35 Media3 and Room graph before implementation</name>
  <reversibility rating="costly">D-02 pins the dependency line used by every later native playback file; changing it requires coordinated service, test, and toolchain work.</reversibility>
  <files>android/app/build.gradle, android/app/src/test/java/com/dazzlingwuming/listen2/PlaybackDependencyProbeTest.java</files>
  <behavior>
    - Media3 exoplayer and session resolve exactly at 1.9.4 under compileSdk 35.
    - Room runtime/compiler/testing resolve at 2.8.4 with Java annotationProcessor and schema export enabled.
    - No DataStore dependency is added; small settings use the bounded platform wrapper resolved in RESEARCH.
    - Debug/release compilation does not select Media3 1.11.0 or require compileSdk 36.
  </behavior>
  <action>
Per D-02/D-17, declare one `media3Version = '1.9.4'` for `media3-exoplayer` and `media3-session`; declare Room runtime/compiler/testing at `2.8.4`, Java `annotationProcessor`, and schema export location. Add only official AndroidX coordinates from the Package Legitimacy Audit and no DataStore package. Create a compile-level probe importing `ExoPlayer`, `MediaSessionService`, `RoomDatabase`, and `MigrationTestHelper`. Preserve Java 17, SDK 35, the asset allow-list, application IDs, WebKit, and existing tests.
  </action>
  <verify>
    <automated>cd android &amp;&amp; gradle --no-daemon :app:dependencyInsight --dependency media3-session --configuration debugRuntimeClasspath :app:dependencyInsight --dependency room-runtime --configuration debugRuntimeClasspath :app:compileDebugJavaWithJavac :app:compileDebugUnitTestJavaWithJavac</automated>
  </verify>
  <done>The pinned graph resolves and compiles on Java 17/SDK 35 with no toolchain uplift or alternate Media3 line.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Freeze the allow-listed command and revisioned snapshot protocol</name>
  <reversibility rating="costly">D-04/D-05 define a cross-language contract later native and classic-script callers must change together.</reversibility>
  <files>android/app/src/main/java/com/dazzlingwuming/listen2/PlaybackCommand.java, android/app/src/main/java/com/dazzlingwuming/listen2/PlaybackSnapshot.java, android/app/src/main/java/com/dazzlingwuming/listen2/PlaybackBridgePolicy.java, android/app/src/test/java/com/dazzlingwuming/listen2/PlaybackBridgePolicyTest.java</files>
  <behavior>
    - `prepareSelection` accepts exactly `source=bilibili`, validated `providerTrackId` BVID, positive `providerPartId` CID, plain `title`/`artist` up to 256 characters, optional `durationMs` from 0 through 28,800,000, and `mediaKind=audio`.
    - Native returns `trackHandle` and `occurrenceId` only in a newer snapshot; `selectPrepared` accepts only that pair plus `selectionAction=replace-current|enqueue-next` and `playWhenReady` boolean.
    - Other commands cover play, pause, seek, previous, next, volume, mute, mode, reorder, remove, clear, retry, subscribe, and detach only.
    - Every mutating command carries a bounded pageEpoch, requestId, expectedRevision, and native-issued occurrence identity when required.
    - Unknown keys, commands, occurrence shapes, negative/overflow revisions, invalid seek/volume values, stale epochs, and stale revisions fail with stable safe codes.
    - Snapshot revision is monotonic and contains metadata, state, position/duration, volume/mute, mode, action availability, queue occurrences, and safe recovery status only.
  </behavior>
  <action>
Implement Android-free immutable command and snapshot DTOs plus `PlaybackBridgePolicy` parsing/validation per D-04, D-05, and D-06. Freeze the two-step hand-off: the envelope carries bounded `requestId` (1–128 ASCII), nonnegative `pageEpoch`, and nonnegative `expectedRevision`; `prepareSelection` accepts only the exact domain fields listed in behavior, after which native mints bounded opaque `trackHandle` and `occurrenceId`; `selectPrepared` can reference only that native-issued pair. Queue rows use occurrence identity, never provider identity. Reject `url`, `uri`, candidate, method, header, cookie, body, bitmap, MediaItem, provider JSON, unknown keys, and caller-chosen native IDs. Snapshot serialization projects safe domain metadata, bundled-placeholder artwork state, action capability, identities, and revision only. Tests cover the full prepare→mint→select sequence, replay/stale pairs, every boundary, and forbidden transport-shaped fields.
  </action>
  <verify>
    <automated>cd android &amp;&amp; gradle --no-daemon :app:testDebugUnitTest --tests com.dazzlingwuming.listen2.PlaybackBridgePolicyTest</automated>
  </verify>
  <done>The tested protocol is closed, revision-aware, occurrence-aware, and safe to connect to the existing single WebMessage listener.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Gradle registry → APK | Only audited AndroidX coordinates enter the native runtime. |
| Packaged page → playback policy | Untrusted page JSON requests native playback transitions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-01 | Tampering | PlaybackBridgePolicy | high | mitigate | Exact schemas, epochs, revisions, occurrence IDs, numeric bounds, and negative tests. |
| T-02-02 | Information Disclosure | PlaybackSnapshot | high | mitigate | DTO allow-list and tests excluding transport, credential, provider-body, and native-object fields. |
| T-02-SC | Tampering | Maven dependencies | high | mitigate | Official AndroidX audit, exact version resolution, Gradle insight, and compile probe. |
</threat_model>

<verification>
Run both task commands, inspect dependency insight for Media3 1.9.4 and Room 2.8.4, then run the authoritative local CI gate before each commit/push.
</verification>

<success_criteria>
- D-02 and D-04 through D-06 have executable contract evidence.
- No dependency or protocol choice widens SDK, WebView, network, credential, or provider scope.
- Downstream queue, Room, service, and frontend plans can consume named immutable contracts without rediscovery.
</success_criteria>

<output>
Create `.planning/phases/02-native-media3-playback-background-control/02-01-SUMMARY.md` when done.
</output>
