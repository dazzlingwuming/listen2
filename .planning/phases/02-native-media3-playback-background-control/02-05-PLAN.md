---
phase: 02-native-media3-playback-background-control
plan: "05"
type: execute
wave: 4
depends_on: [02-04]
files_modified:
  - android/app/src/main/java/com/dazzlingwuming/listen2/AndroidRpcContract.java
  - android/app/src/main/java/com/dazzlingwuming/listen2/AndroidHttpBridge.java
  - android/app/src/main/java/com/dazzlingwuming/listen2/PlaybackBridgeController.java
  - android/app/src/main/java/com/dazzlingwuming/listen2/MainActivity.java
  - android/app/src/test/java/com/dazzlingwuming/listen2/AndroidRpcContractTest.java
  - android/app/src/test/java/com/dazzlingwuming/listen2/PlaybackBridgeControllerTest.java
autonomous: true
requirements: [PLAY-001, PLAY-003, PLAY-006]
estimate: { tokens: 39000, raw_tokens: 39000, tasks: 2, confidence: low }
must_haves:
  truths:
    - "D-04: playback reuses the exact origin-restricted `Listen2AndroidHttp` WebMessage listener and adds no second bridge or JavaScript interface."
    - "D-05: commands require current page epoch/revision; detach makes old-page work inert while service audio remains unchanged."
    - "D-03/D-16: Activity destroy/reload/back disconnects the renderer/controller only and can rebuild the latest snapshot."
  artifacts:
    - { path: android/app/src/main/java/com/dazzlingwuming/listen2/PlaybackBridgeController.java, provides: command-to-coordinator and snapshot-to-page adapter }
    - { path: android/app/src/main/java/com/dazzlingwuming/listen2/MainActivity.java, provides: short-lived page controller composition and layer-aware back dispatch }
  key_links:
    - { from: AndroidHttpBridge.java, to: PlaybackBridgeController.java, via: playback operation family on the existing listener }
    - { from: MainActivity.java, to: PlaybackService.java, via: controller connection that never owns/releases the player }
---
<objective>Connect the sole service to the packaged page through the existing versioned origin-bound RPC lifecycle.</objective>
<execution_context>
@/Users/fluenteng/.codex/gsd-core/workflows/execute-plan.md
@/Users/fluenteng/.codex/gsd-core/templates/summary.md
</execution_context>
<execution_gate>Before every commit/push, run `$run-local-ci` on exact content. No merge/deploy.</execution_gate>
<context>
@.planning/phases/02-native-media3-playback-background-control/02-CONTEXT.md
@.planning/phases/02-native-media3-playback-background-control/02-01-SUMMARY.md
@.planning/phases/02-native-media3-playback-background-control/02-04-SUMMARY.md
@android/app/src/main/java/com/dazzlingwuming/listen2/AndroidHttpBridge.java
@android/app/src/main/java/com/dazzlingwuming/listen2/MainActivity.java
</context>
<file_ownership>Plan 02-05 exclusively owns native RPC/controller/Activity integration and the named tests.</file_ownership>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Dispatch playback commands and monotonic snapshots on the existing bridge</name>
  <files>android/app/src/main/java/com/dazzlingwuming/listen2/AndroidRpcContract.java, android/app/src/main/java/com/dazzlingwuming/listen2/AndroidHttpBridge.java, android/app/src/main/java/com/dazzlingwuming/listen2/PlaybackBridgeController.java, android/app/src/test/java/com/dazzlingwuming/listen2/AndroidRpcContractTest.java, android/app/src/test/java/com/dazzlingwuming/listen2/PlaybackBridgeControllerTest.java</files>
  <behavior>
    - One existing listener parses provider, cancel, exact prepareSelection/selectPrepared, control, subscribe, and detach families without widening HTTP routes.
    - Prepare accepts bounded domain fields; native mints trackHandle/occurrenceId; select rejects caller-minted, stale, replayed, or mismatched pairs.
    - One terminal command result plus newer snapshot settles each request; old epoch/revision replies are suppressed.
    - Snapshot fanout is main-thread-safe, monotonic, bounded, and detached-page-safe.
  </behavior>
  <action>Extend protocol 2 with the exact 02-01 `prepareSelection` and `selectPrepared` hand-off plus allow-listed controls per D-04/D-05. Prepare passes only source/BVID/CID/plain title/artist/optional bounded duration/audio kind to the service; a newer snapshot returns the native-minted trackHandle/occurrenceId. Select accepts only that registered pair plus action/playWhenReady and current expectedRevision. Reject URL/URI/method/header/cookie/body/candidate/bitmap/MediaItem/provider JSON and caller-chosen handles before dispatch. Test bridge→controller→service fake for prepare, mint, select, stale revision/epoch, replay, teardown, and forbidden fields. Page teardown detaches authority without changing service playback; preserve Phase-1 provider/v1 behavior.</action>
  <verify><automated>cd android &amp;&amp; gradle --no-daemon :app:testDebugUnitTest --tests com.dazzlingwuming.listen2.AndroidRpcContractTest --tests com.dazzlingwuming.listen2.PlaybackBridgeControllerTest</automated></verify>
  <done>The single trusted WebMessage object carries typed playback commands/snapshots with first-terminal and stale-page guarantees.</done>
</task>
<task type="auto">
  <name>Task 2: Make Activity and renderer lifecycle controller-only</name>
  <files>android/app/src/main/java/com/dazzlingwuming/listen2/MainActivity.java</files>
  <action>Per D-03/D-16, connect the Activity/page to the service controller after the packaged page is ready, request the latest snapshot, and detach before WebView destruction. Replace the Phase-1 page-owned-audio comment/assumption. Back first asks the packaged player UI to dismiss confirmation, queue, or detail; navigation cancellation and Activity exit never pause/release playback. Rotation/recreation must reconnect without replaying pending commands. Retain secure WebSettings/navigation and Phase-1 bridge teardown order.</action>
  <verify><automated>cd android &amp;&amp; gradle --no-daemon :app:testDebugUnitTest :app:assembleDebug</automated></verify>
  <done>Activity/WebView lifetime no longer owns audio and can reconstruct state from the native snapshot.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| WebMessage page → service controller | A renderer sends state-changing commands. |
| Service snapshot → retiring WebView | Native state crosses a disposable renderer boundary. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-12 | Spoofing | page authority | high | mitigate | Exact origin/main frame, page epoch, listener reuse, and instrumentation. |
| T-02-13 | Tampering | stale revision | high | mitigate | Expected-revision checks and monotonic snapshot tests. |
| T-02-14 | Information Disclosure | snapshot fanout | high | mitigate | Sanitized DTO only and detach-before-destroy behavior. |
</threat_model>
<verification>Run focused tests/build, Phase-1 bridge/navigation regressions, and local CI.</verification>
<success_criteria>- No second bridge exists. - Page teardown cannot affect service playback. - Every visible native update is revisioned and safe.</success_criteria>
<output>Create `.planning/phases/02-native-media3-playback-background-control/02-05-SUMMARY.md` when done.</output>
