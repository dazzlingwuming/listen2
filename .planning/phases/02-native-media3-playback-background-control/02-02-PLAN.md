---
phase: 02-native-media3-playback-background-control
plan: "02"
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - android/app/src/main/java/com/dazzlingwuming/listen2/PlaybackQueueEngine.java
  - android/app/src/test/java/com/dazzlingwuming/listen2/PlaybackQueueEngineTest.java
autonomous: true
requirements: [PLAY-004, PLAY-005]
estimate: { tokens: 34000, raw_tokens: 34000, tasks: 2, confidence: low }
must_haves:
  truths:
    - "D-07/D-08: duplicate play-next entries remain separate FIFO occurrences and consumption returns to the saved base context/mode."
    - "D-09: Fisher-Yates shuffle, repeat, and previous use accepted history and survive serialized restore without polluting the base playlist."
    - "D-08/D-10: natural end and every next surface enter one idempotent transition and a retry never consumes another occurrence."
  artifacts:
    - path: android/app/src/main/java/com/dazzlingwuming/listen2/PlaybackQueueEngine.java
      provides: pure deterministic semantic queue/history/mode transition engine
    - path: android/app/src/test/java/com/dazzlingwuming/listen2/PlaybackQueueEngineTest.java
      provides: duplicate FIFO, return-context, shuffle, history, and idempotency matrix
  key_links:
    - from: PlaybackQueueEngine.java
      to: PlaybackCommand.java
      via: accepted command plus current revision produces one transition result
---

<objective>
Implement and prove the semantic queue engine that remains authoritative above the Media3 timeline.

Purpose: preserve Listen2 FIFO, shuffle, repeat, and real-history behavior across competing control surfaces.
Output: a pure-Java engine and exhaustive deterministic tests.
</objective>

<execution_context>
@/Users/fluenteng/.codex/gsd-core/workflows/execute-plan.md
@/Users/fluenteng/.codex/gsd-core/templates/summary.md
</execution_context>

<execution_gate>Before every task commit or push, invoke `$run-local-ci` for the exact worktree and HEAD; failures block publication. No merge or deploy is authorized.</execution_gate>

<context>
@.planning/phases/02-native-media3-playback-background-control/02-CONTEXT.md
@.planning/phases/02-native-media3-playback-background-control/02-RESEARCH.md
@.planning/phases/02-native-media3-playback-background-control/02-PATTERNS.md
@.planning/phases/02-native-media3-playback-background-control/02-01-SUMMARY.md
@app/listen1_chrome_extension/test/player_play_next_queue.test.js
@app/listen1_chrome_extension/test/player_shuffle.test.js
@app/listen1_chrome_extension/test/player_recovery.test.js
</context>

<file_ownership>Plan 02-02 exclusively owns `PlaybackQueueEngine.java` and its JVM test. No page, Room, player, or service state is written here.</file_ownership>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Model duplicate-preserving FIFO occurrences and return context</name>
  <files>android/app/src/main/java/com/dazzlingwuming/listen2/PlaybackQueueEngine.java, android/app/src/test/java/com/dazzlingwuming/listen2/PlaybackQueueEngineTest.java</files>
  <behavior>
    - Enqueueing the same track twice creates two unique ordered occurrence IDs.
    - Reorder/remove/clear address occurrences, preserve remaining order, and reject stale identities/revisions.
    - Exactly one accepted next/end transition consumes one occurrence.
    - Consuming the final FIFO item restores the unmodified base playlist/context and prior mode.
  </behavior>
  <action>
Write tests first from the desktop behavioral contracts, then implement immutable `State` and `Transition` values. Per D-07/D-08, keep `playNextOccurrences`, base context, and current occurrence distinct from any Media3 timeline or provider ID. Inject occurrence-ID and clock sources for deterministic tests; production IDs must be bounded opaque values. Make transition tokens idempotent so duplicate natural-end/page/notification callbacks return the already accepted revision rather than consuming again.
  </action>
  <verify><automated>cd android &amp;&amp; gradle --no-daemon :app:testDebugUnitTest --tests com.dazzlingwuming.listen2.PlaybackQueueEngineTest</automated></verify>
  <done>Duplicate FIFO rows, mutations, one-time consumption, and base-context restoration pass without a player or database.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Fisher-Yates, repeat, real-history previous, and restart state</name>
  <files>android/app/src/main/java/com/dazzlingwuming/listen2/PlaybackQueueEngine.java, android/app/src/test/java/com/dazzlingwuming/listen2/PlaybackQueueEngineTest.java</files>
  <behavior>
    - Each shuffle round visits every playable base occurrence once and avoids immediate boundary repetition when alternatives exist.
    - Previous walks accepted playback history including queue-only occurrences.
    - Repeat-one never inserts or consumes a FIFO occurrence twice; repeat/shuffle modes restore exactly.
    - Retry/failure retains current occurrence and queue order.
  </behavior>
  <action>
Per D-09/D-10, add an injected random source and Fisher-Yates permutation over playable occurrences, explicit repeat modes, accepted-history entries/cursor, and checkpoint conversion. Preserve queue-only history without adding those tracks to the base playlist. Cover restart round-trip, rapid next, duplicate event token, repeat-one completion, disabled tracks, one-item shuffle, previous after queue drain, and terminal media failure retaining the same occurrence.
  </action>
  <verify><automated>cd android &amp;&amp; gradle --no-daemon :app:testDebugUnitTest --tests com.dazzlingwuming.listen2.PlaybackQueueEngineTest</automated></verify>
  <done>The engine deterministically satisfies PLAY-004/PLAY-005 semantics and produces checkpoint-ready state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Multiple control events → queue engine | Competing page/system/player callbacks request the same semantic transition. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-03 | Tampering | transition tokens | high | mitigate | Revision plus event-token idempotency tests prevent double consumption. |
| T-02-04 | Repudiation | accepted history | medium | mitigate | Immutable transition result records prior/current occurrence and accepted revision. |
| T-02-05 | Denial of Service | queue mutation | medium | mitigate | Bounded queue sizes/IDs and deterministic linear operations. |
</threat_model>

<verification>Run the focused test, then the existing JS queue/shuffle/recovery tests as behavioral parity evidence and local CI before publication.</verification>
<success_criteria>
- PLAY-004 and PLAY-005 have pure deterministic transition evidence.
- No raw Media3 playlist, page localStorage, provider ID, or random object identity becomes semantic queue authority.
</success_criteria>
<output>Create `.planning/phases/02-native-media3-playback-background-control/02-02-SUMMARY.md` when done.</output>
