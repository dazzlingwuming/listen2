---
phase: 02-native-media3-playback-background-control
plan: "06"
type: execute
wave: 5
depends_on: [02-05]
files_modified:
  - app/listen1_chrome_extension/js/lowebutil.js
  - app/listen1_chrome_extension/js/l1_player.js
  - app/listen1_chrome_extension/js/player_thread.js
  - app/listen1_chrome_extension/test/android_native_playback_bridge.test.js
  - app/listen1_chrome_extension/test/android_native_playback_cutover.test.js
autonomous: true
requirements: [PLAY-001, PLAY-003, PLAY-004, PLAY-005]
estimate: { tokens: 41000, raw_tokens: 41000, tasks: 2, confidence: low }
must_haves:
  truths:
    - "D-01: after native handshake, Android controls use commands/snapshots and never construct Howl or register browser MediaSession handlers."
    - "D-04/D-05: page pending commands settle only from a newer current-epoch snapshot; repeated pending taps are ignored."
    - "Desktop/browser Howler, queue, recovery, and MediaSession behavior remains unchanged."
  artifacts:
    - { path: app/listen1_chrome_extension/js/lowebutil.js, provides: native playback request/subscription adapter }
    - { path: app/listen1_chrome_extension/js/l1_player.js, provides: Android-native platform player facade }
    - { path: app/listen1_chrome_extension/test/android_native_playback_cutover.test.js, provides: dual-owner and desktop-regression guard }
  key_links:
    - { from: l1_player.js, to: lowebutil.js, via: Android capability routes controls to typed native intents }
    - { from: player_thread.js, to: l1_player.js, via: Android handshake prevents page-owned Howler path }
---
<objective>Cut Android playback ownership from Howler to the native command/snapshot facade while preserving desktop/browser behavior.</objective>
<execution_context>
@/Users/fluenteng/.codex/gsd-core/workflows/execute-plan.md
@/Users/fluenteng/.codex/gsd-core/templates/summary.md
</execution_context>
<execution_gate>Before every commit/push, run `$run-local-ci` on exact content. No merge/deploy.</execution_gate>
<context>
@.planning/phases/02-native-media3-playback-background-control/02-CONTEXT.md
@.planning/phases/02-native-media3-playback-background-control/02-UI-SPEC.md
@.planning/phases/02-native-media3-playback-background-control/02-05-SUMMARY.md
@app/listen1_chrome_extension/test/android_bilibili_foreground_playback.test.js
@app/listen1_chrome_extension/test/player_play_next_queue.test.js
</context>
<file_ownership>Plan 02-06 exclusively owns lowebutil/l1_player/player_thread and the two new contract tests.</file_ownership>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Subscribe to monotonic native snapshots and send idempotent commands</name>
  <files>app/listen1_chrome_extension/js/lowebutil.js, app/listen1_chrome_extension/test/android_native_playback_bridge.test.js</files>
  <behavior>
    - Handshake retains one listener/epoch; prepare sends exact bounded domain fields and native handles arrive only in a newer snapshot.
    - Snapshot revisions only increase; wrong epoch/schema/identity is ignored without digest or announcement.
    - Commands expose pending/settled state, reject duplicate pending intent, and cancel on page teardown.
    - Safe adapter errors never contain native/provider/transport details.
  </behavior>
  <action>Extend the adapter with `connect`, `prepareSelection`, `selectPrepared`, control `command`, `subscribe`, and `detach` on the same object per D-04/D-05. Prepare serializes only source/BVID/CID/plain title/artist/optional bounded duration/audio kind and rejects artwork/playback URLs, candidates, headers, cookies, methods, bodies, native objects, and provider JSON. Native trackHandle/occurrenceId are accepted only from a newer current-epoch snapshot; select references only that pair plus action/playWhenReady/expectedRevision. Tests cover exact payload keys, caller-minted/replayed handles, stale epoch/revision, transport-field rejection, and reconnect without replay.</action>
  <verify><automated>node app/listen1_chrome_extension/test/android_native_playback_bridge.test.js &amp;&amp; node app/listen1_chrome_extension/test/android_rpc_contract.test.js</automated></verify>
  <done>The page has one epoch-safe command/snapshot client with no local playback truth.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Route every Android player facade method to native and block dual ownership</name>
  <files>app/listen1_chrome_extension/js/l1_player.js, app/listen1_chrome_extension/js/player_thread.js, app/listen1_chrome_extension/test/android_native_playback_cutover.test.js</files>
  <behavior>
    - Android play/pause/seek/prev/next/volume/mute/mode/queue/retry methods send typed commands after handshake.
    - Android `Howl` creation, Howler progress ownership, local queue mutation, and navigator.mediaSession registration do not occur after cutover.
    - Track/part selection submits logical descriptor intent, not a URL or MediaItem.
    - Desktop/browser tests observe unchanged behavior.
  </behavior>
  <action>Per D-01/D-06, implement selection as prepare bounded domain identity → await native-minted handles/revision → selectPrepared. Status is snapshot-only and no command optimistically mutates final state. Guard before Howl allocation/browser media-session setup after trusted native capability; do not retain Android Howler fallback. Discard Phase-1 candidate arrays/URLs at cutover and never put them in prepare/select payloads. Test the full hand-off through a fake bridge, every facade method, replay/stale/teardown, transport-field absence, no-Howl construction, and full legacy suite.</action>
  <verify><automated>node app/listen1_chrome_extension/test/android_native_playback_cutover.test.js &amp;&amp; npm --prefix app/listen1_chrome_extension test</automated></verify>
  <done>Android has one native audio owner and desktop/browser playback remains green.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Native snapshots → Angular state | Page renders native playback truth. |
| Legacy facade → platform branch | Android and desktop choose different owners. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-15 | Tampering | stale snapshot | high | mitigate | Epoch/revision/schema validation and no-digest stale tests. |
| T-02-16 | Denial of Service | dual owner | high | mitigate | No-Howl/no-browser-session contract after handshake. |
| T-02-17 | Information Disclosure | JS state | high | mitigate | Logical identity/safe snapshot only; transport-field fixture rejection. |
</threat_model>
<verification>Run both new tests, the entire extension suite, and authoritative local CI.</verification>
<success_criteria>- Android Howler ownership ends at native handshake. - Commands and UI await newer snapshots. - Desktop/browser regressions stay green.</success_criteria>
<output>Create `.planning/phases/02-native-media3-playback-background-control/02-06-SUMMARY.md` when done.</output>
