---
phase: 02-native-media3-playback-background-control
plan: "08"
type: execute
wave: 7
depends_on: [02-07]
files_modified:
  - android/app/src/androidTest/java/com/dazzlingwuming/listen2/PlaybackServiceInstrumentationTest.java
  - android/app/src/androidTest/java/com/dazzlingwuming/listen2/PlaybackRecoveryInstrumentationTest.java
  - android/app/src/androidTest/java/com/dazzlingwuming/listen2/PlaybackSystemControlsInstrumentationTest.java
  - android/app/src/androidTest/java/com/dazzlingwuming/listen2/PlaybackInstrumentationFixture.java
  - android/scripts/phase02-process-death-smoke.sh
autonomous: true
requirements: [PLAY-001, PLAY-003, PLAY-004, PLAY-005, PLAY-006, DATA-001]
estimate: { tokens: 44000, raw_tokens: 44000, tasks: 3, confidence: low }
must_haves:
  truths:
    - "D-18: API-35 instrumentation observes one player/session and page/controller/session agreement for every basic control."
    - "D-13/D-16: screen off, Activity/renderer loss, rotation, reconnect, noisy/focus, and restart end in explicit state without command replay or queue double-consumption."
    - "D-03: idle playback releases foreground/high-cost service state."
  artifacts:
    - { path: android/app/src/androidTest/java/com/dazzlingwuming/listen2/PlaybackServiceInstrumentationTest.java, provides: sole owner/control parity test }
    - { path: android/app/src/androidTest/java/com/dazzlingwuming/listen2/PlaybackRecoveryInstrumentationTest.java, provides: lifecycle/checkpoint recovery test }
    - { path: android/app/src/androidTest/java/com/dazzlingwuming/listen2/PlaybackSystemControlsInstrumentationTest.java, provides: notification/focus/noisy/system command test }
  key_links:
    - { from: PlaybackSystemControlsInstrumentationTest.java, to: PlaybackService.java, via: MediaController/session commands and observable snapshots }
---
<objective>Prove the real installed Media3/service/Room/page lifecycle on API 35 with deterministic native media fixtures.</objective>
<execution_context>
@/Users/fluenteng/.codex/gsd-core/workflows/execute-plan.md
@/Users/fluenteng/.codex/gsd-core/templates/summary.md
</execution_context>
<execution_gate>Before every commit/push, run `$run-local-ci` on exact content. No merge/deploy.</execution_gate>
<context>
@.planning/phases/02-native-media3-playback-background-control/02-04-SUMMARY.md
@.planning/phases/02-native-media3-playback-background-control/02-05-SUMMARY.md
@.planning/phases/02-native-media3-playback-background-control/02-07-SUMMARY.md
@android/app/src/androidTest/java/com/dazzlingwuming/listen2/Phase01WebViewInstrumentationTest.java
</context>
<file_ownership>Plan 02-08 exclusively owns the four new androidTest files and the host-driven process-death script; production seams are consumed without refactoring.</file_ownership>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Prove one service/player/session and control parity</name>
  <files>android/app/src/androidTest/java/com/dazzlingwuming/listen2/PlaybackInstrumentationFixture.java, android/app/src/androidTest/java/com/dazzlingwuming/listen2/PlaybackServiceInstrumentationTest.java</files>
  <action>Create a deterministic test fixture using Media3 `SilenceMediaSource` through the production resolver/player test seam, never a public URL or test-only page command. On API 35 connect multiple MediaControllers plus packaged page, assert one service/player/session identity, and drive play/pause/seek/volume/mute/previous/next. Poll/latch on native revisions rather than fixed sleeps; pause must halt and resume advance position, and action availability/metadata must agree.</action>
  <verify><automated>cd android &amp;&amp; gradle --no-daemon :app:connectedDebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=com.dazzlingwuming.listen2.PlaybackServiceInstrumentationTest</automated></verify>
  <done>Installed API-35 tests prove one owner and basic-control agreement.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Prove duplicate FIFO, lifecycle disconnect, and checkpoint recovery</name>
  <files>android/app/src/androidTest/java/com/dazzlingwuming/listen2/PlaybackRecoveryInstrumentationTest.java, android/app/src/androidTest/java/com/dazzlingwuming/listen2/PlaybackInstrumentationFixture.java, android/scripts/phase02-process-death-smoke.sh</files>
  <action>Implement Stage A instrumentation that seeds deterministic current occurrence, duplicate queue, mode, accepted-history cursor, and nonzero position, waits for a committed checkpoint, and reports only checkpoint revision plus bounded semantic expectations. The host script runs Stage A, executes `adb shell am force-stop com.dazzlingwuming.listen2.debug`, requires empty `pidof`, relaunches with `adb shell am start -W -S -n com.dazzlingwuming.listen2.debug/.MainActivity`, waits for page/controller reconnect, then runs Stage B. Stage B must assert paused/actionable restoration of the same occurrence, exact queue order, mode, history cursor, revision not older than Stage A, and position within five seconds. Scan Room/snapshot output for no signed candidate/URL/header/cookie fields; any missing stage, surviving process, identity drift, autoplay, or mismatch exits nonzero. Retain rotation and renderer-disconnect assertions.</action>
  <verify><automated>bash android/scripts/phase02-process-death-smoke.sh --verify</automated></verify>
  <done>Renderer/Activity/service recovery preserves semantics without second ownership or double consumption.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Prove notification, noisy/focus, screen-off, and idle release</name>
  <files>android/app/src/androidTest/java/com/dazzlingwuming/listen2/PlaybackSystemControlsInstrumentationTest.java, android/app/src/androidTest/java/com/dazzlingwuming/listen2/PlaybackInstrumentationFixture.java</files>
  <action>Use MediaController/system session commands, notification inspection, UiAutomation shell support, and allowed audio-focus/noisy broadcasts to prove D-13/D-18: notification metadata/actions match snapshot; screen-off retains legal playback; focus/noisy pauses with truthful interrupted state; available media-button next is idempotent; and clearing meaningful context removes foreground notification/service cost. Bluetooth/AVRCP unavailable on emulator must be recorded as unavailable for live evidence, not treated as pass.</action>
  <verify><automated>cd android &amp;&amp; gradle --no-daemon :app:connectedDebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=com.dazzlingwuming.listen2.PlaybackSystemControlsInstrumentationTest</automated></verify>
  <done>API-35 instrumentation covers system controls/lifecycle and identifies any device-only remainder explicitly.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Android system controllers/events → session | Notification, focus, noisy, and media controls mutate playback. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-21 | Tampering | competing controllers | high | mitigate | Multi-controller revision and occurrence assertions. |
| T-02-22 | Denial of Service | lifecycle/FGS | high | mitigate | Screen-off/recovery/idle-release installed tests. |
| T-02-23 | Repudiation | unavailable Bluetooth | low | accept | Record unavailable on emulator; do not convert absence to passing evidence. |
</threat_model>
<verification>Run the three focused connected classes on the supported API-35 emulator, then local CI.</verification>
<success_criteria>- Installed deterministic Media3 paths satisfy D-18 except explicitly device/provider-dependent proof. - No build/JVM result substitutes for connected behavior.</success_criteria>
<output>Create `.planning/phases/02-native-media3-playback-background-control/02-08-SUMMARY.md` when done.</output>
