---
phase: 02-native-media3-playback-background-control
plan: "03"
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - android/app/src/main/java/com/dazzlingwuming/listen2/data/PlaybackEntities.java
  - android/app/src/main/java/com/dazzlingwuming/listen2/data/DurableRecordEntities.java
  - android/app/src/main/java/com/dazzlingwuming/listen2/data/Listen2Dao.java
  - android/app/src/main/java/com/dazzlingwuming/listen2/data/Listen2Database.java
  - android/app/src/main/java/com/dazzlingwuming/listen2/PlaybackCheckpointRepository.java
  - android/app/src/main/java/com/dazzlingwuming/listen2/PlaybackSettingsStore.java
  - android/app/src/androidTest/java/com/dazzlingwuming/listen2/PlaybackPersistenceInstrumentationTest.java
  - android/app/src/androidTest/java/com/dazzlingwuming/listen2/PlaybackMigrationInstrumentationTest.java
  - android/schemas/com.dazzlingwuming.listen2.data.Listen2Database/1.json
autonomous: true
requirements: [DATA-001, PLAY-004, PLAY-005, PLAY-006]
estimate: { tokens: 39000, raw_tokens: 39000, tasks: 2, confidence: low }
must_haves:
  truths:
    - "D-11: Room transactions persist occurrence-based queue order, base context, real history, modes, current checkpoint, and revision atomically."
    - "DATA-001: schema 1 includes migration-safe records for playlists/favorites, queue checkpoints, lyric metadata, listening history, cache catalog, and SAF references."
    - "D-06/D-12: durable state contains logical provider/domain references and bounded checkpoints, never signed candidates, cookies, headers, secrets, or per-callback position churn; settings contain only volumePercent/muted."
  artifacts:
    - path: android/app/src/main/java/com/dazzlingwuming/listen2/data/Listen2Database.java
      provides: schema-versioned Room composition root
    - path: android/app/src/main/java/com/dazzlingwuming/listen2/PlaybackCheckpointRepository.java
      provides: transactional checkpoint and queue/history boundary
    - path: android/schemas/com.dazzlingwuming.listen2.data.Listen2Database/1.json
      provides: checked schema-1 migration baseline
  key_links:
    - from: PlaybackCheckpointRepository.java
      to: Listen2Dao.java
      via: one Room transaction per accepted logical transition
---

<objective>
Create the migration-safe durable data boundary required by DATA-001 and service recovery.

Purpose: make queue/history/checkpoints transactionally recoverable without persisting transient transport material.
Output: Room schema 1, repository/settings adapters, and device migration/transaction tests.
</objective>

<execution_context>
@/Users/fluenteng/.codex/gsd-core/workflows/execute-plan.md
@/Users/fluenteng/.codex/gsd-core/templates/summary.md
</execution_context>
<execution_gate>Before every task commit or push, invoke `$run-local-ci` for the exact worktree and HEAD; failures block publication. No merge or deploy is authorized.</execution_gate>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/02-native-media3-playback-background-control/02-CONTEXT.md
@.planning/phases/02-native-media3-playback-background-control/02-RESEARCH.md
@.planning/phases/02-native-media3-playback-background-control/02-01-SUMMARY.md
</context>
<file_ownership>Plan 02-03 exclusively owns the data package, repository/settings adapters, schema-1 export, and the two persistence instrumentation tests.</file_ownership>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Persist playback semantics in one Room transaction</name>
  <reversibility rating="one-way">D-11/DATA-001 create the on-disk schema later phases will migrate; incompatible changes require explicit migrations for installed users.</reversibility>
  <files>android/app/src/main/java/com/dazzlingwuming/listen2/data/PlaybackEntities.java, android/app/src/main/java/com/dazzlingwuming/listen2/data/DurableRecordEntities.java, android/app/src/main/java/com/dazzlingwuming/listen2/data/Listen2Dao.java, android/app/src/main/java/com/dazzlingwuming/listen2/data/Listen2Database.java, android/app/src/main/java/com/dazzlingwuming/listen2/PlaybackCheckpointRepository.java</files>
  <behavior>
    - One transition commits checkpoint revision, current/base context, occurrence order, mode, and history cursor atomically.
    - Replaying the same transition token is idempotent; stale revision rolls back without partial rows.
    - Duplicate tracks have separate occurrence primary keys and stable ordinal order.
    - All DATA-001 record categories have typed schema-owned tables and foreign-key/index strategy.
  </behavior>
  <action>
Implement Room schema version 1 per D-11 with normalized playback checkpoint, queue occurrence, history, playlist/favorite, lyric metadata, listening-history, cache-catalog, and SAF-reference records. This phase establishes safe durable shapes and DAOs; later feature phases populate their domain-specific tables. Use unique occurrence keys, foreign keys, explicit ordering columns, indices, transactions, and exported schemas. `PlaybackCheckpointRepository.applyTransition` must compare expected revision and write the semantic transition plus checkpoint atomically. Per D-06/D-12, fields are bounded logical references/metadata only; do not add media candidates, URLs, credentials, raw provider JSON, arbitrary file paths, or exception text.
  </action>
  <verify><automated>cd android &amp;&amp; gradle --no-daemon :app:compileDebugJavaWithJavac :app:assembleDebug</automated></verify>
  <done>Schema 1 and repository compile with transaction, identity, ordering, and sensitive-data boundaries explicit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove schema 1, transaction rollback, restore, and small-settings isolation</name>
  <files>android/app/src/main/java/com/dazzlingwuming/listen2/PlaybackSettingsStore.java, android/app/src/androidTest/java/com/dazzlingwuming/listen2/PlaybackPersistenceInstrumentationTest.java, android/app/src/androidTest/java/com/dazzlingwuming/listen2/PlaybackMigrationInstrumentationTest.java, android/schemas/com.dazzlingwuming.listen2.data.Listen2Database/1.json</files>
  <behavior>
    - Schema export is present and MigrationTestHelper opens the schema-1 database without destructive fallback.
    - Transaction failure leaves the previous checkpoint/queue/history intact.
    - Restore round-trips duplicates, order, modes, history cursor, volume/mute, and bounded position.
    - The one application-scoped settings adapter accepts only small non-sensitive preference keys.
  </behavior>
  <action>
Create one application-scoped `PlaybackSettingsStore` over one named platform `SharedPreferences` file, exposing exactly `volumePercent` integer 0–100 and `muted` boolean plus a local migration seam; reject all other keys and list/metadata/transport/secret-shaped values. Add device tests that create schema 1 from the checked export, insert all DATA-001 record categories, exercise atomic transition success/stale/failure, close/reopen, and verify no destructive migration path. Scan exported schema and rows for forbidden transport/credential field names. Use in-memory Room only for transaction cases; the migration test must use the exported schema file.
  </action>
  <verify><automated>cd android &amp;&amp; gradle --no-daemon :app:connectedDebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=com.dazzlingwuming.listen2.PlaybackPersistenceInstrumentationTest,com.dazzlingwuming.listen2.PlaybackMigrationInstrumentationTest</automated></verify>
  <done>DATA-001 schema and restore behavior pass on the API-35 device without destructive fallback or sensitive durable fields.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Coordinator → Room | Concurrent logical transitions become durable user-visible state. |
| Provider descriptor → checkpoint | Transient authorized playback data is reduced to logical references. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-06 | Tampering | Room transaction | high | mitigate | Expected revision, unique constraints, foreign keys, rollback and stale-write tests. |
| T-02-07 | Information Disclosure | schema/checkpoints | high | mitigate | Field allow-list plus schema/row scan excluding transport and credential material. |
| T-02-08 | Denial of Service | checkpoint writes | medium | mitigate | Logical-transition/seek/interval cadence contract; no raw position-callback writes. |
</threat_model>
<verification>Run compile/build, focused connected persistence tests on API 35, inspect the schema export, then local CI before publication.</verification>
<success_criteria>
- DATA-001 has a checked schema-1 migration baseline and transaction evidence.
- D-11/D-12 recovery state is durable, occurrence-based, bounded, and secret-free.
</success_criteria>
<output>Create `.planning/phases/02-native-media3-playback-background-control/02-03-SUMMARY.md` when done.</output>
