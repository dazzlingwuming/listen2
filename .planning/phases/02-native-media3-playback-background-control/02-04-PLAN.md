---
phase: 02-native-media3-playback-background-control
plan: "04"
type: execute
wave: 3
depends_on: [02-02, 02-03]
files_modified:
  - android/app/src/main/java/com/dazzlingwuming/listen2/PlaybackCoordinator.java
  - android/app/src/main/java/com/dazzlingwuming/listen2/PlaybackMediaResolver.java
  - android/app/src/main/java/com/dazzlingwuming/listen2/PlaybackService.java
  - android/app/src/main/AndroidManifest.xml
  - android/app/src/test/java/com/dazzlingwuming/listen2/PlaybackCoordinatorTest.java
  - android/app/src/test/java/com/dazzlingwuming/listen2/PlaybackMediaResolverTest.java
autonomous: true
requirements: [PLAY-001, PLAY-003, PLAY-004, PLAY-005, PLAY-006, DATA-001]
estimate: { tokens: 48000, raw_tokens: 48000, tasks: 3, confidence: low }
must_haves:
  truths:
    - "D-01/D-03: one MediaSessionService owns exactly one ExoPlayer/MediaSession and releases foreground/high-cost state when no meaningful context remains."
    - "D-08/D-10: page, session, player-ended, and media-button transitions serialize through one coordinator and cannot double-consume or silently skip."
    - "D-06/D-12/D-13: prepare validates bounded Bilibili domain identity, native mints track/occurrence handles, select consumes only those handles, and restore re-resolves without durable candidates."
  artifacts:
    - { path: android/app/src/main/java/com/dazzlingwuming/listen2/PlaybackService.java, provides: sole Media3 service/session/player owner }
    - { path: android/app/src/main/java/com/dazzlingwuming/listen2/PlaybackCoordinator.java, provides: serialized semantic transition lane }
    - { path: android/app/src/main/AndroidManifest.xml, provides: legal non-exported mediaPlayback foreground service declaration }
  key_links:
    - { from: PlaybackCoordinator.java, to: PlaybackCheckpointRepository.java, via: persist transition before projecting player state }
    - { from: PlaybackService.java, to: PlaybackCoordinator.java, via: all session/player callbacks delegate to one lane }
---

<objective>Build the one native playback owner and connect durable semantic transitions to Media3 without involving the page lifecycle.</objective>
<execution_context>
@/Users/fluenteng/.codex/gsd-core/workflows/execute-plan.md
@/Users/fluenteng/.codex/gsd-core/templates/summary.md
</execution_context>
<execution_gate>Before every task commit or push, invoke `$run-local-ci` for the exact worktree and HEAD. Failure blocks publication; no merge/deploy is authorized.</execution_gate>
<context>
@.planning/phases/02-native-media3-playback-background-control/02-CONTEXT.md
@.planning/phases/02-native-media3-playback-background-control/02-RESEARCH.md
@.planning/phases/02-native-media3-playback-background-control/02-02-SUMMARY.md
@.planning/phases/02-native-media3-playback-background-control/02-03-SUMMARY.md
@android/app/src/main/java/com/dazzlingwuming/listen2/BilibiliResponseMapper.java
</context>
<file_ownership>Plan 02-04 exclusively owns coordinator, resolver, service, manifest, and their JVM tests. MainActivity/bridge/frontend remain untouched.</file_ownership>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Serialize every semantic transition before Media3 projection</name>
  <files>android/app/src/main/java/com/dazzlingwuming/listen2/PlaybackCoordinator.java, android/app/src/test/java/com/dazzlingwuming/listen2/PlaybackCoordinatorTest.java</files>
  <behavior>
    - Page, session, natural-end, retry, noisy/focus, and restore inputs share one ordered lane.
    - Accepted transition commits Room state before one player projection and one snapshot revision.
    - Duplicate/stale event tokens do not mutate state or publish a second revision.
    - Seek completion and bounded position intervals checkpoint; raw callbacks do not.
  </behavior>
  <action>Write fake player/repository tests first, then implement `PlaybackCoordinator` around the 02-02 engine and 02-03 repository. Per D-08/D-09/D-12, normalize every input into an event token plus expected revision; commit transactionally before calling the player port; publish exactly one snapshot. A projection failure retains the selected occurrence and records an actionable error, never advances. Include an injected serial executor/clock so races and checkpoint cadence require no sleeps.</action>
  <verify><automated>cd android &amp;&amp; gradle --no-daemon :app:testDebugUnitTest --tests com.dazzlingwuming.listen2.PlaybackCoordinatorTest</automated></verify>
  <done>Deterministic tests prove transaction-before-projection, one transition result, checkpoint cadence, and no double consumption.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Re-resolve and retry only the current occurrence</name>
  <files>android/app/src/main/java/com/dazzlingwuming/listen2/PlaybackMediaResolver.java, android/app/src/test/java/com/dazzlingwuming/listen2/PlaybackMediaResolverTest.java</files>
  <behavior>
    - Prepare accepts only source/BVID/CID/title/artist/optional duration/audio kind and mints the opaque track/occurrence pair.
    - Select accepts only the minted pair, expected revision, action, and playWhenReady; stale, replayed, or unregistered pairs fail.
    - Candidate attempts are ordered, deduplicated, bounded, occurrence/revision scoped, and cancellable.
    - Expired/unsafe/unsupported candidates fail safely; terminal failure retains current context.
    - Restore stores/reuses no signed candidate and returns paused/actionable when refresh is unavailable.
  </behavior>
  <action>Per D-06/D-10/D-12, implement the 02-01 prepare→mint→select hand-off. Prepare accepts the exact bounded domain descriptor, registers a native-minted `trackHandle`/`occurrenceId`, and calls the existing Phase-1 typed Bilibili manifest seam internally so candidates never arrive from or return to the page. Select requires a registered pair and expected revision before coordinator mutation. Reject stale, replayed, and unregistered pairs. Tests prove safe prepare fields, native identity minting, select replay/staleness, candidate cap/order/cancellation/expiry, restore refresh failure, and zero transport material in snapshots/checkpoints.</action>
  <verify><automated>cd android &amp;&amp; gradle --no-daemon :app:testDebugUnitTest --tests com.dazzlingwuming.listen2.PlaybackMediaResolverTest</automated></verify>
  <done>Media recovery is current-occurrence-bound, finite, stale-safe, and durable-state-free.</done>
</task>
<task type="auto">
  <name>Task 3: Own ExoPlayer and MediaSession in one legal playback service</name>
  <files>android/app/src/main/java/com/dazzlingwuming/listen2/PlaybackService.java, android/app/src/main/AndroidManifest.xml</files>
  <action>Per D-01/D-03/D-13, implement a final `MediaSessionService` that constructs exactly one `ExoPlayer`, one `MediaSession`, coordinator, repository, resolver, and bounded SharedPreferences settings adapter. Use the standard Media3 notification: play/pause plus previous/next only when the snapshot advertises them; timeline seek only when duration/seekability are known; no custom seek/volume/mute buttons. All surfaces use sanitized title/artist/source and the bundled neutral artwork placeholder—no artwork URL or bitmap crosses the bridge. Support defensive playback resumption and idempotent checkpoint/release. Add only the two foreground-service permissions and a non-exported mediaPlayback service; never start from BOOT_COMPLETED; release high-cost state when idle.</action>
  <verify><automated>cd android &amp;&amp; gradle --no-daemon :app:testDebugUnitTest :app:assembleDebug :app:assembleRelease</automated></verify>
  <done>The APK contains one legal Media3 playback service and no Activity/page-owned player.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Session/page/player callbacks → coordinator | Multiple authorities request playback mutation. |
| Logical identity → transient candidate | Authorized metadata becomes a playable MediaSource. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-09 | Tampering | coordinator lane | high | mitigate | Event/revision idempotency and transaction-before-projection tests. |
| T-02-10 | Information Disclosure | resolver | high | mitigate | Transient candidates only; safe DTO/checkpoint tests. |
| T-02-11 | Denial of Service | foreground service | high | mitigate | Legal start path, bounded retry, idle clear/release, lifecycle instrumentation in 02-08. |
</threat_model>
<verification>Run focused JVM tests, debug/release assembly, manifest inspection, and local CI.</verification>
<success_criteria>- One MediaSessionService is the only native owner. - Queue/history/checkpoint semantics precede player projection. - Retry, focus, restore, and idle lifecycle are explicit and testable.</success_criteria>
<output>Create `.planning/phases/02-native-media3-playback-background-control/02-04-SUMMARY.md` when done.</output>
