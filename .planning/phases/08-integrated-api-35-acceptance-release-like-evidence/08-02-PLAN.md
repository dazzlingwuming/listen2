---
phase: 08-integrated-api-35-acceptance-release-like-evidence
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - mobile/scripts/acceptance/fixtures/phase08.json
  - mobile/scripts/acceptance/generate-fixtures.mjs
  - mobile/scripts/acceptance/avd-manager.sh
  - mobile/scripts/acceptance/run-api35-journey.sh
  - mobile/android/app/src/androidTest/java/com/listen2mobile/acceptance/AccessibilityDriver.kt
  - mobile/android/app/src/androidTest/java/com/listen2mobile/acceptance/UpgradeSeedTest.kt
  - mobile/android/app/src/androidTest/java/com/listen2mobile/acceptance/IntegratedJourneyTest.kt
  - ".planning/phases/08-integrated-api-35-acceptance-release-like-evidence/evidence/<runId>/08-journey.json"
autonomous: true
requirements: [TEST-002, TEST-003, TEST-004, REL-002]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "An evaluator can install the exact hashed release-like candidate on API 35 and complete one uninterrupted phone journey through every Phase 4–7 user domain per D-07."
    - "Five-source searches use production routes and fixed semantic expectations; live provider/account/codec outcomes remain independently truthful per D-05 and D-06."
    - "Native playback, notification/background control, Room/library/history, SAF/local, backup, offline/cache, MV/PiP, effects/loudness, DeepSeek and navigation evidence all belong to the same run and APK hash."
    - "Device UI evidence covers phone geometry, rotation, 200% font, contrast/reduced motion, TalkBack order and safe Back/external navigation without leaking user data."
  artifacts:
    - path: mobile/android/app/src/androidTest/java/com/listen2mobile/acceptance/IntegratedJourneyTest.kt
      provides: Accessibility-driven integrated API 35 user journey and native/system assertions
    - path: mobile/scripts/acceptance/fixtures/phase08.json
      provides: Sanitized five-source/local/recovery fixture contract
    - path: mobile/scripts/acceptance/run-api35-journey.sh
      provides: Serial-bound install, device mutation, journey, evidence and cleanup harness
    - path: ".planning/phases/08-integrated-api-35-acceptance-release-like-evidence/evidence/<runId>/08-journey.json"
      provides: Schema-valid integrated journey evidence bound to the release-like hash
  key_links:
    - from: mobile/scripts/acceptance/run-api35-journey.sh
      to: mobile/android/app/src/androidTest/java/com/listen2mobile/acceptance/IntegratedJourneyTest.kt
      via: exact API-verified serial and one class-filtered releaseLike instrumentation invocation
      pattern: "am instrument.*-e class com.listen2mobile.acceptance.IntegratedJourneyTest"
    - from: mobile/android/app/src/androidTest/java/com/listen2mobile/acceptance/IntegratedJourneyTest.kt
      to: mobile/scripts/acceptance/fixtures/phase08.json
      via: validated source order/query/local media hashes and expected semantic states
      pattern: "phase08"
    - from: mobile/scripts/acceptance/run-api35-journey.sh
      to: mobile/scripts/acceptance/evidence.mjs
      via: sanitized screenshots/system probes/outcomes written against one APK SHA
      pattern: "evidence.mjs"
---

<objective>
Run one complete production-route user journey against the exact release-like APK on a recorded API 35 emulator.

Purpose: Replace source-level confidence with one integrated mobile UI, native, storage, playback and recovery proof while keeping external account/provider gaps honest.
Output: Sanitized fixture manifest/generator, accessibility driver, integrated instrumentation, serial-bound runner and schema-valid journey evidence.
D-04/D-07: API 35 owns the full uninterrupted journey; state resets occur only at the recorded start and prescribed recovery steps.
</objective>

<execution_context>
@/Users/fluenteng/.codex/gsd-core/workflows/execute-plan.md
@/Users/fluenteng/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-integrated-api-35-acceptance-release-like-evidence/08-CONTEXT.md
@.planning/phases/08-integrated-api-35-acceptance-release-like-evidence/08-RESEARCH.md
@.planning/phases/08-integrated-api-35-acceptance-release-like-evidence/08-UI-SPEC.md
@.planning/phases/08-integrated-api-35-acceptance-release-like-evidence/08-VALIDATION.md
@.planning/phases/08-integrated-api-35-acceptance-release-like-evidence/08-01-SUMMARY.md

<interfaces>
Existing screen labels include source tabs, `搜索歌曲、歌手或歌单`, mini-player/player controls, local import/repair/remove, cache management, backup preview/merge/overwrite, account QR/logout, DeepSeek configure/test/clear, MV/PiP and history controls.

Existing native instrumentation covers `LibraryMigrationTest`, `LibraryRepositoryInstrumentationTest`, `BackupTransactionInstrumentationTest`, `SafImportInstrumentationTest`, `LocalMediaProviderInstrumentationTest` and `ListeningLedgerInstrumentationTest`.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Execute the complete API 35 integrated user journey once</name>
  <files>mobile/scripts/acceptance/fixtures/phase08.json, mobile/scripts/acceptance/generate-fixtures.mjs, mobile/scripts/acceptance/avd-manager.sh, mobile/scripts/acceptance/run-api35-journey.sh, mobile/android/app/src/androidTest/java/com/listen2mobile/acceptance/AccessibilityDriver.kt, mobile/android/app/src/androidTest/java/com/listen2mobile/acceptance/UpgradeSeedTest.kt, mobile/android/app/src/androidTest/java/com/listen2mobile/acceptance/IntegratedJourneyTest.kt</files>
  <behavior>
    - Fixture generator creates a ten-minute synthetic mono WAV and deterministic LRC outside source control, records SHA-256/size/duration, and never uses copyrighted/user media.
    - On a fresh API 35 install, the local shell is interactive and the five source tabs appear exactly NetEase, Kugou, Kuwo, QQ, Bilibili with query `青花瓷` retained across source changes.
    - The runner seeds safe semantic library/queue/settings data in the diagnostic debug baseline, upgrades in place to the higher-version release-like candidate, and the cold candidate shell reads the same data through current migrations.
    - Immediately after debug installation and before `UpgradeSeedTest`, the host performs exactly one package-data reset for `com.dazzlingwuming.listen2`, records its terminal result, and verifies an empty baseline; no later clear/reset or candidate substitution is permitted.
    - The host runner installs the three APKs recorded by `08-build.json`: debug app plus the black-box releaseLike AndroidTest APK, then `adb install -r` of the same releaseLike app bytes. It runs only `UpgradeSeedTest` on debug and exactly one `IntegratedJourneyTest` on releaseLike with explicit serial and class filter.
    - Each source reaches a terminal result containing source/title/artist-or-author/type/duration/playability-or-action; stale, cancelled and prior-source replies cannot replace the active source.
    - NetEase and Bilibili open detail, select the intended semantic track/part, enter RNTP playback, advance position, show matching mini-player/detail/notification state, and show matching lyrics or an exact actionable live limitation.
    - Duplicate play-next entries, reorder/remove, shuffle/repeat/previous and lyric offset/manual selection retain occurrence and revision identity through rotation and Back.
    - Personal library/history, SAF local audio/LRC, backup, owner-aware cache/offline, rendition/MV/PiP, effects/visualizer/loudness, DeepSeek, background/notification/audio-focus, renderer/process recovery and navigation all execute in the same candidate session.
    - Every device/network/accessibility mutation is snapshotted before use and restored by a trap; the result binds to one APK hash and run ID.
    - The runner rejects credential-like arguments/environment/files and performs no Bilibili QR or DeepSeek secret injection; absent credentials are `NOT_VERIFIED`, while an actually reached external limitation is `DEGRADED` per D-06.
  </behavior>
  <action>Create the sanitized fixture manifest/generator and accessibility driver. Implement black-box `UpgradeSeedTest` only to drive the real debug UI and persist bounded playlist/duplicate queue/settings/migration markers after the one initial clear. Implement `IntegratedJourneyTest` as the one releaseLike journey through the complete D-07/UI-spec path; it consumes production routes and records live semantic outcomes. Implement `avd-manager.sh` for exact `system-images;android-{26,35,36};google_apis;HOST_ABI` contracts: arm64 host uses `arm64-v8a`, x86_64 host uses `x86_64`; missing installed image writes `BLOCKED` plus exact `sdkmanager` command and stops; existing AVDs must match image/ABI or stop; created AVDs get a run marker; the returned serial is verified by `ro.boot.qemu.avd_name`, API and ABI; cleanup stops the launched PID and deletes only a run-created AVD. Implement `run-api35-journey.sh` to resolve the one run root/current HEAD, require `--serial`, reject credential-like args/env and any credential file channel, verify API 35/image/ABI, snapshot/arm traps, and hash-check all three APKs. Run `adb -s "$SERIAL" install -r "$DEBUG_APK"`, then exactly once run `adb -s "$SERIAL" shell pm clear com.dazzlingwuming.listen2`; record command ID, time, exit, package and verified empty baseline in the reset section of `08-journey.json`. Install `$RELEASE_LIKE_TEST_APK`, then execute exactly `adb -s "$SERIAL" shell am instrument -w -r -e class com.listen2mobile.acceptance.UpgradeSeedTest com.dazzlingwuming.listen2.test/androidx.test.runner.AndroidJUnitRunner`. Next execute `adb -s "$SERIAL" install -r "$RELEASE_LIKE_APK"` against the same recorded hash, verify the same test APK/package remains installed, and execute exactly `adb -s "$SERIAL" shell am instrument -w -r -e class com.listen2mobile.acceptance.IntegratedJourneyTest com.dazzlingwuming.listen2.test/androidx.test.runner.AndroidJUnitRunner`. After the recorded reset, reject any second package-data clear, uninstall, data-directory deletion, app reset, rebuild, or candidate/test-APK hash change; run the releaseLike class exactly once. Capture only sanitized run-root artifacts and atomically write `08-journey.json`. Per D-05/D-06/D-07, no mock authority exists and missing secure user interaction is fixed `NOT_VERIFIED`.</action>
  <verify>
    <automated>node mobile/scripts/acceptance/generate-fixtures.mjs --self-test &amp;&amp; bash -n mobile/scripts/acceptance/avd-manager.sh mobile/scripts/acceptance/run-api35-journey.sh &amp;&amp; RUN_DIR="$(node mobile/scripts/acceptance/evidence.mjs --resolve-current-run --phase-dir .planning/phases/08-integrated-api-35-acceptance-release-like-evidence --head "$(git rev-parse HEAD)")" &amp;&amp; SERIAL="$(bash mobile/scripts/acceptance/avd-manager.sh --run-dir "$RUN_DIR" --start --api 35)" &amp;&amp; bash mobile/scripts/acceptance/run-api35-journey.sh --run-dir "$RUN_DIR" --serial "$SERIAL" --seed-class com.listen2mobile.acceptance.UpgradeSeedTest --journey-class com.listen2mobile.acceptance.IntegratedJourneyTest</automated>
  </verify>
  <done>The exact release-like APK completes one uninterrupted, production-route API 35 journey covering every required Phase 4–7 user/system domain and writes sanitized evidence without hidden network authority.</done>
</task>

<task type="auto">
  <name>Task 2: Validate and seal the single journey evidence without rerunning it</name>
  <files>.planning/phases/08-integrated-api-35-acceptance-release-like-evidence/evidence/&lt;runId&gt;/08-journey.json</files>
  <behavior>
    - Schema, run ID, git SHA, APK hash/signature, API/image and fixture hash match `08-build.json` and retained artifacts.
    - Required domain outcomes and screenshots exist once; exactly one successful reset record precedes `UpgradeSeedTest`, while duplicate record/command/requirement IDs, missing terminal states, any later clear/reset, and any candidate hash change are rejected.
    - All external credential/provider/CDN/codec limitations retain `DEGRADED` or `NOT_VERIFIED` and cannot be promoted by deterministic local evidence.
    - APK re-hash and post-run device snapshot prove the tested artifact is unchanged and network/accessibility/rotation/font/screen settings were restored.
  </behavior>
  <action>Resolve the same run root and validate its immutable `08-journey.json`; independently recompute every contained artifact hash, rerun `apksigner` against the run-root candidate, compare pre/post device snapshots, and require exactly one terminal outcome per journey domain. Scan only that run directory with the Phase 7 scanner. Do not launch instrumentation, reinstall, repeat the journey, mutate the sealed record, or inspect another run. Missing live/account/device proof must already be an uncovered `NOT_VERIFIED`/`DEGRADED` record per D-06; otherwise validation fails.</action>
  <verify>
    <automated>RUN_DIR="$(node mobile/scripts/acceptance/evidence.mjs --resolve-current-run --phase-dir .planning/phases/08-integrated-api-35-acceptance-release-like-evidence --head "$(git rev-parse HEAD)")" &amp;&amp; node mobile/scripts/acceptance/evidence.mjs --validate "$RUN_DIR/08-journey.json" &amp;&amp; bash mobile/scripts/acceptance/verify-apk.sh --from-build-evidence "$RUN_DIR/08-build.json" --no-build &amp;&amp; node mobile/scripts/verify-phase7-security.mjs "$RUN_DIR"</automated>
  </verify>
  <done>The single journey is not rerun; its schema, hashes, signer, complete domain coverage, sanitization and cleanup all validate, with every external limitation preserved as a non-pass outcome.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Test automation -> production UI/native bridge | Automation must not gain authority unavailable to a real user. |
| Provider/account/media -> evidence | Live replies, credentials and media can contain sensitive or copyrighted data. |
| Host shell -> emulator state | Network, permissions, settings and files are mutated for recovery scenarios. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-02-01 | Elevation | Fixture/automation | critical | mitigate | Production routes only, semantic accessibility actions and no mock transport/URL/header/cookie bypass. |
| T-08-02-02 | Information disclosure | Screenshots/logs/evidence | critical | mitigate | Generated media, safe semantic projections, QR/account exclusion, schema redaction and artifact scan. |
| T-08-02-03 | Spoofing | External live status | high | mitigate | Separate PASS/DEGRADED/NOT_VERIFIED records and requirement gating; fixture results never stand for live proof. |
| T-08-02-04 | Tampering | Emulator state | medium | mitigate | Explicit serial, pre-state snapshot, cleanup traps and post-run equality checks. |
| T-08-02-05 | Denial of service | Live providers/device | low | transfer | Provider/device availability is externally controlled; bounded deadlines and truthful recovery status prevent hangs or false passes. |
| T-08-SC | Tampering | Package installs | high | mitigate | No new package dependency; Android platform automation and existing runner are used. |
</threat_model>

<verification>
Run Task 2 once against the `08-build.json` candidate. Validate `08-journey.json` with `evidence.mjs`, re-hash the installed APK, verify the release-like signer again, and confirm the cleanup record says device settings restored.
</verification>

<success_criteria>
- The exact release-like APK completes one API 35 integrated journey through every Phase 4–7 domain.
- Five-source/live/account outcomes remain honest and no deterministic fixture expands product authority.
- All screenshots, system probes and results are sanitized, hashed, schema-valid and bound to one run/APK.
</success_criteria>

<output>
Create `.planning/phases/08-integrated-api-35-acceptance-release-like-evidence/08-02-SUMMARY.md` when done.
</output>
