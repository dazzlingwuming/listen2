---
phase: 06-personal-library-continuity
plan: "07"
type: execute
wave: 7
depends_on: [06-06]
files_modified:
  - mobile/src/history/history.ts
  - mobile/src/history/__tests__/historyLedger.test.ts
  - mobile/src/store/index.ts
  - mobile/src/player/playerController.ts
  - mobile/src/player/playbackService.ts
  - mobile/src/player/__tests__/playerController.test.ts
  - mobile/src/player/__tests__/playerController.lifecycle.test.ts
  - mobile/src/player/__tests__/playbackService.test.ts
  - mobile/src/screens/HistoryScreen.tsx
  - mobile/src/screens/MyMusicScreen.tsx
  - mobile/src/screens/__tests__/historyFlow.test.tsx
  - mobile/src/history/__tests__/historyPrivacy.test.tsx
  - mobile/src/navigation/RootNavigator.tsx
  - mobile/src/navigation/types.ts
autonomous: true
requirements: [HIST-001, HIST-002, HIST-003]
estimate: { tokens: 32000, raw_tokens: 32000, tasks: 3, confidence: low }
must_haves:
  truths:
    - "Only identity-confirmed RNTP events submit evidence; track load/start never creates recent/history by itself."
    - "Ledger submission is fire-and-forget, bounded, lifecycle-safe, and cannot delay playback commands or introduce another player."
    - "History/recap display durable totals, plays, distinct songs/artists, top items, monthly trend, stable years, and honest empty/error states."
    - "Disable, safe export, and irreversible clear are usable; late pre-clear events and every alternate store cannot resurrect history."
  artifacts:
    - "playerController.lifecycle.test.ts proves load identity, stale callbacks, restart/remount, queue occurrence, and clear generation behavior."
    - "historyPrivacy.test.tsx proves disable/export/clear privacy and non-resurrection UI contracts."
    - "HistoryScreen.tsx delivers the complete mobile history/annual recap surface."
  key_links:
    - from: mobile/src/player/playbackService.ts
      to: mobile/src/history/history.ts
      via: "safe identity-confirmed semantic observations"
    - from: mobile/src/history/history.ts
      to: mobile/android/app/src/main/java/com/listen2mobile/history/HistoryPackage.kt
      via: "strict named NativeModules methods and safe snapshot projection"
    - from: mobile/src/screens/HistoryScreen.tsx
      to: mobile/src/history/history.ts
      via: "year query, recording preference, safe export, and clear receipts"
---

<objective>
Connect RNTP to the proven native ledger and deliver complete recap/privacy UX.

Purpose: Finish Phase 6 with genuine user-visible listening continuity while keeping history work off the playback critical path.
Output: Lifecycle-safe RNTP adapter, disposable history projection, history/recap screen, safe export, disable, and irreversible clear.
</objective>

<execution_context>
@/Users/fluenteng/.codex/gsd-core/workflows/execute-plan.md
@/Users/fluenteng/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-personal-library-continuity/06-CONTEXT.md
@.planning/phases/06-personal-library-continuity/06-UI-SPEC.md
@.planning/phases/06-personal-library-continuity/06-VALIDATION.md
@.planning/phases/06-personal-library-continuity/COVERAGE.md
@.planning/phases/06-personal-library-continuity/06-06-SUMMARY.md
@mobile/src/player/playerController.ts
@mobile/src/player/playbackService.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Drive one valid native commit from identity-confirmed RNTP callbacks</name>
  <files>mobile/src/history/history.ts, mobile/src/player/playerController.ts, mobile/src/player/playbackService.ts, mobile/src/player/__tests__/playerController.lifecycle.test.ts, mobile/src/history/__tests__/historyLedger.test.ts</files>
  <behavior>
    - Each accepted load gets a fresh playbackInstanceId bound to active semantic track, queue occurrence, load identity, and current clearGeneration.
    - Only matching RNTP playing/progress/pause/buffer/seek/completion/error callbacks submit observations; stale previous-load callbacks are ignored.
    - Begin/load/preload/browse/failure and seek jumps cannot commit; a threshold-crossing genuine forward session appears once after native receipt/snapshot.
    - Submission does not await Room/player commands, is bounded/coalesced, preserves sequence, and reports safe diagnostics without playback rejection.
    - No media/private/signed URL, local provider URI, cookie, token, queue object, or raw error enters history DTOs.
  </behavior>
  <action>Create a consolidated strict history client/projection/adapter in `history.ts`. At accepted load, allocate a new playbackInstanceId and bind semantic identity, queue occurrence, native clearGeneration, and current player load identity; never reuse across replay/reload. Feed only authoritative matching RNTP callbacks from `playbackService`, explicitly mark seek boundaries, coalesce redundant progress while preserving sequence, bound pending work, and submit without awaiting from the player path. Remove all start/load-time `library/recordRecent` dispatches. Build an explicit safe metadata DTO and refresh projection only from native commit/snapshot receipts. Test Activity-style detach/reattach, service remount, stale callbacks, queue next/repeat, load replacement, clear-generation change, native rejection/timeout, and threshold arrival in the required lifecycle test.</action>
  <verify><automated>npm run mobile:test -- --runInBand mobile/src/player/__tests__/playerController.lifecycle.test.ts mobile/src/history/__tests__/historyLedger.test.ts</automated></verify>
  <done>One genuine RNTP session commits once; lifecycle/stale/seek/preload/failure cases do not, and no ledger submission delays playback or leaks transport data.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace recent state with a disposable native-history projection</name>
  <files>mobile/src/history/history.ts, mobile/src/store/index.ts, mobile/src/player/__tests__/playerController.test.ts, mobile/src/player/__tests__/playbackService.test.ts</files>
  <behavior>
    - Redux recreation starts unhydrated and rebuilds history/recent solely from a validated native snapshot.
    - Old start-time recent reducer state is neither persisted nor displayed and cannot overwrite native history.
    - Matching revisioned snapshot/clear receipt replaces projection; stale/error retains last confirmed data and triggers bounded reload.
    - Existing queue/seek/repeat/stop/error/background RNTP behavior remains unchanged outside the observation side channel.
  </behavior>
  <action>Register the disposable history projection and selectors without Redux Persist ownership. Derive My Music recent data from committed native entries, not player-load actions. Apply snapshot/clear receipts with revision/request correlation and last-confirmed retention. Update existing player/service regression tests to prove the observation adapter has no control over transport ordering, queue state, audio focus/service lifecycle, retry, or errors and cannot become a second player.</action>
  <verify><automated>npm run mobile:test -- --runInBand mobile/src/player/__tests__/playerController.test.ts mobile/src/player/__tests__/playbackService.test.ts mobile/src/history/__tests__/historyLedger.test.ts &amp;&amp; npm run mobile:typecheck</automated></verify>
  <done>History/recent can be discarded and rehydrated from native truth, and the observation side channel leaves all existing player semantics intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Deliver recap, disable, safe export, and irreversible clear UX</name>
  <files>mobile/src/screens/HistoryScreen.tsx, mobile/src/screens/MyMusicScreen.tsx, mobile/src/navigation/RootNavigator.tsx, mobile/src/navigation/types.ts, mobile/src/screens/__tests__/historyFlow.test.tsx, mobile/src/history/__tests__/historyPrivacy.test.tsx</files>
  <behavior>
    - My Music opens `听歌历史与年度回响`; year-scoped history/recap has loading, retained-error/retry, and honest empty states.
    - Each year displays total time, play count, distinct songs/artists, top tracks/artists, and labeled monthly trend; empty years fabricate no ranking.
    - Disable affects only future recording and preserves existing rows; re-enable begins only future sessions.
    - Export is bounded safe history/recap data and excludes local IDs/URIs/grants, queue, media URL, accounts/session, cache, backup coupling, and diagnostics.
    - Irreversible clear confirmation yields an empty matching receipt; late pre-clear events, remount/reload, backup/cache/log/remote cannot resurrect data.
  </behavior>
  <action>Add the typed child route and UI-SPEC mobile hierarchy with 48dp labeled controls, 320dp and 2x-font resilience, preserved confirmed content during refresh, explicit error/retry, accessible year selection, labeled monthly values, and honest empty recap. Load native snapshots/recaps on focus and matching receipts. Explain recording disable without implying deletion. Implement safe history export separately from portable library backup. Require the locked irreversible clear confirmation, invoke one native clear transaction, accept only its matching empty snapshot/generation, and reject late old-generation updates. Cover screen/remount/error/year cases in `historyFlow.test.tsx` and all disable/export/redaction/clear/non-resurrection cases in the required `historyPrivacy.test.tsx`.</action>
  <verify>
    <automated>npm run mobile:test -- --runInBand mobile/src/screens/__tests__/historyFlow.test.tsx mobile/src/history/__tests__/historyPrivacy.test.tsx mobile/src/player/__tests__/playerController.lifecycle.test.ts &amp;&amp; npm run mobile:typecheck</automated>
    <human-check>At 320dp and 2x font, inspect My Music → 听歌历史与年度回响 for readable wrapping, labeled monthly values, focus/order, loading/error/empty states, disable explanation, export status, and irreversible-clear confirmation. This is design-contract review, not Phase 8 APK acceptance.</human-check>
  </verify>
  <done>History and recap are accurate and accessible; disable/export/clear privacy works end to end and old data cannot reappear through lifecycle or another store.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Severity | Disposition | Mitigation |
|---|---|---:|---|---|
| T-06-07-01 | Tampering—RNTP callbacks | high | mitigate | Active identity/generation/sequence correlation and safe semantic DTO. |
| T-06-07-02 | Information disclosure—UI/export | high | mitigate | Explicit bounded export and private/runtime-field negative fixtures. |
| T-06-07-03 | Denial of service—observation path | medium | mitigate | Fire-and-forget bounded/coalesced adapter; no player awaits. |
| T-06-07-04 | Repudiation—clear race | high | mitigate | Matching clear receipt, new generation, stale/late rejection, native-only source of truth. |
</threat_model>

<verification>
Run all exact JS selectors above, then `npm run mobile:test`, `npm run mobile:typecheck`, `npm --prefix mobile run lint -- --quiet`, Java 17 Gradle `:app:testDebugUnitTest :app:compileDebugAndroidTestKotlin`, and `git diff --check`. After every Phase 6 product slice is implemented, run exactly one controlled native fixture invocation: `JAVA_HOME=/opt/homebrew/opt/openjdk@17 ANDROID_HOME=/opt/homebrew/share/android-commandlinetools ANDROID_SDK_ROOT=/opt/homebrew/share/android-commandlinetools PATH=/opt/homebrew/opt/openjdk@17/bin:$PATH ./mobile/android/gradlew --offline --no-daemon -p mobile/android :app:connectedDebugAndroidTest`. This is Phase 6's only device/APK-producing test operation; it is fixture-only and does not substitute for Phase 8 installed-product, live provider/account/document, system process-death, performance, accessibility-runtime, or release-signing gates.
</verification>

<success_criteria>
HIST-001/002/003 close through exact lifecycle, ledger, flow, and privacy selectors, with genuine native history as the only source and no first-play latency regression.
</success_criteria>

## Source Coverage Audit

| Source | Item | Coverage |
|---|---|---|
| GOAL | Durable personal library, account truth, private local audio, portable data, valid history/recap | COVERED by 06-01…06-07 |
| REQ | LIB-001, LIB-002, LIB-003 | COVERED by 06-01/02/03 |
| REQ | AUTH-001, AUTH-002, AUTH-003 | COVERED by 06-03 |
| REQ | DATA-001, DATA-002, DATA-003 | COVERED by 06-01/02/03 |
| REQ | LOCAL-001, LOCAL-002, LOCAL-003 | COVERED by 06-04/05 |
| REQ | HIST-001, HIST-002, HIST-003 | COVERED by 06-06/07 |
| RESEARCH | Room/DataStore authority, strict bridges, atomic backup | COVERED by 06-01/02/03 |
| RESEARCH | Native SAF ownership, independent package, private provider, sole RNTP | COVERED by 06-04/05 |
| RESEARCH | Monotonic ledger, independent package, stable local commit buckets, async adapter | COVERED by 06-06/07 |
| CONTEXT | Reversible migration; confirmed projections; fixed account order; explicit LRC; private identifiers; valid-listen predicate; privacy controls | COVERED by the named owning plans |
| EXCLUSION | Phase 7 cache/download/media-byte behavior | EXCLUDED as required; schema catalog only in 06-01 |
| EXCLUSION | Phase 8 installed APK/live provider/account/document/runtime acceptance | EXCLUDED as required; deterministic controlled fixtures only |

All 15 Phase 6 requirement IDs appear in plan frontmatter and executable tasks; no source item is missing.

<output>Create `.planning/phases/06-personal-library-continuity/06-07-SUMMARY.md`.</output>
