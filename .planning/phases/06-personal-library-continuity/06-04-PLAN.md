---
phase: 06-personal-library-continuity
plan: "04"
type: execute
wave: 4
depends_on: [06-03]
files_modified:
  - mobile/android/app/src/main/java/com/listen2mobile/local/LocalAudioPolicy.kt
  - mobile/android/app/src/main/java/com/listen2mobile/local/LocalMetadataReader.kt
  - mobile/android/app/src/main/java/com/listen2mobile/local/LocalAudioModule.kt
  - mobile/android/app/src/main/java/com/listen2mobile/local/LocalAudioPackage.kt
  - mobile/android/app/src/main/java/com/listen2mobile/library/LibraryRepository.kt
  - mobile/android/app/src/main/java/com/listen2mobile/MainApplication.kt
  - mobile/android/app/src/test/java/com/listen2mobile/local/LocalAudioPolicyTest.kt
  - mobile/android/app/src/test/java/com/listen2mobile/local/LocalMetadataReaderTest.kt
  - mobile/android/app/src/androidTest/java/com/listen2mobile/local/SafImportInstrumentationTest.kt
  - mobile/src/localAudio/picker.ts
  - mobile/src/localAudio/__tests__/picker.test.ts
  - mobile/src/screens/MyMusicScreen.tsx
  - mobile/src/localAudio/__tests__/localLibraryFlow.test.tsx
autonomous: true
requirements: [LOCAL-001, LOCAL-002]
estimate: { tokens: 30000, raw_tokens: 30000, tasks: 3, confidence: low }
must_haves:
  truths:
    - "Native ACTION_OPEN_DOCUMENT multi-select imports supported audio and retains read grants without broad storage permission."
    - "JavaScript receives only opaque record IDs, safe bounded metadata, artwork display data, lyric/capability state, and receipts."
    - "Tags, duration, artwork, and explicit user-selected LRC are bounded and tested independently from UI code."
  artifacts:
    - "LocalAudioPackage.kt registers local capabilities independently from LibraryPackage."
    - "LocalMetadataReaderTest.kt proves normalization, bounds, duration, artwork, and failure rules."
    - "SafImportInstrumentationTest.kt uses a controlled fake document provider to prove actual grant/import/reopen behavior."
  key_links:
    - from: mobile/android/app/src/main/java/com/listen2mobile/local/LocalAudioModule.kt
      to: mobile/android/app/src/main/java/com/listen2mobile/library/LibraryRepository.kt
      via: "native-only grant association and one safe-record transaction"
    - from: mobile/src/localAudio/picker.ts
      to: mobile/android/app/src/main/java/com/listen2mobile/local/LocalAudioPackage.kt
      via: "named import/artwork/LRC methods with safe DTO guards"
---

<objective>
Implement native-owned SAF import, bounded metadata/artwork, and explicit LRC association behind an independent local-audio package.

Purpose: Establish private local records before provider playback and repair/removal are added in 06-05.
Output: Native picker/grant/metadata module, safe TypeScript facade and UI, JVM policy/metadata tests, and fake-provider instrumentation.
</objective>

<execution_context>
@/Users/fluenteng/.codex/gsd-core/workflows/execute-plan.md
@/Users/fluenteng/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/06-personal-library-continuity/06-CONTEXT.md
@.planning/phases/06-personal-library-continuity/06-RESEARCH.md
@.planning/phases/06-personal-library-continuity/06-UI-SPEC.md
@.planning/phases/06-personal-library-continuity/06-VALIDATION.md
@.planning/phases/06-personal-library-continuity/06-03-SUMMARY.md
@mobile/src/localAudio/picker.ts
@mobile/android/app/src/main/java/com/listen2mobile/MainApplication.kt
</context>

<interfaces>
- `LocalRecord { recordId, title, artist?, album?, durationMs?, hasArtwork, lyricState, availability, seekable, capabilities }`
- `ImportReceipt { requestId, imported, duplicates, unsupported, unreadable, cancelled, revision, snapshot }`
- Named native methods: `importAudio`, `loadArtwork(recordId)`, `attachExplicitLrc(recordId)`, `cancelLocalRequest(requestId)`.

No public method accepts an arbitrary URI/path or returns a third-party URI, grant, filename, bookmark, descriptor, native handle, or raw error.
</interfaces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Import one supported fake-provider document into an opaque Room record</name>
  <files>mobile/android/app/src/main/java/com/listen2mobile/local/LocalAudioPolicy.kt, mobile/android/app/src/main/java/com/listen2mobile/local/LocalMetadataReader.kt, mobile/android/app/src/main/java/com/listen2mobile/local/LocalAudioModule.kt, mobile/android/app/src/main/java/com/listen2mobile/local/LocalAudioPackage.kt, mobile/android/app/src/androidTest/java/com/listen2mobile/local/SafImportInstrumentationTest.kt</files>
  <behavior>
    - ACTION_OPEN_DOCUMENT uses CATEGORY_OPENABLE, persistable read flags, multi-select, and no broad storage permission.
    - mp3/flac/mp4-m4a/ogg-opus/wav/webm audio pass native container/stream inspection; MIME or extension alone cannot authorize unsupported bytes.
    - A readable selection acquires its grant before one opaque record transaction and survives repository/process-style reopen.
    - Cancel, duplicate, unsupported, unreadable, late request, or grant failure performs no unintended record write.
  </behavior>
  <action>Implement an independent `LocalAudioPackage` and module that launches native ACTION_OPEN_DOCUMENT, normalizes single/ClipData results, correlates request identity/lifecycle, acquires persistable read access, validates descriptors with bounded I/O, and stores third-party URI/grant associations only in native private data linked to opaque UUID records. Do not add READ_MEDIA_AUDIO, MANAGE_EXTERNAL_STORAGE, file browsing, directory grants, arbitrary URI intake, or a generic resolver bridge. Build a controlled fake document provider inside the instrumentation suite and prove format acceptance/rejection, multi-select, duplicate, cancel, late result, grant failure, transaction rollback, and reopen.</action>
  <verify><automated>JAVA_HOME=/opt/homebrew/opt/openjdk@17 ANDROID_HOME=/opt/homebrew/share/android-commandlinetools ANDROID_SDK_ROOT=/opt/homebrew/share/android-commandlinetools PATH=/opt/homebrew/opt/openjdk@17/bin:$PATH ./mobile/android/gradlew --offline --no-daemon -p mobile/android :app:compileDebugAndroidTestKotlin</automated></verify>
  <done>A controlled system-document selection creates an opaque durable record and retained native read grant; all invalid/cancelled paths are zero-write.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register the package and prove metadata/artwork/public DTO bounds</name>
  <files>mobile/android/app/src/main/java/com/listen2mobile/MainApplication.kt, mobile/android/app/src/test/java/com/listen2mobile/local/LocalAudioPolicyTest.kt, mobile/android/app/src/test/java/com/listen2mobile/local/LocalMetadataReaderTest.kt, mobile/src/localAudio/picker.ts, mobile/src/localAudio/__tests__/picker.test.ts</files>
  <behavior>
    - LocalAudioPackage is registered separately and exposes only named local operations.
    - Metadata normalization bounds title/artist/album/duration/artwork dimensions/bytes and returns deterministic fallbacks.
    - Artwork is requested by recordId and returned as bounded display data without URI/grant/path leakage or Redux persistence.
    - TypeScript rejects every response containing private identifiers, unknown states, oversized data, or mismatched request identity.
  </behavior>
  <action>Register `LocalAudioPackage` explicitly in `MainApplication` without adding local methods to the general Library bridge. Extract pure format/metadata/artwork normalization and bounds so JVM tests cover supported types, malformed tags, invalid duration, oversized/corrupt artwork, deterministic fallback, and safe errors; instrumentation retains actual descriptor coverage. Replace raw JavaScript document-picker audio results with a strict facade over named native calls. Artwork may cross only as bounded display bytes for the requested record and must not enter Redux, Room backup, routes, logs, diagnostics, or accessibility values.</action>
  <verify><automated>JAVA_HOME=/opt/homebrew/opt/openjdk@17 ANDROID_HOME=/opt/homebrew/share/android-commandlinetools ANDROID_SDK_ROOT=/opt/homebrew/share/android-commandlinetools PATH=/opt/homebrew/opt/openjdk@17/bin:$PATH ./mobile/android/gradlew --offline --no-daemon -p mobile/android :app:testDebugUnitTest --tests 'com.listen2mobile.local.LocalAudioPolicyTest' --tests 'com.listen2mobile.local.LocalMetadataReaderTest' &amp;&amp; npm run mobile:test -- --runInBand mobile/src/localAudio/__tests__/picker.test.ts</automated></verify>
  <done>Independent package registration, metadata normalization, artwork bounds, and the safe JS DTO contract have exact passing JVM/JS selectors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add local-library import, artwork, explicit LRC, playlist and queue UI</name>
  <files>mobile/android/app/src/main/java/com/listen2mobile/local/LocalAudioModule.kt, mobile/android/app/src/main/java/com/listen2mobile/library/LibraryRepository.kt, mobile/android/app/src/androidTest/java/com/listen2mobile/local/SafImportInstrumentationTest.kt, mobile/src/screens/MyMusicScreen.tsx, mobile/src/localAudio/__tests__/localLibraryFlow.test.tsx</files>
  <behavior>
    - My Music imports multiple records, displays safe metadata/artwork/capability state, and can add a record to personal playlist or queue.
    - Explicit LRC selection attaches bounded normalized lyric content to exactly one record.
    - Audio import never scans adjacent files or requests/enumerates a directory.
    - Pending/cancel/error retains the confirmed local projection and exposes deterministic retry.
  </behavior>
  <action>Add the local section/actions using receipt-backed repository state, 48dp labeled targets, safe artwork loading, add-to-playlist, and add-to-queue semantic record operations. Implement a separate explicit LRC ACTION_OPEN_DOCUMENT request; validate request/record identity, bounded bytes/lines/encoding/BOM/newlines, acquire only required read access, and store normalized lyric metadata transactionally. Do not auto-discover adjacent LRC, request directory access, enumerate siblings, or expose the LRC document identifier. Extend fake-provider instrumentation and screen tests for explicit selection, wrong/oversized/cancelled lyric, artwork failure, rapid requests, rotation-style remount, and confirmed-state retention.</action>
  <verify><automated>npm run mobile:test -- --runInBand mobile/src/localAudio/__tests__/localLibraryFlow.test.tsx mobile/src/localAudio/__tests__/picker.test.ts &amp;&amp; JAVA_HOME=/opt/homebrew/opt/openjdk@17 ANDROID_HOME=/opt/homebrew/share/android-commandlinetools ANDROID_SDK_ROOT=/opt/homebrew/share/android-commandlinetools PATH=/opt/homebrew/opt/openjdk@17/bin:$PATH ./mobile/android/gradlew --offline --no-daemon -p mobile/android :app:compileDebugAndroidTestKotlin</automated></verify>
  <done>Users can import, inspect, attach explicit lyrics, and enqueue/playlist opaque local records without exposing or broadening SAF access.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Severity | Disposition | Mitigation |
|---|---|---:|---|---|
| T-06-04-01 | Information disclosure—SAF DTO | critical | mitigate | Opaque IDs and safe serializers; native-only URI/grant/path ownership. |
| T-06-04-02 | Spoofing—Activity result | high | mitigate | Request identity, lifecycle cancellation, exact flags, late-result rejection. |
| T-06-04-03 | Denial of service—metadata/art/LRC | medium | mitigate | Time/byte/dimension/line/string/batch bounds. |
| T-06-04-04 | Elevation of privilege—storage | high | mitigate | ACTION_OPEN_DOCUMENT only; no broad permission, directory enumeration, or generic resolver. |
</threat_model>

<verification>
Run the exact policy, metadata, picker, screen, and fake-provider source-compilation selectors plus full mobile and Gradle gates. The controlled fixture runs once only after 06-07; it is not Phase 8 real-document/APK acceptance.
</verification>

<success_criteria>
LOCAL-001 and the import/metadata/artwork/LRC/library portions of LOCAL-002 have independent native-package, JVM, JS, and instrumentation evidence.
</success_criteria>

<output>Create `.planning/phases/06-personal-library-continuity/06-04-SUMMARY.md`.</output>
