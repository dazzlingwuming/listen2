---
phase: 06-personal-library-continuity
plan: "06"
type: execute
wave: 6
depends_on: [06-05]
files_modified:
  - mobile/android/app/src/main/java/com/listen2mobile/history/ListeningPolicy.kt
  - mobile/android/app/src/main/java/com/listen2mobile/history/ListeningLedger.kt
  - mobile/android/app/src/main/java/com/listen2mobile/history/HistoryModule.kt
  - mobile/android/app/src/main/java/com/listen2mobile/history/HistoryPackage.kt
  - mobile/android/app/src/main/java/com/listen2mobile/library/LibraryDatabase.kt
  - mobile/android/app/src/main/java/com/listen2mobile/library/LibraryPreferences.kt
  - mobile/android/app/src/main/java/com/listen2mobile/MainApplication.kt
  - mobile/android/app/src/test/java/com/listen2mobile/history/ListeningLedgerTest.kt
  - mobile/android/app/src/test/java/com/listen2mobile/history/HistoryBridgeContractTest.kt
  - mobile/android/app/src/androidTest/java/com/listen2mobile/history/ListeningLedgerInstrumentationTest.kt
autonomous: true
requirements: [HIST-001, HIST-002, HIST-003]
estimate: { tokens: 28000, raw_tokens: 28000, tasks: 3, confidence: low }
must_haves:
  truths:
    - "Native ledger counts exactly once only after genuine forward playback exceeds 30 seconds and reaches min(duration/2, 4 minutes)."
    - "Pause/buffer/seek/preload/failure/browse/duplicate callbacks contribute no false evidence."
    - "Database reopen, midnight/year/timezone boundaries, and clear-generation races preserve stable committed results."
    - "History package is independent, asynchronous, bounded, and exposes only safe semantic methods/data."
  artifacts:
    - "ListeningLedgerTest.kt proves the pure threshold/segment/deduplication state machine."
    - "ListeningLedgerInstrumentationTest.kt proves actual Room restart/year/aggregate/clear-fence behavior."
    - "HistoryPackage.kt is registered independently from LibraryPackage and LocalAudioPackage."
  key_links:
    - from: mobile/android/app/src/main/java/com/listen2mobile/history/HistoryModule.kt
      to: mobile/android/app/src/main/java/com/listen2mobile/history/ListeningLedger.kt
      via: "bounded serial executor and versioned semantic observations"
    - from: mobile/android/app/src/main/java/com/listen2mobile/history/ListeningLedger.kt
      to: mobile/android/app/src/main/java/com/listen2mobile/library/LibraryDatabase.kt
      via: "one transaction for evidence/commit/aggregates or clear generation"
---

<objective>
Build and independently prove the native valid-listening ledger, aggregate ownership, restart/calendar behavior, and clear fence.

Purpose: Native history truth must be correct before RNTP and UI adapters are connected.
Output: Pure policy, Room ledger, independent package/bridge, JVM contracts, and restart/year/clear instrumentation.
</objective>

<execution_context>
@/Users/fluenteng/.codex/gsd-core/workflows/execute-plan.md
@/Users/fluenteng/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/06-personal-library-continuity/06-CONTEXT.md
@.planning/phases/06-personal-library-continuity/06-RESEARCH.md
@.planning/phases/06-personal-library-continuity/06-VALIDATION.md
@.planning/phases/06-personal-library-continuity/06-05-SUMMARY.md
@mobile/android/app/src/main/java/com/listen2mobile/MainApplication.kt
</context>

<interfaces>
- `beginPlayback({ playbackInstanceId, clearGeneration, trackIdentity, safeMetadata, durationMs, startedElapsedMs })`
- `observePlayback({ playbackInstanceId, clearGeneration, sequence, kind, positionMs?, observedElapsedMs })`
- `ListeningCommit { eventId, playbackInstanceId, committedLocalDate, committedLocalYear, committedLocalMonth, listenedForwardMs, thresholdMs }`
- Named methods: `getHistory`, `getRecap(year)`, `getRecordingPreference`, `setRecordingPreference`, `exportSafeHistory`, `clearHistory`.

Valid listen requires `listenedForwardMs > 30_000` and `listenedForwardMs >= min(validDurationMs / 2, 240_000)`. Unknown/invalid duration cannot commit. Commit local date/year/month never changes later.
</interfaces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Commit one exact-threshold listen through the pure policy and Room ledger</name>
  <files>mobile/android/app/src/main/java/com/listen2mobile/history/ListeningPolicy.kt, mobile/android/app/src/main/java/com/listen2mobile/history/ListeningLedger.kt, mobile/android/app/src/main/java/com/listen2mobile/library/LibraryDatabase.kt, mobile/android/app/src/test/java/com/listen2mobile/history/ListeningLedgerTest.kt</files>
  <behavior>
    - Genuine monotonic forward evidence satisfying both predicates commits one event and aggregates atomically.
    - At/below 30 seconds, below half-duration/capped threshold, unknown duration, pause, buffer, seek jump, preload, failure, and browse commit nothing.
    - Seek resets baseline; subsequent real forward listening may accumulate without counting the jump.
    - Duplicate/reordered sequence and one playbackInstanceId repeat cannot double count.
  </behavior>
  <action>Extract a pure state machine that accepts allow-listed semantic transitions, uses monotonic elapsed evidence, clamps impossible position/time deltas, excludes pause/buffer/seek/load/failure gaps, and applies the exact two-part predicate. Persist safe evidence checkpoints, committed rows, total/play/distinct/top/month aggregates, and unique `(playbackInstanceId, clearGeneration)` in Room. Update event and all aggregates in one transaction; use semantic track/local-record identity and safe title/artist only, never player/media URLs or raw callbacks. Make clock/timezone sources injectable for deterministic JVM tests.</action>
  <verify><automated>JAVA_HOME=/opt/homebrew/opt/openjdk@17 ANDROID_HOME=/opt/homebrew/share/android-commandlinetools ANDROID_SDK_ROOT=/opt/homebrew/share/android-commandlinetools PATH=/opt/homebrew/opt/openjdk@17/bin:$PATH ./mobile/android/gradlew --offline --no-daemon -p mobile/android :app:testDebugUnitTest --tests 'com.listen2mobile.history.ListeningLedgerTest'</automated></verify>
  <done>The exact valid-listen predicate, exclusion matrix, seek baseline, and exactly-once behavior pass deterministic JVM tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register an independent bounded asynchronous History package</name>
  <files>mobile/android/app/src/main/java/com/listen2mobile/history/HistoryModule.kt, mobile/android/app/src/main/java/com/listen2mobile/history/HistoryPackage.kt, mobile/android/app/src/main/java/com/listen2mobile/library/LibraryPreferences.kt, mobile/android/app/src/main/java/com/listen2mobile/MainApplication.kt, mobile/android/app/src/test/java/com/listen2mobile/history/HistoryBridgeContractTest.kt</files>
  <behavior>
    - HistoryPackage registers separately and exposes only named begin/observe/snapshot/recap/preference/export/clear calls.
    - Observation submission queues bounded work and returns without awaiting Room; overload/failure cannot block player commands.
    - recordingEnabled is the only history preference in DataStore; lists/evidence/aggregates remain Room-owned.
    - DTOs reject URL/local handle/session/cache/raw diagnostics and bound sequences, strings, batches, years, queries, and export size.
  </behavior>
  <action>Create `HistoryModule` on one bounded serial executor and `HistoryPackage` registered independently in `MainApplication`. Validate observation identity/generation/sequence/kind and safe metadata before enqueueing, expose bounded snapshots/recaps/export, and return stable public errors without raw exceptions. Put only recordingEnabled in `LibraryPreferences` DataStore. Ensure disable rejects new session/evidence writes without deleting existing history; clear remains a separate transactional command. Test method allow-list, async return, queue bounds, DTO redaction, invalid year/export, preference behavior, and shutdown/late-call safety.</action>
  <verify><automated>JAVA_HOME=/opt/homebrew/opt/openjdk@17 ANDROID_HOME=/opt/homebrew/share/android-commandlinetools ANDROID_SDK_ROOT=/opt/homebrew/share/android-commandlinetools PATH=/opt/homebrew/opt/openjdk@17/bin:$PATH ./mobile/android/gradlew --offline --no-daemon -p mobile/android :app:testDebugUnitTest --tests 'com.listen2mobile.history.HistoryBridgeContractTest'</automated></verify>
  <done>Independent History package boundaries, bounded asynchrony, DataStore scope, and DTO privacy have an exact JVM contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove restart, local year buckets, aggregates, and clear fence in Room</name>
  <files>mobile/android/app/src/main/java/com/listen2mobile/history/ListeningLedger.kt, mobile/android/app/src/main/java/com/listen2mobile/library/LibraryDatabase.kt, mobile/android/app/src/androidTest/java/com/listen2mobile/history/ListeningLedgerInstrumentationTest.kt</files>
  <behavior>
    - Incomplete evidence survives reopen without committing or duplicating; completed transaction reads back once.
    - Local date/year/month captured at commit stay fixed across midnight, year, timezone change, and later reads.
    - Totals, plays, distinct song/artist, top tracks/artists, monthly trend and empty-year query equal committed rows after reopen.
    - Clear atomically deletes evidence/events/aggregates and increments clearGeneration; every late pre-clear observation is rejected after reopen.
  </behavior>
  <action>Instrument a real Room database with injectable elapsed/wall clocks and timezone. Cover reopen before/after threshold, duplicate callback replay, commit on both sides of midnight/year, timezone changes, aggregate recomputation checks, zero-data year, recording disabled, and failures between evidence/commit/aggregate writes. Implement clear as one transaction that removes incomplete and committed rows plus aggregates, increments generation, and returns an empty revisioned snapshot. Reopen after clear and submit old-generation observations to prove non-resurrection; assert no backup/cache/log/remote table holds history data.</action>
  <verify><automated>JAVA_HOME=/opt/homebrew/opt/openjdk@17 ANDROID_HOME=/opt/homebrew/share/android-commandlinetools ANDROID_SDK_ROOT=/opt/homebrew/share/android-commandlinetools PATH=/opt/homebrew/opt/openjdk@17/bin:$PATH ./mobile/android/gradlew --offline --no-daemon -p mobile/android :app:compileDebugAndroidTestKotlin</automated></verify>
  <done>Actual Room instrumentation proves restart, calendar stability, aggregate correctness, atomic failure recovery, and irreversible clear-generation fencing.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Severity | Disposition | Mitigation |
|---|---|---:|---|---|
| T-06-06-01 | Tampering—observations | high | mitigate | Identity/generation/sequence checks, monotonic clamp, allow-listed transitions. |
| T-06-06-02 | Information disclosure—history DTO/export | high | mitigate | Safe metadata serializer and URL/local/session/cache/raw diagnostic rejection. |
| T-06-06-03 | Denial of service—event stream | medium | mitigate | Bounded serial executor, coalescing-compatible interface, bounded query/export. |
| T-06-06-04 | Repudiation—clear/restart | medium | mitigate | Transactional generation, unique session key, stable commit buckets/revision. |
</threat_model>

<verification>
Run exact ledger/bridge JVM selectors and compile ledger instrumentation, then Gradle unit/instrumentation compile and `git diff --check`. The controlled ledger fixture runs once only after 06-07. Phase 8 owns real process-death/system-player acceptance, not these deterministic ledger rules.
</verification>

<success_criteria>
The native portions of HIST-001/002/003 are independently proven for threshold, exclusions, restart/year stability, aggregates, async boundary, preference, and clear fence.
</success_criteria>

<output>Create `.planning/phases/06-personal-library-continuity/06-06-SUMMARY.md`.</output>
