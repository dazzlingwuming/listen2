---
phase: 06-personal-library-continuity
plan: "03"
type: execute
wave: 3
depends_on: [06-02]
files_modified:
  - mobile/android/app/src/main/java/com/listen2mobile/library/LibraryRepository.kt
  - mobile/android/app/src/androidTest/java/com/listen2mobile/library/BackupTransactionInstrumentationTest.kt
  - mobile/src/screens/MyMusicScreen.tsx
  - mobile/src/screens/PlaylistDetailScreen.tsx
  - mobile/src/screens/SettingsScreen.tsx
  - mobile/src/screens/__tests__/libraryFlow.test.tsx
  - mobile/src/screens/__tests__/backupFlow.test.tsx
  - mobile/src/screens/__tests__/accountFlow.test.tsx
  - mobile/src/backup/backupCodec.ts
  - mobile/src/backup/__tests__/backupCodec.test.ts
  - mobile/src/localAudio/backup.ts
  - mobile/src/localAudio/__tests__/backup.test.ts
  - mobile/src/navigation/RootNavigator.tsx
  - mobile/src/navigation/types.ts
autonomous: true
requirements: [LIB-001, LIB-002, LIB-003, AUTH-001, AUTH-002, AUTH-003, DATA-002, DATA-003]
estimate: { tokens: 34000, raw_tokens: 34000, tasks: 3, confidence: low }
must_haves:
  truths:
    - "Personal, favorite, remote, and local sections remain distinct; remote failure cannot erase durable local/personal data."
    - "All playlist/favorite edits are revisioned Room transactions with confirmed UI receipts and explicit destructive confirmation."
    - "Portable backup is previewed and applied once atomically with the required merge/overwrite identity rules and privacy allow-list."
    - "The fixed seven-provider account matrix is truthful; only supported Bilibili authentication controls are exposed."
  artifacts:
    - "libraryFlow.test.tsx proves complete receipt-backed CRUD/order/favorite behavior."
    - "BackupTransactionInstrumentationTest.kt proves actual Room import commit and rollback."
    - "SettingsScreen.tsx exports the account child screen, and accountFlow.test.tsx proves the fixed matrix and QR/logout lifecycle."
  key_links:
    - from: mobile/src/screens/PlaylistDetailScreen.tsx
      to: mobile/android/app/src/main/java/com/listen2mobile/library/LibraryRepository.kt
      via: "libraryClient mutation plus expectedRevision receipt"
    - from: mobile/src/screens/SettingsScreen.tsx
      to: mobile/android/app/src/androidTest/java/com/listen2mobile/library/BackupTransactionInstrumentationTest.kt
      via: "checksum/base-revision preview token and one transaction"
    - from: mobile/src/screens/SettingsScreen.tsx
      to: mobile/src/bilibili/client.ts
      via: "existing status/qrBegin/qrPoll/qrCancel/logout DTOs"
---

<objective>
Deliver complete personal-library editing, atomic portable backup, and the truthful provider account matrix on the durable foundation.

Purpose: Close all non-local-media library/account/data workflows before native SAF work begins.
Output: Receipt-backed library screens, Room-proven backup preview/apply, and seven-provider account UI.
</objective>

<execution_context>
@/Users/fluenteng/.codex/gsd-core/workflows/execute-plan.md
@/Users/fluenteng/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/06-personal-library-continuity/06-CONTEXT.md
@.planning/phases/06-personal-library-continuity/06-UI-SPEC.md
@.planning/phases/06-personal-library-continuity/06-VALIDATION.md
@.planning/phases/06-personal-library-continuity/06-02-SUMMARY.md
@mobile/src/screens/MyMusicScreen.tsx
@mobile/src/screens/PlaylistDetailScreen.tsx
@mobile/src/backup/backupCodec.ts
@mobile/src/screens/SettingsScreen.tsx
@mobile/src/bilibili/client.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Commit one playlist edit and expand the confirmed library matrix</name>
  <files>mobile/android/app/src/main/java/com/listen2mobile/library/LibraryRepository.kt, mobile/android/app/src/androidTest/java/com/listen2mobile/library/BackupTransactionInstrumentationTest.kt, mobile/src/screens/MyMusicScreen.tsx, mobile/src/screens/PlaylistDetailScreen.tsx, mobile/src/screens/__tests__/libraryFlow.test.tsx</files>
  <behavior>
    - Create, rename, delete, add/remove track, explicit move up/down, and favorite operations update only after the matching applied receipt.
    - Two rapid edits at one base revision cannot both commit; stale reload preserves the last confirmed snapshot.
    - Playlist membership deduplicates `(source, semanticTrackId)` and same-name playlists remain independent IDs.
    - Remote source/sync error changes only that row and never deletes personal/favorite/local data.
    - Destructive rejection/cancel retains the prior projection and focus returns to a deterministic target.
  </behavior>
  <action>Implement semantic library operations in `LibraryRepository` as one revisioned transaction each, including deterministic order updates, duplicate handling, capability projection, source/sync status, idempotent requests, and stable rejection codes. Render the UI-SPEC hierarchy through personal/favorite/remote/local sections with 48dp labeled controls, explicit up/down reorder, pending state on the initiating control, and confirmation for destructive playlist deletion. Screens call only `libraryClient` from 06-02 and retain the confirmed projection on error. Add actual Room CRUD/reopen assertions to the instrumentation class before backup cases are added.</action>
  <verify><automated>npm run mobile:test -- --runInBand mobile/src/screens/__tests__/libraryFlow.test.tsx &amp;&amp; JAVA_HOME=/opt/homebrew/opt/openjdk@17 ANDROID_HOME=/opt/homebrew/share/android-commandlinetools ANDROID_SDK_ROOT=/opt/homebrew/share/android-commandlinetools PATH=/opt/homebrew/opt/openjdk@17/bin:$PATH ./mobile/android/gradlew --offline --no-daemon -p mobile/android :app:compileDebugAndroidTestKotlin</automated></verify>
  <done>Complete library CRUD/order/favorite behavior is visible, transactionally proven in Room, and robust to stale/rapid/destructive failures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove strict portable backup preview and one-transaction apply</name>
  <files>mobile/android/app/src/main/java/com/listen2mobile/library/LibraryRepository.kt, mobile/android/app/src/androidTest/java/com/listen2mobile/library/BackupTransactionInstrumentationTest.kt, mobile/src/backup/backupCodec.ts, mobile/src/backup/__tests__/backupCodec.test.ts, mobile/src/localAudio/backup.ts, mobile/src/localAudio/__tests__/backup.test.ts, mobile/src/screens/SettingsScreen.tsx, mobile/src/screens/__tests__/backupFlow.test.tsx</files>
  <behavior>
    - Export contains only personal playlists, favorites, and required safe remote metadata; recursive private/runtime fields are absent.
    - Malformed, oversized, too-old, unsupported-newer, prototype-bearing, or checksum-changed documents perform zero writes.
    - Preview defaults to merge, skips identical semantic tracks, keeps same-name playlists independent, and remints colliding IDs consistently.
    - Overwrite requires the second destructive confirmation and never touches local records, history, lyrics, settings, cache, queue, or account data.
    - Injected failure/interruption/stale revision rolls back the entire Room transaction.
  </behavior>
  <action>Define an explicit integer backup format and strict bounded parser/serializer. Remove queue and every local/session/history/lyric/setting/cache/media field from export. Have native code independently validate safe decoded input, produce a checksum/baseRevision-bound preview token, and apply merge or overwrite in one Room transaction only when the token remains current. Implement identical skip, same-name independence, collision remint, membership rewrites, overwrite preservation boundaries, and rollback. Replace the current queue-then-library Settings apply with a scrollable preview sheet, merge default, warnings/counts, and a second overwrite confirmation. Extend the exact instrumentation class with merge, overwrite, malformed, stale, failure-injection, and reopen assertions against real Room state.</action>
  <reversibility rating="costly">Backup format and overwrite semantics are user-visible; explicit versioning, preview binding, atomic rollback, and double confirmation contain the cost.</reversibility>
  <verify><automated>npm run mobile:test -- --runInBand mobile/src/backup/__tests__/backupCodec.test.ts mobile/src/localAudio/__tests__/backup.test.ts mobile/src/screens/__tests__/backupFlow.test.tsx &amp;&amp; JAVA_HOME=/opt/homebrew/opt/openjdk@17 ANDROID_HOME=/opt/homebrew/share/android-commandlinetools ANDROID_SDK_ROOT=/opt/homebrew/share/android-commandlinetools PATH=/opt/homebrew/opt/openjdk@17/bin:$PATH ./mobile/android/gradlew --offline --no-daemon -p mobile/android :app:compileDebugAndroidTestKotlin</automated></verify>
  <done>Portable backup privacy and identity rules pass pure tests, UI flow tests, and actual Room commit/rollback/reopen instrumentation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Render the fixed account matrix and complete Bilibili QR/logout UI</name>
  <files>mobile/src/screens/SettingsScreen.tsx, mobile/src/screens/__tests__/accountFlow.test.tsx, mobile/src/navigation/RootNavigator.tsx, mobile/src/navigation/types.ts</files>
  <behavior>
    - Rows appear in locked order: QQ音乐, 酷狗音乐, 酷我音乐, 咪咕音乐, Taihe, 哔哩哔哩, 网易云音乐.
    - Only Bilibili exposes supported login/logout; other rows show truthful unavailable/unverified state without fake authentication.
    - Waiting/scanned/authenticated/expired/cancelled/error/unavailable QR states have distinct status and recovery actions.
    - Leaving/restarting cancels the previous attempt; late polls cannot update a new attempt.
    - Confirmed logout clears public session/protected references while no library/history/local deletion is dispatched, and relogin cannot reuse old attempt state.
  </behavior>
  <action>Export the account child-screen component from `SettingsScreen.tsx`, add typed `AccountProviderId` separate from playback `SourceId`, register the `账号与来源` route, and render the fixed matrix/capability copy. Reuse only existing Bilibili public client methods and attempt identity; cancel on unmount/new attempt, bound polling, refresh status after success/logout, and show safe error recovery. Require logout confirmation and assert non-account state preservation. Do not expose login-shaped controls for unsupported providers or cookies/tokens/QR credentials/raw errors anywhere in state, logs, route params, or accessibility values.</action>
  <verify><automated>npm run mobile:test -- --runInBand mobile/src/screens/__tests__/accountFlow.test.tsx mobile/src/bilibili/__tests__/client.test.ts &amp;&amp; JAVA_HOME=/opt/homebrew/opt/openjdk@17 ANDROID_HOME=/opt/homebrew/share/android-commandlinetools ANDROID_SDK_ROOT=/opt/homebrew/share/android-commandlinetools PATH=/opt/homebrew/opt/openjdk@17/bin:$PATH ./mobile/android/gradlew --offline --no-daemon -p mobile/android :app:testDebugUnitTest --tests 'com.listen2mobile.bilibili.BilibiliContractTest'</automated></verify>
  <done>The fixed matrix is truthful, Bilibili lifecycle is cancellation-safe, and logout cannot expose or erase unrelated durable user data.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Severity | Disposition | Mitigation |
|---|---|---:|---|---|
| T-06-03-01 | Tampering—backup import | high | mitigate | Strict decode, native revalidation, preview token, base revision, one transaction. |
| T-06-03-02 | Information disclosure—export/account | high | mitigate | Explicit serializers and no secrets/local/history/runtime fields. |
| T-06-03-03 | Spoofing—account state | medium | mitigate | Fixed typed matrix; authentication only from native Bilibili status. |
| T-06-03-04 | Denial of service—document preview | medium | mitigate | Byte/depth/count/string caps and zero-write parse failure. |
</threat_model>

<verification>
Run focused selectors, then full mobile Jest/typecheck/lint, Java 17 Gradle unit/instrumentation compile, and `git diff --check`. Live provider/account entitlement remains Phase 8.
</verification>

<success_criteria>
LIB-001/002/003, AUTH-001/002/003, and DATA-002/003 are covered by user-visible flows plus exact Room backup transaction and existing Bilibili native contract evidence.
</success_criteria>

<output>Create `.planning/phases/06-personal-library-continuity/06-03-SUMMARY.md`.</output>
