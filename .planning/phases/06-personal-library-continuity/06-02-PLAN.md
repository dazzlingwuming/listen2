---
phase: 06-personal-library-continuity
plan: "02"
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - mobile/src/library/types.ts
  - mobile/src/library/libraryClient.ts
  - mobile/src/library/libraryProjection.ts
  - mobile/src/library/legacyMigration.ts
  - mobile/src/library/LibraryBootGate.tsx
  - mobile/src/library/__tests__/libraryRepositoryClient.test.ts
  - mobile/src/library/__tests__/libraryMigration.test.ts
  - mobile/src/store/index.ts
  - mobile/src/store/librarySlice.ts
  - mobile/src/store/playerPersistence.ts
  - mobile/src/store/__tests__/librarySlice.test.ts
  - mobile/src/store/__tests__/playerPersistence.test.ts
  - mobile/src/navigation/RootNavigator.tsx
autonomous: true
requirements: [DATA-001, LIB-003]
estimate: { tokens: 26000, raw_tokens: 26000, tasks: 3, confidence: low }
must_haves:
  truths:
    - "Redux is a disposable render projection and a fresh JavaScript runtime hydrates the same confirmed Room snapshot."
    - "Pending, stale, rejected, or late mutation receipts never replace the last confirmed projection."
    - "The real legacy Redux Persist payload migrates paused and URL-free; native failure leaves the legacy backend readable and retryable."
  artifacts:
    - "libraryClient.ts validates the complete native snapshot/receipt contract."
    - "LibraryBootGate.tsx prevents library-dependent commands before native backend selection and hydration finish."
    - "libraryMigration.test.ts proves JavaScript export, interruption, retry, and later-start cleanup eligibility."
  key_links:
    - from: mobile/src/library/libraryClient.ts
      to: mobile/android/app/src/main/java/com/listen2mobile/library/LibraryBridge.kt
      via: "versioned high-level NativeModules calls"
    - from: mobile/src/library/libraryProjection.ts
      to: mobile/src/store/librarySlice.ts
      via: "confirmed snapshot replacement only"
    - from: mobile/src/library/legacyMigration.ts
      to: mobile/src/store/playerPersistence.ts
      via: "bounded safe legacy export with paused URL-free player semantics"
---

<objective>
Cut the canonical React Native state layer over to the native repository with deterministic hydration and rollback-safe legacy orchestration.

Purpose: Remove competing AsyncStorage/Redux library ownership before feature screens issue durable commands.
Output: Strict client DTOs, projection-only Redux state, boot gate, and tested JavaScript migration handshake.
</objective>

<execution_context>
@/Users/fluenteng/.codex/gsd-core/workflows/execute-plan.md
@/Users/fluenteng/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-personal-library-continuity/06-CONTEXT.md
@.planning/phases/06-personal-library-continuity/06-VALIDATION.md
@.planning/phases/06-personal-library-continuity/06-01-SUMMARY.md
@mobile/src/store/index.ts
@mobile/src/store/librarySlice.ts
@mobile/src/store/playerPersistence.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Hydrate one safe native snapshot into a confirmed projection</name>
  <files>mobile/src/library/types.ts, mobile/src/library/libraryClient.ts, mobile/src/library/libraryProjection.ts, mobile/src/library/__tests__/libraryRepositoryClient.test.ts</files>
  <behavior>
    - A valid native snapshot becomes the exact deterministic screen projection at its revision.
    - Unknown schema/status, unsafe nested field, malformed identity/order, or oversized response fails closed with a stable client error.
    - Matching applied receipt replaces projection; stale/rejected/mismatched requestId keeps the last confirmed projection and schedules one reload.
    - Concurrent stale results coalesce to one snapshot request and an older reload cannot overwrite a newer revision.
  </behavior>
  <action>Define discriminated safe DTOs and recursive runtime guards for snapshots, mutations, receipts, migration state, capability flags, backup preview/apply, and later local/history consumers. Implement an allow-listed `libraryClient` over the 06-01 bridge with stable timeouts/errors and no raw NativeModules pass-through. Implement pure deterministic snapshot projection, receipt correlation, stale reload coalescing, and monotonic revision acceptance. Reject paths, URIs, grants, media URLs, credentials, SQL, native handles, and raw exceptions at the client boundary.</action>
  <verify><automated>npm run mobile:test -- --runInBand mobile/src/library/__tests__/libraryRepositoryClient.test.ts</automated></verify>
  <done>One real contract-shaped native snapshot and receipt can safely drive the TypeScript projection; all malformed/stale/late fixtures retain confirmed state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove durable Redux library ownership and gate startup hydration</name>
  <files>mobile/src/library/LibraryBootGate.tsx, mobile/src/store/index.ts, mobile/src/store/librarySlice.ts, mobile/src/store/__tests__/librarySlice.test.ts, mobile/src/navigation/RootNavigator.tsx</files>
  <behavior>
    - Store recreation starts unhydrated, then reaches ready only from a validated native snapshot.
    - Library arrays are absent from the authoritative Redux Persist allow-list and cannot overwrite Room on startup.
    - Mutation lifecycle actions preserve confirmed data during pending/error and accept only the matching receipt/revision.
    - Boot failure exposes retry without navigating into writable library controls or showing an invented empty library.
  </behavior>
  <action>Refactor `librarySlice` to hold snapshot projection, revision, hydration status, pending request identities, and safe errors rather than durable mutation ownership. Remove library collections from Redux Persist ownership while preserving unrelated semantic player persistence. Install `LibraryBootGate` around library-dependent navigation; load backend status and snapshot once, retry explicitly, and keep an error distinct from an empty library. Keep selectors compatible with screen needs, but require all durable writes to flow through `libraryClient` and matching receipts.</action>
  <verify><automated>npm run mobile:test -- --runInBand mobile/src/store/__tests__/librarySlice.test.ts mobile/src/library/__tests__/libraryRepositoryClient.test.ts &amp;&amp; npm run mobile:typecheck</automated></verify>
  <done>Restart/rotation-style store recreation hydrates from Room, and no reducer or persisted legacy array can become a second durable authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Connect the actual legacy payload to the reversible native migration</name>
  <files>mobile/src/library/legacyMigration.ts, mobile/src/library/__tests__/libraryMigration.test.ts, mobile/src/store/playerPersistence.ts, mobile/src/store/__tests__/playerPersistence.test.ts</files>
  <behavior>
    - One bounded legacy payload exports personal/favorite/remote safe metadata, queue checkpoint, lyric metadata, and paused URL-free player semantics.
    - Legacy local entries export safe display metadata only and become needs-repair placeholders.
    - Native copied/validated/active/failure statuses drive retry without deleting or blanking the legacy source.
    - Only a later startup readback at the activated checksum marks cleanup eligible; repeated startup cannot duplicate import.
  </behavior>
  <action>Read the one known Redux Persist key and build an explicit bounded migration DTO; never enumerate arbitrary AsyncStorage or forward the serialized tree. Reuse player persistence sanitizers to strip resolved/signed/private playback URIs and force paused/no-autoplay restoration. Correlate attemptId and checksum across bridge calls, resume the native journal after interruption, hydrate Room only after active status, and retain legacy content until the later-start readback acknowledgement. Convert unsafe legacy local references to safe needs-repair data. Test native rejection, bridge timeout, corrupted legacy JSON, repeated calls, process-style rerun, and cleanup eligibility without deleting the source in this plan.</action>
  <verify><automated>npm run mobile:test -- --runInBand mobile/src/library/__tests__/libraryMigration.test.ts mobile/src/store/__tests__/playerPersistence.test.ts &amp;&amp; npm run mobile:typecheck</automated></verify>
  <done>The production legacy payload follows the native copy-validate-activate protocol, remains rollback-readable on every failure, and cannot restore autoplay or unsafe local/media references.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Severity | Disposition | Mitigation |
|---|---|---:|---|---|
| T-06-02-01 | Tampering—native DTO | high | mitigate | Recursive guards, schema/revision/request correlation, monotonic projection acceptance. |
| T-06-02-02 | Information disclosure—legacy export | high | mitigate | Explicit safe-field DTO and URL/path/grant/secret rejection fixtures. |
| T-06-02-03 | Denial of service—hydration/reload | medium | mitigate | Bounds, one boot request, coalesced reload, explicit retry. |
</threat_model>

<verification>
Run all focused selectors, then `npm run mobile:test`, `npm run mobile:typecheck`, `npm --prefix mobile run lint -- --quiet`, and `git diff --check`.
</verification>

<success_criteria>
DATA-001 and LIB-003 now have a production JavaScript cutover: Room remains sole durable authority, Redux is rebuildable, and failed migration/hydration retains recoverable confirmed data.
</success_criteria>

<output>Create `.planning/phases/06-personal-library-continuity/06-02-SUMMARY.md`.</output>
