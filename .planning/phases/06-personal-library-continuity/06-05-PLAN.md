---
phase: 06-personal-library-continuity
plan: "05"
type: execute
wave: 5
depends_on: [06-04]
files_modified:
  - mobile/android/app/src/main/java/com/listen2mobile/local/LocalMediaPolicy.kt
  - mobile/android/app/src/main/java/com/listen2mobile/local/LocalMediaProvider.kt
  - mobile/android/app/src/main/java/com/listen2mobile/local/LocalAudioModule.kt
  - mobile/android/app/src/main/java/com/listen2mobile/library/LibraryRepository.kt
  - mobile/android/app/src/main/AndroidManifest.xml
  - mobile/android/app/src/test/java/com/listen2mobile/local/LocalMediaPolicyTest.kt
  - mobile/android/app/src/androidTest/java/com/listen2mobile/local/LocalMediaProviderInstrumentationTest.kt
  - mobile/src/types/music.ts
  - mobile/src/player/playerController.ts
  - mobile/src/player/__tests__/playerController.test.ts
  - mobile/src/screens/MyMusicScreen.tsx
  - mobile/src/localAudio/__tests__/localLibraryFlow.test.tsx
autonomous: true
requirements: [LOCAL-002, LOCAL-003]
estimate: { tokens: 28000, raw_tokens: 28000, tasks: 3, confidence: low }
must_haves:
  truths:
    - "Opaque local records play through the sole RNTP controller using a short-lived app-private read-only provider token."
    - "Provider query/write/delete/traversal/unknown/expired-token requests are rejected and cannot reveal the underlying SAF URI."
    - "Revoked, unreadable, unsupported, duplicate, and non-seekable records expose truthful repair/remove/capability behavior."
    - "Repair preserves record identity and relationships; removal never deletes the source document and failure retains confirmed state."
  artifacts:
    - "LocalMediaProvider.kt is non-exported and resolves exact short-lived tokens to native-held documents read-only."
    - "LocalMediaProviderInstrumentationTest.kt proves actual open/read and every write/token/path rejection."
    - "playerController.test.ts proves transient handoff without persistence or a second player."
  key_links:
    - from: mobile/src/player/playerController.ts
      to: mobile/android/app/src/main/java/com/listen2mobile/local/LocalAudioModule.kt
      via: "prepareLocalPlayback(recordId, playbackRequestId) at accepted-load boundary"
    - from: mobile/android/app/src/main/java/com/listen2mobile/local/LocalMediaProvider.kt
      to: Android ContentResolver
      via: "validated token → opaque record → retained grant → read-only descriptor"
---

<objective>
Complete private local playback, capability/error handling, repair, and removal without leaking native document ownership.

Purpose: Close LOCAL-002/003 after import and metadata are proven, while retaining RNTP as the only player.
Output: Non-exported provider and policy tests, transient RNTP handoff, and repair/removal lifecycle.
</objective>

<execution_context>
@/Users/fluenteng/.codex/gsd-core/workflows/execute-plan.md
@/Users/fluenteng/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/06-personal-library-continuity/06-CONTEXT.md
@.planning/phases/06-personal-library-continuity/06-VALIDATION.md
@.planning/phases/06-personal-library-continuity/06-04-SUMMARY.md
@mobile/src/player/playerController.ts
@mobile/src/types/music.ts
@mobile/android/app/src/main/AndroidManifest.xml
@mobile/android/app/src/main/java/com/listen2mobile/offline/OfflineAudioProvider.kt
</context>

<interfaces>
- `prepareLocalPlayback(recordId, playbackRequestId) -> { playbackRequestId, privatePlaybackUri, seekable }`
- `RepairReceipt { requestId, recordId, status: repaired | mismatch | cancelled | rejected, revision, snapshot? }`
- `availability = available | needs-repair | revoked | unreadable | unsupported`

`privatePlaybackUri` exists only for the active matching load and must never enter Redux, Room, backup, logs, diagnostics, routes, accessibility values, or player persistence.
</interfaces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Open one opaque record through a tokenized read-only provider</name>
  <files>mobile/android/app/src/main/java/com/listen2mobile/local/LocalMediaPolicy.kt, mobile/android/app/src/main/java/com/listen2mobile/local/LocalMediaProvider.kt, mobile/android/app/src/main/AndroidManifest.xml, mobile/android/app/src/test/java/com/listen2mobile/local/LocalMediaPolicyTest.kt, mobile/android/app/src/androidTest/java/com/listen2mobile/local/LocalMediaProviderInstrumentationTest.kt</files>
  <behavior>
    - A valid current playback token opens the retained document read-only and expires according to bounded use/lifetime policy.
    - Unknown/expired/reused/wrong-record tokens, malformed/traversal paths, external callers, query/insert/update/delete, and write modes are rejected.
    - Revoked/unreadable descriptors map to stable public status without returning the third-party URI or raw exception.
    - Manifest declares exact authority, exported=false, no external grant surface, and no broad storage permission.
  </behavior>
  <action>Extract pure authority/path/token/mode validation into `LocalMediaPolicy`. Implement a non-exported application-authority `LocalMediaProvider` that accepts only the exact playback-token path, resolves opaque record ownership inside native code, and opens `ParcelFileDescriptor` read-only. Tokens bind recordId plus playbackRequestId, have bounded lifetime/use, and reveal no source identifier. Reject every generic ContentProvider operation, traversal, external/broad grant, and write mode. Add JVM policy cases and instrumentation that registers controlled documents, reads valid bytes, then proves expired/reused/wrong token, revoke, query, mutation, and write rejection.</action>
  <verify><automated>JAVA_HOME=/opt/homebrew/opt/openjdk@17 ANDROID_HOME=/opt/homebrew/share/android-commandlinetools ANDROID_SDK_ROOT=/opt/homebrew/share/android-commandlinetools PATH=/opt/homebrew/opt/openjdk@17/bin:$PATH ./mobile/android/gradlew --offline --no-daemon -p mobile/android :app:testDebugUnitTest --tests 'com.listen2mobile.local.LocalMediaPolicyTest' :app:compileDebugAndroidTestKotlin</automated></verify>
  <done>Actual provider reads pass only for a valid token; token, path, caller, query, mutation, and write rejection have exact JVM/instrumentation evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Hand the private URI to RNTP at the last responsible moment</name>
  <files>mobile/src/types/music.ts, mobile/src/player/playerController.ts, mobile/src/player/__tests__/playerController.test.ts</files>
  <behavior>
    - LocalTrack carries only opaque safe record identity/metadata/capabilities, never contentUri/fileName/bookmark/path/grant.
    - Only the accepted matching local load requests a private URI and passes it immediately to the existing RNTP add/load operation.
    - Stale preparation response is ignored; no private URI is persisted, logged, emitted, backed up, routed, or retained in Redux.
    - Non-seekable local audio can play sequentially while seek controls/calls are disabled; load failure keeps repair available.
  </behavior>
  <action>Replace the raw local track model with the safe record contract. At the accepted-load boundary in `playerController`, call the named native prepare method, verify playbackRequestId/current semantic record identity again, and construct the RNTP item without storing the returned app-private URI anywhere else. Persist only semantic record identity and safe metadata. Keep RNTP as sole player, preserve queue occurrence semantics, map native unavailable/revoked/unreadable statuses safely, and gate seeking from native capability. Do not copy media, add ExoPlayer, or implement Phase 7 cache/download behavior.</action>
  <verify><automated>npm run mobile:test -- --runInBand mobile/src/player/__tests__/playerController.test.ts mobile/src/localAudio/__tests__/picker.test.ts &amp;&amp; npm run mobile:typecheck</automated></verify>
  <done>Opaque local records reach RNTP through a transient matching provider URI with no persistence/leak and correct non-seekable behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Make revoke/repair/removal transactional and relationship-safe</name>
  <files>mobile/android/app/src/main/java/com/listen2mobile/local/LocalAudioModule.kt, mobile/android/app/src/main/java/com/listen2mobile/library/LibraryRepository.kt, mobile/android/app/src/androidTest/java/com/listen2mobile/local/LocalMediaProviderInstrumentationTest.kt, mobile/src/screens/MyMusicScreen.tsx, mobile/src/localAudio/__tests__/localLibraryFlow.test.tsx</files>
  <behavior>
    - Access failure marks the existing record revoked/unreadable without deleting it or its playlist/favorite/queue references.
    - Repair validates/acquires a compatible replacement before atomically swapping ownership and preserves recordId/relationships.
    - Failed/mismatched/cancelled repair retains old confirmed state and grant; successful repair releases the old grant after commit.
    - Confirmed removal cleans semantic references and releases the grant after commit but never deletes the provider's source file.
  </behavior>
  <action>Add named repair and remove methods to `LocalAudioModule`. Repair launches a fresh ACTION_OPEN_DOCUMENT request, validates supported compatible audio and bounded metadata, acquires the new grant, atomically swaps native ownership while retaining record identity and relationships, then releases the prior grant only after commit. In `MyMusicScreen`, render truthful repair/remove affordances for revoked, unreadable, unsupported, duplicate, and non-seekable records; require the locked `从本地音乐移除？只会移除 Listen2 记录，不会删除设备上的原文件。` confirmation before removal, preserve the confirmed row on cancellation/failure, and restore focus to that row after rejection. Confirmed removal cleans cross references, then releases access without invoking source delete. Extend provider instrumentation and UI tests for moved/revoked/cloud/unreadable/duplicate/mismatch/cancel/failure/reopen and source-file non-deletion.</action>
  <verify><automated>npm run mobile:test -- --runInBand mobile/src/localAudio/__tests__/localLibraryFlow.test.tsx &amp;&amp; JAVA_HOME=/opt/homebrew/opt/openjdk@17 ANDROID_HOME=/opt/homebrew/share/android-commandlinetools ANDROID_SDK_ROOT=/opt/homebrew/share/android-commandlinetools PATH=/opt/homebrew/opt/openjdk@17/bin:$PATH ./mobile/android/gradlew --offline --no-daemon -p mobile/android :app:compileDebugAndroidTestKotlin</automated></verify>
  <done>Every access/repair/remove state preserves transactional identity and privacy, with actual revoke/write/source-non-deletion provider evidence.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Severity | Disposition | Mitigation |
|---|---|---:|---|---|
| T-06-05-01 | Elevation of privilege—provider | critical | mitigate | exported=false, exact token/path, read-only descriptor, no generic operations. |
| T-06-05-02 | Information disclosure—playback URI | high | mitigate | One-shot URI at matching RNTP handoff; persistence/log/DTO tests. |
| T-06-05-03 | Tampering—repair swap | high | mitigate | Validate/acquire first, atomic ownership swap, release old grant after commit. |
| T-06-05-04 | Denial of service—token table | medium | mitigate | Bounded lifetime/count/use and cleanup on playback lifecycle. |
</threat_model>

<verification>
Run exact JVM/provider-source-compilation/player selectors, then full mobile and Gradle gates. The controlled provider fixture runs once only after 06-07. No downloadable media/cache or Phase 8 real-device acceptance is added.
</verification>

<success_criteria>
LOCAL-002/003 close with exact read-only/token/write-rejection evidence, sole-RNTP playback, native-only identifiers, and tested repair/removal lifecycle.
</success_criteria>

<output>Create `.planning/phases/06-personal-library-continuity/06-05-SUMMARY.md`.</output>
