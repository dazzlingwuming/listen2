---
phase: 06-personal-library-continuity
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - mobile/android/app/build.gradle
  - mobile/android/app/schemas/com.listen2mobile.library.Listen2Database/1.json
  - mobile/android/app/src/main/java/com/listen2mobile/library/LibraryDatabase.kt
  - mobile/android/app/src/main/java/com/listen2mobile/library/LibraryRepository.kt
  - mobile/android/app/src/main/java/com/listen2mobile/library/LibraryBridge.kt
  - mobile/android/app/src/main/java/com/listen2mobile/library/LibraryPackage.kt
  - mobile/android/app/src/main/java/com/listen2mobile/library/LibraryPreferences.kt
  - mobile/android/app/src/main/java/com/listen2mobile/library/LegacyLibraryMigration.kt
  - mobile/android/app/src/main/java/com/listen2mobile/MainApplication.kt
  - mobile/android/app/src/test/java/com/listen2mobile/library/LibraryRepositoryTest.kt
  - mobile/android/app/src/test/java/com/listen2mobile/library/LibraryBridgeContractTest.kt
  - mobile/android/app/src/androidTest/java/com/listen2mobile/library/LibraryRepositoryInstrumentationTest.kt
  - mobile/android/app/src/androidTest/java/com/listen2mobile/library/LibraryMigrationTest.kt
autonomous: true
requirements: [DATA-001, LIB-003]
estimate: { tokens: 30000, raw_tokens: 30000, tasks: 3, confidence: low }
must_haves:
  truths:
    - "Room is the sole relational owner and DataStore contains only bounded non-sensitive flags."
    - "A revision-checked mutation commits once, survives database reopen, and returns a safe idempotent receipt."
    - "Legacy data activates only after copy and checksum/readback validation; interruption leaves the legacy source usable."
  artifacts:
    - "LibraryDatabase.kt defines the exported Room schema, entities, ordered joins, DAOs, and explicit migration boundary."
    - "LibraryRepository.kt owns transactions, revisions, request idempotency, and deterministic snapshots."
    - "LibraryMigrationTest.kt proves interrupted and successful cutover against a real test database."
  key_links:
    - from: mobile/android/app/src/main/java/com/listen2mobile/library/LibraryBridge.kt
      to: mobile/android/app/src/main/java/com/listen2mobile/library/LibraryRepository.kt
      via: "allow-listed methods and bounded DTO validation"
    - from: mobile/android/app/src/main/java/com/listen2mobile/library/LegacyLibraryMigration.kt
      to: mobile/android/app/src/main/java/com/listen2mobile/library/LibraryDatabase.kt
      via: "staging transaction, migration journal, checksum, and atomic activation"
---

<objective>
Create and prove the native durable-data foundation and reversible migration boundary.

Purpose: All later Phase 6 features need one transactional native authority before Redux, SAF, backup, or history consumers are connected.
Output: Room schema/repository, small DataStore flags, safe bridge/package registration, and real-device migration/reopen evidence.
</objective>

<execution_context>
@/Users/fluenteng/.codex/gsd-core/workflows/execute-plan.md
@/Users/fluenteng/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-personal-library-continuity/06-CONTEXT.md
@.planning/phases/06-personal-library-continuity/06-RESEARCH.md
@.planning/phases/06-personal-library-continuity/06-PATTERNS.md
@.planning/phases/06-personal-library-continuity/06-VALIDATION.md
@mobile/android/app/build.gradle
@mobile/android/app/src/main/java/com/listen2mobile/MainApplication.kt
</context>

<interfaces>
- `LibrarySnapshot { schemaVersion, revision, personalPlaylists, favorites, remoteCollections, localRecords, queueCheckpoint, lyricMetadata, migrationState }`
- `LibraryMutation { requestId, expectedRevision, operation, payload }`
- `LibraryReceipt { requestId, status: applied | stale | rejected, revision, snapshot?, errorCode? }`
- `MigrationStatus { backend, phase, attemptId, checksum?, sourceRetained }`

Only semantic IDs and safe display metadata cross the React Native boundary. SQL, paths, third-party URIs, grants, media URLs, cookies, secrets, and raw errors never do.
</interfaces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Commit and reopen one revisioned Room mutation</name>
  <files>mobile/android/app/build.gradle, mobile/android/app/src/main/java/com/listen2mobile/library/LibraryDatabase.kt, mobile/android/app/src/main/java/com/listen2mobile/library/LibraryRepository.kt, mobile/android/app/src/test/java/com/listen2mobile/library/LibraryRepositoryTest.kt, mobile/android/app/src/androidTest/java/com/listen2mobile/library/LibraryRepositoryInstrumentationTest.kt</files>
  <behavior>
    - Current-revision create commits exactly once and returns the next revision.
    - Repeated requestId is idempotent; stale expectedRevision and malformed/oversized payloads do not write.
    - Reopening the database returns identical semantic identity and order.
    - Playlist membership deduplicates by `(source, semanticTrackId)` while queue occurrences remain independent.
  </behavior>
  <action>Add Room 2.8.5 runtime/ktx/compiler with Kotlin annotation processing, DataStore Preferences 1.2.1, deterministic schema export, AndroidJUnitRunner 1.6.2, and existing JUnit 4.13.2 integration. Define normalized entities and DAOs for personal playlists/membership order, favorites, safe remote metadata, queue checkpoint, lyric metadata, opaque local records, history evidence/events/aggregates, mutation receipts, migration journal, and the schema-only cache catalog required by context. Cache download, byte, quota, eviction, and offline behavior remain Phase 7. Implement one Room transaction per mutation, monotonic revision, requestId uniqueness, expectedRevision checks, bounded values, deterministic snapshot ordering, and internal native-only transaction entry points. Write pure JVM tests for validation/identity/revision rules and instrumentation tests for actual CRUD, rollback, idempotency, and reopen.</action>
  <verify>
    <automated>JAVA_HOME=/opt/homebrew/opt/openjdk@17 ANDROID_HOME=/opt/homebrew/share/android-commandlinetools ANDROID_SDK_ROOT=/opt/homebrew/share/android-commandlinetools PATH=/opt/homebrew/opt/openjdk@17/bin:$PATH ./mobile/android/gradlew --offline --no-daemon -p mobile/android :app:testDebugUnitTest --tests 'com.listen2mobile.library.LibraryRepositoryTest' :app:compileDebugAndroidTestKotlin</automated>
  </verify>
  <done>Native repository CRUD, rejection, rollback, idempotency, and reopen pass against an actual Room database.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement copy-validate-activate migration with retained rollback source</name>
  <files>mobile/android/app/schemas/com.listen2mobile.library.Listen2Database/1.json, mobile/android/app/src/main/java/com/listen2mobile/library/LibraryPreferences.kt, mobile/android/app/src/main/java/com/listen2mobile/library/LegacyLibraryMigration.kt, mobile/android/app/src/androidTest/java/com/listen2mobile/library/LibraryMigrationTest.kt</files>
  <behavior>
    - Bounded legacy fixtures copy into staging, validate counts/checksum, and activate atomically.
    - Failure or process-style reopen before activation keeps legacy selected and allows retry without duplicates.
    - Local legacy entries become safe needs-repair placeholders without URI/path/bookmark/grant fields.
    - Legacy source stays retained until a separate later startup validates active Room readback and marks cleanup eligibility.
  </behavior>
  <action>Implement DataStore flags for backend, attemptId, migration phase, checksum, later-start validation, and cleanup eligibility only. Implement a Room staging/migration journal that normalizes corrupt partial arrays, remints colliding personal IDs, preserves safe queue semantics, converts local entries to opaque needs-repair records, refuses unsupported downgrade/schema states, and never deletes the legacy source during migration. Export the initial schema and make all future changes require explicit migration. Instrument first run, injected copy/write/validation failures, restart at each journal state, retry, activation, next-start readback, and non-duplication.</action>
  <reversibility rating="costly">The on-disk schema is durable, but source retention and atomic backend selection preserve a tested rollback path until a later startup proves readback.</reversibility>
  <verify>
    <automated>JAVA_HOME=/opt/homebrew/opt/openjdk@17 ANDROID_HOME=/opt/homebrew/share/android-commandlinetools ANDROID_SDK_ROOT=/opt/homebrew/share/android-commandlinetools PATH=/opt/homebrew/opt/openjdk@17/bin:$PATH ./mobile/android/gradlew --offline --no-daemon -p mobile/android :app:compileDebugAndroidTestKotlin</automated>
  </verify>
  <done>Every migration journal interruption is recoverable, successful activation reads back exactly once, and the rollback source remains retained through the independent validation startup.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Register a narrow independently tested Library bridge</name>
  <files>mobile/android/app/src/main/java/com/listen2mobile/library/LibraryBridge.kt, mobile/android/app/src/main/java/com/listen2mobile/library/LibraryPackage.kt, mobile/android/app/src/main/java/com/listen2mobile/MainApplication.kt, mobile/android/app/src/test/java/com/listen2mobile/library/LibraryBridgeContractTest.kt</files>
  <behavior>
    - Only named snapshot, migration, mutation, and later native-internal transaction capabilities are exposed.
    - Unsupported method/operation/schema, oversized DTO, and raw private-field attempts return stable public errors.
    - Package registration creates one application-scoped repository and survives Activity recreation.
  </behavior>
  <action>Create `LibraryBridge` and a dedicated `LibraryPackage`, register it explicitly in `MainApplication`, and keep the database/repository application-scoped. Validate each public DTO recursively and serialize explicit allow-listed fields. Do not expose DAO/SQL, arbitrary storage, caller-supplied URL/header, clock, file, content resolver, credential, log, or exception capabilities. Test public method names, bounds, error shapes, redaction, and lifecycle-independent repository identity.</action>
  <verify>
    <automated>JAVA_HOME=/opt/homebrew/opt/openjdk@17 ANDROID_HOME=/opt/homebrew/share/android-commandlinetools ANDROID_SDK_ROOT=/opt/homebrew/share/android-commandlinetools PATH=/opt/homebrew/opt/openjdk@17/bin:$PATH ./mobile/android/gradlew --offline --no-daemon -p mobile/android :app:testDebugUnitTest --tests 'com.listen2mobile.library.LibraryBridgeContractTest'</automated>
  </verify>
  <done>The registered bridge exposes only safe versioned repository capabilities and its contract suite rejects unsafe or unbounded input/output.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Severity | Disposition | Mitigation |
|---|---|---:|---|---|
| T-06-01-01 | Tampering—bridge mutation | high | mitigate | DTO bounds, operation allow-list, expected revision, request idempotency, Room transaction. |
| T-06-01-02 | Information disclosure—snapshot/migration | high | mitigate | Explicit safe serialization and recursive private-field rejection. |
| T-06-01-03 | Denial of service—legacy import | medium | mitigate | Byte/count/depth/string bounds and staged rollback. |
| T-06-01-SC | Tampering—Gradle supply chain | high | mitigate | Official Google Maven coordinates audited in 06-RESEARCH.md and deterministic versions/schema output. |
</threat_model>

<verification>
Run the exact JVM selectors and compile the instrumentation source with Java 17 Gradle `:app:testDebugUnitTest :app:compileDebugAndroidTestKotlin`, then `git diff --check`. The controlled database fixture itself runs once only after 06-07; it is Phase 6 contract evidence, not Phase 8 installed-product/APK acceptance.
</verification>

<success_criteria>
DATA-001 and the persistence portion of LIB-003 have real Room CRUD/reopen/migration evidence, a narrow registered bridge, and a reversible activation path without Phase 7 cache behavior.
</success_criteria>

<output>Create `.planning/phases/06-personal-library-continuity/06-01-SUMMARY.md`.</output>
