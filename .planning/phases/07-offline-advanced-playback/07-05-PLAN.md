---
phase: 07-offline-advanced-playback
plan: 05
type: execute
wave: 4
depends_on: [07-01, 07-02, 07-03, 07-04]
files_modified:
  - mobile/android/app/src/main/java/com/listen2mobile/media/NativeMediaDescriptor.kt
  - mobile/android/app/src/main/java/com/listen2mobile/bilibili/BilibiliPolicy.kt
  - mobile/android/app/src/main/java/com/listen2mobile/bilibili/BilibiliMvPolicy.kt
  - mobile/android/app/src/main/java/com/listen2mobile/offline/OfflineCore.kt
  - mobile/android/app/src/test/java/com/listen2mobile/security/Phase7EntitlementMatrixTest.kt
  - mobile/android/app/src/main/AndroidManifest.xml
  - mobile/android/app/src/main/res/xml/backup_rules.xml
  - mobile/package.json
  - mobile/scripts/verify-phase7-security.mjs
  - mobile/src/security/__tests__/phase7Security.test.ts
autonomous: true
requirements: [PLAY-002, CACHE-001, CACHE-002, CACHE-003, CACHE-004, FX-001, FX-002, FX-003, AI-001, AI-002, AI-003, SEC-004]
estimate:
  tokens: 17000
  raw_tokens: 17000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Every play, download, resume, cached play, Bilibili part/quality, MV, retry, and account/login transition uses one fail-closed entitlement vocabulary per D-04."
    - "Source, packaged assets, JS/native state, logs/errors, test outputs, Android backup, and the later Phase 8 APK scan expose no secret, signed URL, private path, media bytes, full lyric request payload, or raw model response."
    - "The complete deterministic Phase 7 source/JVM/Jest/type/lint gate passes once after all functional slices are implemented, with device-only claims left to Phase 8 per D-10."
  artifacts:
    - path: mobile/android/app/src/test/java/com/listen2mobile/security/Phase7EntitlementMatrixTest.kt
      provides: Cross-provider/use-boundary entitlement and denial/fallback matrix
    - path: mobile/scripts/verify-phase7-security.mjs
      provides: Repeatable source/assets/backup/artifact canary scanner with Phase 8 APK input support
    - path: mobile/android/app/src/main/res/xml/backup_rules.xml
      provides: Explicit Android data-extraction exclusions for secrets, database/cache/media and analysis data
  key_links:
    - from: mobile/android/app/src/test/java/com/listen2mobile/security/Phase7EntitlementMatrixTest.kt
      to: mobile/android/app/src/main/java/com/listen2mobile/media/NativeMediaDescriptor.kt
      via: every privileged media action and account generation fixture
      pattern: "entitlement|accountGeneration|rendition|part"
    - from: mobile/android/app/src/main/AndroidManifest.xml
      to: mobile/android/app/src/main/res/xml/backup_rules.xml
      via: fullBackupContent/dataExtractionRules declarations
      pattern: "backup_rules|dataExtractionRules|fullBackupContent"
---

<objective>
Close SEC-004 across all Phase 7 capabilities and run one fast integrated deterministic gate after feature implementation.

Purpose: Prove that entitlement cannot be bypassed and sensitive media/AI/cache material cannot escape source, bridge, state, backup, logs, tests, or artifacts before Phase 8 device acceptance.
Output: Cross-feature entitlement matrix, explicit backup policy, repeatable security scanner, and one complete source/JVM/Jest/type/lint result.
D-02: All functional plans precede this one complete fast gate; no per-plan APK or emulator loop is introduced.
D-01: Every implementation and test target in this phase remains under the canonical `mobile/` tree; desktop and legacy Android sources are reference-only.
</objective>

<execution_context>
@/Users/fluenteng/.codex/gsd-core/workflows/execute-plan.md
@/Users/fluenteng/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-offline-advanced-playback/07-CONTEXT.md
@.planning/phases/07-offline-advanced-playback/07-RESEARCH.md
@.planning/phases/07-offline-advanced-playback/07-VALIDATION.md
@.planning/phases/07-offline-advanced-playback/07-01-SUMMARY.md
@.planning/phases/07-offline-advanced-playback/07-02-SUMMARY.md
@.planning/phases/07-offline-advanced-playback/07-03-SUMMARY.md
@.planning/phases/07-offline-advanced-playback/07-04-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Deny one stale-account cached rendition across every privileged path</name>
  <files>mobile/android/app/src/main/java/com/listen2mobile/media/NativeMediaDescriptor.kt, mobile/android/app/src/main/java/com/listen2mobile/bilibili/BilibiliPolicy.kt, mobile/android/app/src/main/java/com/listen2mobile/bilibili/BilibiliMvPolicy.kt, mobile/android/app/src/main/java/com/listen2mobile/offline/OfflineCore.kt, mobile/android/app/src/test/java/com/listen2mobile/security/Phase7EntitlementMatrixTest.kt</files>
  <behavior>
    - A descriptor/download created under account generation A is denied after logout/login generation B until native re-resolution establishes current authorization.
    - The same anonymous/member/region/DRM/expired/provider-unavailable vocabulary governs fresh play, retry/CDN, download/resume, cached play, part/quality change, and MV; denial never advances to bytes/player/video.
    - Authorized degradation selects only a native-reported lower rendition or audio-only fallback; caller-forged membership, rendition, part, cached flag, or URL cannot bypass policy.
    - All denial results contain bounded code/source/stage/action only and no credentials, signed transport, provider body, path, or raw exception.
  </behavior>
  <action>Build the cross-feature SEC-004 entitlement matrix first, then close any policy divergence it exposes in the shared descriptor, Bilibili/MV, and offline boundaries. Per D-04, require current native entitlement/account generation at download acquisition and every use/login boundary; treat unknown as denied and separate retryable provider availability from non-retryable membership/region/DRM denial. Ensure quality/part/CDN/audio fallback is selected only from the current native authorization response. Test all five providers with deterministic fakes and assert downstream transport/player/cache/MV call counts remain zero after denial. Preserve stable actionable errors and sanitize all exception/provider text.</action>
  <verify>
    <automated>JAVA_HOME=/opt/homebrew/opt/openjdk@17 ANDROID_HOME=/opt/homebrew/share/android-commandlinetools ANDROID_SDK_ROOT=/opt/homebrew/share/android-commandlinetools PATH=/opt/homebrew/opt/openjdk@17/bin:$PATH ./mobile/android/gradlew --offline --no-daemon -p mobile/android :app:testDebugUnitTest --tests 'com.listen2mobile.security.Phase7EntitlementMatrixTest'</automated>
  </verify>
  <done>The complete privileged-action matrix shares one fail-closed current-account policy, every denial blocks downstream access, and only authorized native-reported degradation remains possible.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce backup/artifact/log secrecy and run the complete fast gate</name>
  <files>mobile/android/app/src/main/AndroidManifest.xml, mobile/android/app/src/main/res/xml/backup_rules.xml, mobile/package.json, mobile/scripts/verify-phase7-security.mjs, mobile/src/security/__tests__/phase7Security.test.ts</files>
  <behavior>
    - Android backup/data-extraction excludes databases, Keystore-dependent ciphertext metadata, caches, private media, partials, analysis data, and files that can contain provider/AI payloads; explicit semantic backup remains the supported transfer route.
    - Scanner detects injected marker key/cookie/signed-URL/private-path/full-lyric/raw-model canaries in source, copied assets, safe error/log fixtures, JS persistence snapshots, test results, and optional APK/unpacked paths.
    - Scanner/contract checks reject any production `BootstrapTrack`/raw URL-header playback contract, registered raw source module, legacy download-slice import/registration, or player-addressable partial directory.
    - Scanner accepts the clean tree and fails with a path plus non-secret classification, never echoing the detected secret/canary value.
    - Full Jest, TypeScript, lint, JVM, and security gates pass together after all Phase 7 behavior; no APK/emulator/live claim is emitted.
  </behavior>
  <action>Add explicit Android backup/data-extraction rules and wire them in the manifest so system backup excludes the whole Room database and private/cache/media/analysis/DeepSeek transaction storage; preserve the existing explicit semantic export/import contract. Create a dependency-free Node scanner with bounded file types/roots, binary-safe handling, redacted findings, known generated/vendor exclusions, marker-canary self-tests, and an optional path argument Phase 8 can use for unpacked APK artifacts. Add a package script and security test that inspects JS store/navigation/error snapshots and exact bridge schemas, proves the retired raw bootstrap/URL-header contract and legacy download slice have no production import/registration/call site, confirms all registered remote modules return safe descriptors, and proves attempt partial paths are absent from every player-facing resolver. Run one complete deterministic integration gate after the scanner passes. Per D-09, include key/prompt/raw-response canaries; per D-06 include signed transport/header/path canaries; per D-10 report APK/backup extraction/runtime logs as Phase 8 proof obligations rather than inferred passes.</action>
  <verify>
    <automated>cd mobile &amp;&amp; npm run verify:phase7-security &amp;&amp; npm test -- --runInBand &amp;&amp; ./node_modules/.bin/tsc --noEmit &amp;&amp; npm run lint &amp;&amp; cd .. &amp;&amp; JAVA_HOME=/opt/homebrew/opt/openjdk@17 ANDROID_HOME=/opt/homebrew/share/android-commandlinetools ANDROID_SDK_ROOT=/opt/homebrew/share/android-commandlinetools PATH=/opt/homebrew/opt/openjdk@17/bin:$PATH ./mobile/android/gradlew --offline --no-daemon -p mobile/android :app:testDebugUnitTest</automated>
  </verify>
  <done>Backup boundaries and the repeatable redacting scanner enforce SEC-004, and the complete Phase 7 deterministic gate passes once with device-only coverage explicitly handed to Phase 8.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Account/provider state -> privileged feature | Current entitlement decides access to network, cache, player, rendition and MV. |
| Runtime/private storage -> Android backup | OS backup could export app databases/files unless explicitly excluded. |
| Source/test/build artifact -> developer/evaluator | Secrets or payload canaries could escape through logs, fixtures, copied assets or APK contents. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-07-05-01 | Spoofing/Elevation | Entitlement matrix | critical | mitigate | Native current-account generation, exact allowed part/rendition and zero downstream calls on denial. |
| T-07-05-02 | Information disclosure | Android backup | critical | mitigate | Explicit data-extraction/full-backup exclusions and semantic export as the only supported transfer. |
| T-07-05-03 | Information disclosure | Logs/assets/tests/artifacts | critical | mitigate | Redacting repeatable scanner plus canary self-tests and exact safe bridge/state schemas. |
| T-07-05-04 | Denial of service | Scanner/gates | low | accept | Bounded roots/file types and vendor/generated exclusions keep the local gate predictable; failures block completion with classification. |
| T-07-SC | Tampering | Package installs | high | mitigate | No new npm/pip/cargo package; scanner is dependency-free and existing locks remain unchanged except the audited AndroidX dependency from 07-02. |
</threat_model>

<verification>
The Task 2 command is the Phase 7 integrated deterministic acceptance gate. Record command, branch/SHA, dirty-tree scope, JDK/SDK/Node versions, start/end time, and result in the summary. Do not run `assemble*`, install an APK, launch an emulator, or use live credentials here; list those as Phase 8 required evidence per D-10.
</verification>

<success_criteria>
- Entitlement/membership/region/DRM/account-generation bypass fixtures fail closed across every privileged action.
- Backup rules and canary scanners prove no Phase 7 secret/media/transport/AI payload enters covered source/state/log/test/asset paths.
- Full Jest, TypeScript, lint, JVM, and security gates pass once after all feature plans.
</success_criteria>

<output>
Create `.planning/phases/07-offline-advanced-playback/07-05-SUMMARY.md` when done.
</output>
