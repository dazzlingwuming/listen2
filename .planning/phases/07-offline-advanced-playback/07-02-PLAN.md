---
phase: 07-offline-advanced-playback
plan: 02
type: execute
wave: 2
depends_on: [07-01, 07-04]
files_modified:
  - mobile/android/app/build.gradle
  - mobile/android/app/src/main/AndroidManifest.xml
  - mobile/android/app/src/main/java/com/listen2mobile/library/LibraryDatabase.kt
  - mobile/android/app/src/main/java/com/listen2mobile/library/LibraryRepository.kt
  - mobile/android/app/src/main/java/com/listen2mobile/offline/OfflineCatalogRepository.kt
  - mobile/android/app/src/main/java/com/listen2mobile/offline/OfflineCore.kt
  - mobile/android/app/src/main/java/com/listen2mobile/offline/OfflineAudioModule.kt
  - mobile/android/app/schemas/com.listen2mobile.library.Listen2Database/4.json
  - mobile/android/app/src/test/java/com/listen2mobile/offline/OfflineCatalogMigrationTest.kt
  - mobile/android/app/src/test/java/com/listen2mobile/offline/OfflineAudioContractTest.kt
  - mobile/android/app/src/test/java/com/listen2mobile/offline/OfflineRecoveryContractTest.kt
  - mobile/src/offline/offlineAudio.ts
  - mobile/src/player/playbackService.ts
  - mobile/src/store/downloadSlice.ts
  - mobile/src/store/index.ts
  - mobile/src/screens/CacheLibraryScreen.tsx
  - mobile/src/screens/SettingsScreen.tsx
  - mobile/src/screens/SearchScreen.tsx
  - mobile/src/navigation/types.ts
  - mobile/src/navigation/RootNavigator.tsx
  - mobile/src/offline/__tests__/cacheLibrary.test.tsx
  - mobile/src/offline/__tests__/offlineLegacyChainRemoval.test.ts
autonomous: true
requirements: [CACHE-001, CACHE-002, CACHE-003, CACHE-004, SEC-004]
estimate:
  tokens: 39000
  raw_tokens: 39000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Per D-03, actual playback, playlist association, and explicit user action create distinct owners over deduplicated verified media."
    - "Only complete length/hash/media-readable and currently authorized entries can play; partial, corrupt, cancelled, stale-account, and repair states cannot reach TrackPlayer."
    - "Per D-05, quota is 2 GB by default, exposes 1/5/10 GB and nullable unlimited, and LRU never removes explicit downloads."
    - "Users can search, sort, filter, promote, retry/repair, delete selected entries, and clear eligible entries through a reachable cache library."
    - "Temporary, playlist and explicit owners have distinct physical alias directories and indexes over one deduplicated content-addressed blob; attempt partials have no playable alias or route."
  artifacts:
    - path: mobile/android/app/src/main/java/com/listen2mobile/offline/OfflineCatalogRepository.kt
      provides: Room-authoritative blob/owner/attempt/analysis catalog and transactional policies
    - path: mobile/android/app/src/main/java/com/listen2mobile/offline/OfflineCore.kt
      provides: WorkManager-backed resume/cancel/acquire/verify/repair/evict engine
    - path: mobile/src/screens/CacheLibraryScreen.tsx
      provides: Complete cache management UI
  key_links:
    - from: mobile/src/player/playbackService.ts
      to: mobile/android/app/src/main/java/com/listen2mobile/offline/OfflineAudioModule.kt
      via: actual PlaybackState.Playing owner signal and ready lookup
      pattern: "temporary|resolveReady|accountGeneration"
    - from: mobile/android/app/src/main/java/com/listen2mobile/library/LibraryRepository.kt
      to: mobile/android/app/src/main/java/com/listen2mobile/offline/OfflineCatalogRepository.kt
      via: playlist membership transaction creates/removes playlist owner
      pattern: "playlist|owner"
---

<objective>
Replace the limited JSON download queue with a durable, authorized, owner-aware offline cache and expose its complete mobile management UI.

Purpose: Deliver CACHE-001 through CACHE-004 with the ownership, authorization, recovery, quota, integrity, scheduling, and privacy behavior locked in D-03 through D-05.
Output: Room v4 cache catalog, constrained durable acquisition engine, safe playback lookup, cache library UI, and deterministic recovery/policy tests.
</objective>

<execution_context>
@/Users/fluenteng/.codex/gsd-core/workflows/execute-plan.md
@/Users/fluenteng/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-offline-advanced-playback/07-CONTEXT.md
@.planning/phases/07-offline-advanced-playback/07-RESEARCH.md
@.planning/phases/06-personal-library-continuity/06-06-SUMMARY.md
@mobile/android/app/src/main/java/com/listen2mobile/library/LibraryDatabase.kt
@mobile/android/app/src/main/java/com/listen2mobile/offline/OfflineCore.kt
@mobile/src/offline/offlineAudio.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Persist one explicit download as a verified Room-owned blob</name>
  <files>mobile/android/app/src/main/java/com/listen2mobile/library/LibraryDatabase.kt, mobile/android/app/src/main/java/com/listen2mobile/library/LibraryRepository.kt, mobile/android/app/src/main/java/com/listen2mobile/offline/OfflineCatalogRepository.kt, mobile/android/app/schemas/com.listen2mobile.library.Listen2Database/4.json, mobile/android/app/src/test/java/com/listen2mobile/offline/OfflineCatalogMigrationTest.kt</files>
  <behavior>
    - Migrating v3 to v4 preserves every Phase 6 table/row and creates normalized blob, owner, transfer-attempt, verified-range, quota/use, and analysis-metadata structures without paths or signed URLs.
    - Explicit acquisition publishes one ready blob only after expected length, SHA-256, signature/codec, fsync/atomic move, and player-provider read-open checks succeed.
    - Repeating or promoting the same identity adds/updates owner rows without duplicating the physical blob; removing one owner retains bytes required by another.
  </behavior>
  <action>Expand the current skeletal `cache_catalog` through an explicit Room v3-to-v4 migration and exported schema, adapting to all Phase 6 changes already present rather than overwriting them. Implement a transactional catalog keyed by source/semantic track/part/rendition/media revision and content hash, with separate owner and attempt/range state. Create distinct physical alias/index roots for `owners/temporary`, `owners/playlist/<playlist-key>`, and `owners/explicit`, one content-addressed `blobs/` root for deduplicated media, and a separate `attempts/` root that no player-facing URI can address. Per D-03, make `explicit` creatable only through a user-intent method and make playlist membership transactions add/remove `playlist:<id>` aliases without copying the blob or converting ownership. Store only semantic identity, sizes, hashes, MIME/codec, bounded state/status, timestamps, and private relative keys. Migrate the old JSON/files through a one-time idempotent reconciliation: verify and import recoverable complete media, quarantine/delete untrusted partial/orphan/duplicate state, preserve retry intent, and make Room the sole writer only after repair succeeds.</action>
  <verify>
    <automated>JAVA_HOME=/opt/homebrew/opt/openjdk@17 ANDROID_HOME=/opt/homebrew/share/android-commandlinetools ANDROID_SDK_ROOT=/opt/homebrew/share/android-commandlinetools PATH=/opt/homebrew/opt/openjdk@17/bin:$PATH ./mobile/android/gradlew --offline --no-daemon -p mobile/android :app:testDebugUnitTest --tests 'com.listen2mobile.offline.OfflineCatalogMigrationTest'</automated>
  </verify>
  <done>A migrated or fresh database has one authoritative owner-aware catalog, explicit acquisition becomes verified ready media atomically, and Phase 6 data remains intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make acquisition resumable, constrained, authorized, repairable, and quota-safe</name>
  <files>mobile/android/app/src/main/java/com/listen2mobile/offline/OfflineCore.kt, mobile/android/app/src/main/java/com/listen2mobile/offline/OfflineAudioModule.kt, mobile/android/app/build.gradle, mobile/android/app/src/main/AndroidManifest.xml, mobile/android/app/src/test/java/com/listen2mobile/offline/OfflineAudioContractTest.kt, mobile/android/app/src/test/java/com/listen2mobile/offline/OfflineRecoveryContractTest.kt</files>
  <behavior>
    - Unique durable work survives recreation, obeys network/battery/storage constraints, publishes a bounded foreground notification for user work, and cancellation tombstones prevent late publication.
    - Valid byte-range/validator responses resume; ignored ranges, changed ETag/media revision, expired leases, and account changes restart or fail safely without duplicate ready files.
    - Ready lookup revalidates entitlement per D-04 and never serves partial/corrupt/unreadable media; bounded repair reconciles missing/orphan/duplicate rows/files and disk-full states.
    - Quota defaults to 2 GB, accepts exact 1/5/10 GB or `null`, accounts for concurrent reservations/partials, and evicts oldest non-explicit media only.
    - No `attempts/` partial, missing alias, stale owner index, orphan blob, or pre-migration file can resolve through the content provider until bounded repair publishes a valid ready row and owner alias.
  </behavior>
  <action>Add audited WorkManager runtime/testing dependencies and implement unique per-identity acquisition work in the offline core, including explicit constraints, foreground notification/cancel action, progress, process-safe idempotency, Range plus validator resume, resolver lease renewal, attempt-scoped partials, and atomic verified publication. Publish the content-addressed blob and owner alias/index transactionally only after length/hash/signature/codec/read-open succeed; provider resolution requires both ready blob and valid owner alias and rejects the entire attempts root. Per D-04, revalidate current account/member/region/DRM entitlement at enqueue, download/resume, ready lookup, play, and login/account-generation changes. Per D-05, store nullable quota, implement transactional reservations and LRU selection that excludes every explicit-owned blob, and return stable disk-full/constraint/repair/entitlement statuses. Make repair bounded and idempotent for migrated/orphan/missing/duplicate alias/blob/attempt state. Adapt the prior offline contract fixtures to the new catalog without running the old suite here, add the new recovery selector, and keep worker/network internals native-private.</action>
  <verify>
    <automated>JAVA_HOME=/opt/homebrew/opt/openjdk@17 ANDROID_HOME=/opt/homebrew/share/android-commandlinetools ANDROID_SDK_ROOT=/opt/homebrew/share/android-commandlinetools PATH=/opt/homebrew/opt/openjdk@17/bin:$PATH ./mobile/android/gradlew --offline --no-daemon -p mobile/android :app:testDebugUnitTest --tests 'com.listen2mobile.offline.OfflineRecoveryContractTest'</automated>
  </verify>
  <done>All five source identities can acquire/resume/cancel/repair under durable constraints, only complete authorized media is playable, quota/LRU follows D-05, and no partial becomes ready.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Connect playback ownership and deliver the complete cache library</name>
  <files>mobile/src/offline/offlineAudio.ts, mobile/src/player/playbackService.ts, mobile/src/store/downloadSlice.ts, mobile/src/store/index.ts, mobile/src/screens/CacheLibraryScreen.tsx, mobile/src/screens/SettingsScreen.tsx, mobile/src/screens/SearchScreen.tsx, mobile/src/navigation/types.ts, mobile/src/navigation/RootNavigator.tsx, mobile/src/offline/__tests__/cacheLibrary.test.tsx, mobile/src/offline/__tests__/offlineLegacyChainRemoval.test.ts</files>
  <behavior>
    - A real transition to playing associates temporary ownership once per content generation; resolving/queueing without actual playback does not.
    - Settings exposes 1/2/5/10 GB and unlimited with `null` round-trip, current/reserved byte status, constraints, and a route into the cache library.
    - Cache library search/sort/filter and single/selected/eligible-clear/promote/retry/repair/cancel operations update bounded projections and display actionable stable errors.
    - Promotion reuses bytes and creates explicit ownership; delete/clear never removes a shared or explicit blob without the corresponding explicit user action.
    - Settings navigates to the typed CacheLibrary route; Search and playback use the new owner-aware client, and the legacy download slice/store registration/action path is removed.
  </behavior>
  <action>Replace the old download adapter with exact versioned owner/catalog/action types and subscribe to bounded native projections. On actual TrackPlayer playing state, signal temporary ownership per D-03; never create it at search, resolve, queue, or failed-start time. Move Search explicit-download actions to the new client, migrate any required presentation state out of `downloadSlice`, delete the legacy slice and its `store/index.ts` registration once no imports/actions/selectors remain, and add a selector that proves the old chain is unreachable. Add a typed `CacheLibrary` navigation route in `navigation/types.ts`/`RootNavigator.tsx`; make Settings navigate to it. Build the dedicated screen with accessible search, owner/status filters, sort, selection, bulk actions, promotion, retry/repair, cancellation, destructive confirmations, nullable quota, and honest constraint/disk/entitlement copy. Keep transport, paths, content URIs, partial ranges, credentials, and media bytes out of Redux/AsyncStorage/snapshots.</action>
  <verify>
    <automated>cd mobile &amp;&amp; npm test -- --runInBand src/offline/__tests__/cacheLibrary.test.tsx src/offline/__tests__/offlineLegacyChainRemoval.test.ts</automated>
  </verify>
  <done>Every owner cause and cache-library operation is reachable, accessible, deterministic, and privacy-safe, with explicit-only durability and honest recovery states.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| JS -> offline native module | User actions and semantic IDs request privileged downloads/deletes/repairs. |
| Worker -> provider/CDN | Network bytes, validators, redirects, and entitlement are untrusted. |
| Catalog -> private files/player provider | Database state controls whether local bytes become playable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-07-02-01 | Tampering | Partial/ready publication | critical | mitigate | Attempt IDs, length/hash/signature/read-open validation, fsync and atomic publication. |
| T-07-02-02 | Elevation of privilege | Entitlement/offline lookup | critical | mitigate | Revalidation at acquisition/use/account boundaries and account-generation binding. |
| T-07-02-03 | Denial of service | Quota/concurrent work | high | mitigate | Durable unique work, bounded concurrency/retries/repair and transactional reservations/LRU. |
| T-07-02-04 | Information disclosure | Catalog/backup/log/UI | high | mitigate | Private no-backup files and semantic bounded projections without URLs, paths, credentials or bytes. |
| T-07-SC | Tampering | AndroidX WorkManager dependency | high | mitigate | Use only audited official AndroidX coordinates/versions recorded in RESEARCH.md and existing repositories. |
</threat_model>

<verification>
Run only the newly added migration, recovery, cache-library, and legacy-chain selectors above. Per D-02/D-10, 07-05 owns the complete source/JVM gate and Phase 8 owns WorkManager/Doze/process-death/network switching/notification device proof.
</verification>

<success_criteria>
- Owner, integrity, entitlement, resume/cancel/repair, quota/LRU, and migration fixtures pass.
- A complete cache library exposes every CACHE-004 action without storing sensitive transport/file data in JS.
- Only verified ready media with current authorization reaches the player provider.
</success_criteria>

<output>
Create `.planning/phases/07-offline-advanced-playback/07-02-SUMMARY.md` when done.
</output>
