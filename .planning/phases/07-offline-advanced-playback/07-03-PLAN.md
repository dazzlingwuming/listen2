---
phase: 07-offline-advanced-playback
plan: 03
type: execute
wave: 3
depends_on: [07-01, 07-02]
files_modified:
  - mobile/patches/react-native-track-player+4.1.2.patch
  - mobile/android/app/src/main/java/com/listen2mobile/audiofx/AudioEffectsModule.kt
  - mobile/android/app/src/main/java/com/listen2mobile/audiofx/AudioEffectsPackage.kt
  - mobile/android/app/src/main/java/com/listen2mobile/audiofx/LoudnessAnalyzer.kt
  - mobile/android/app/src/main/java/com/listen2mobile/MainApplication.kt
  - mobile/android/app/src/main/AndroidManifest.xml
  - mobile/android/app/src/test/java/com/listen2mobile/audiofx/AudioEffectsContractTest.kt
  - mobile/android/app/src/test/java/com/listen2mobile/audiofx/LoudnessAnalyzerTest.kt
  - mobile/src/audioFx/client.ts
  - mobile/src/player/playerController.ts
  - mobile/src/screens/PlayerScreen.tsx
  - mobile/src/screens/__tests__/audioEffectsFlow.test.tsx
  - mobile/src/player/__tests__/loudnessPlaybackIntegration.test.ts
autonomous: true
requirements: [FX-001, FX-002, FX-003]
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Per D-07, presets attach only to the current player audio session, expose no session ID, and every failure preserves original audio."
    - "Visualization displays bounded real analyzer frames synchronized to playback or an honestly labelled static/hidden state."
    - "Per D-08, complete-media loudness analysis targets -14 LUFS/-1 dBTP asynchronously, invalidates by content identity, and never delays first play."
    - "Device volume/mute, user application volume, effect preset, and fixed normalization gain remain independent controls."
  artifacts:
    - path: mobile/patches/react-native-track-player+4.1.2.patch
      provides: Pinned native-only actual-session and independent fixed-gain seam
    - path: mobile/android/app/src/main/java/com/listen2mobile/audiofx/AudioEffectsModule.kt
      provides: Capability-driven Equalizer/Visualizer lifecycle
    - path: mobile/android/app/src/main/java/com/listen2mobile/audiofx/LoudnessAnalyzer.kt
      provides: Complete-media decode, LUFS/true-peak policy, and background work
  key_links:
    - from: mobile/android/app/src/main/java/com/listen2mobile/audiofx/AudioEffectsModule.kt
      to: mobile/patches/react-native-track-player+4.1.2.patch
      via: process-local native listener for actual session lifecycle
      pattern: "session|generation|release"
    - from: mobile/src/player/playerController.ts
      to: mobile/android/app/src/main/java/com/listen2mobile/audiofx/LoudnessAnalyzer.kt
      via: content-identity lookup and independent normalization-gain command
      pattern: "loudness|gain|contentHash"
---

<objective>
Deliver actual-session audio effects, genuine synchronized visualization, and non-blocking content-addressed loudness normalization.

Purpose: Satisfy FX-001 through FX-003 with native Android behavior and honest capability fallback per D-07 and D-08.
Output: Pinned RNTP session/gain seam, high-level native effects API, player controls/visualization, complete-media loudness worker, and deterministic failure/vector tests.
</objective>

<execution_context>
@/Users/fluenteng/.codex/gsd-core/workflows/execute-plan.md
@/Users/fluenteng/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-offline-advanced-playback/07-CONTEXT.md
@.planning/phases/07-offline-advanced-playback/07-RESEARCH.md
@mobile/patches/react-native-track-player+4.1.2.patch
@mobile/src/player/playerController.ts
@mobile/src/screens/PlayerScreen.tsx
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Apply one neutral-to-preset transition on the real TrackPlayer session</name>
  <files>mobile/patches/react-native-track-player+4.1.2.patch, mobile/android/app/src/main/java/com/listen2mobile/audiofx/AudioEffectsModule.kt, mobile/android/app/src/main/java/com/listen2mobile/audiofx/AudioEffectsPackage.kt, mobile/android/app/src/main/java/com/listen2mobile/MainApplication.kt, mobile/android/app/src/test/java/com/listen2mobile/audiofx/AudioEffectsContractTest.kt</files>
  <behavior>
    - The pinned RNTP service reports current audio-session create/replace/release through a process-local native listener; no React method/event exposes the numeric session ID.
    - Enable/select/reset/disable creates Equalizer only on that actual nonzero session, validates device band/range capability, and neutral reset restores flat effect state.
    - Missing session, unsupported device, constructor/parameter/control-loss/player-recreation failure releases effect state and returns a stable capability status while playback remains active and unmodified.
    - Normalization gain has a separate native seam from user volume and defaults to unity.
  </behavior>
  <action>Extend the already-pinned RNTP patch rather than editing generated dependency files: add a process-local native listener for the actual playback audio session and a separate fixed-gain input composed with existing app volume. If the current pinned player internals cannot provide a supported listener directly, add the smallest source seam in the patch; do not use reflection, public JS session IDs, broadcasts, or output-mix/session-zero effects. Implement/register `AudioEffectsModule` with exact versioned high-level capability and preset commands, validated preset/band ranges, generation-aware recreation, idempotent reset/disable, and deterministic release on session loss. Per D-07, catch every platform effect failure at the boundary and restore neutral/original audio.</action>
  <verify>
    <automated>JAVA_HOME=/opt/homebrew/opt/openjdk@17 ANDROID_HOME=/opt/homebrew/share/android-commandlinetools ANDROID_SDK_ROOT=/opt/homebrew/share/android-commandlinetools PATH=/opt/homebrew/opt/openjdk@17/bin:$PATH ./mobile/android/gradlew --offline --no-daemon -p mobile/android :app:testDebugUnitTest --tests 'com.listen2mobile.audiofx.AudioEffectsContractTest'</automated>
  </verify>
  <done>A real player session supports capability-driven preset lifecycle, the session identifier remains native-private, and every unsupported/error path leaves playback at neutral/original audio.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render genuine foreground visualization with labelled fallback</name>
  <files>mobile/android/app/src/main/java/com/listen2mobile/audiofx/AudioEffectsModule.kt, mobile/android/app/src/main/AndroidManifest.xml, mobile/src/audioFx/client.ts, mobile/src/screens/PlayerScreen.tsx, mobile/src/screens/__tests__/audioEffectsFlow.test.tsx</files>
  <behavior>
    - Visualization asks for RECORD_AUDIO only after the user enables it, then captures capped frames from the current nonzero player session while foreground and playing.
    - Pause, seek, track/session generation change, background, permission loss, and disable stop/release capture; stale-generation frames are ignored.
    - Permission denied, low-end policy, unsupported capture, or native failure shows an explicit static/hidden label and emits no fabricated spectrum values.
    - Effect enable/disable/select/reset and capability/reason state are accessible from the player without coupling device volume/mute to presets.
  </behavior>
  <action>Add only the Android permission required for specific-session Visualizer and request it from the explicit visualization user action with explanatory copy. Expose bounded frame bins, monotonic timestamp, playback generation, and stable analyzer status through the high-level client; cap capture size/rate and retain no frames after rendering. Synchronize native resource lifecycle with playing/pause/seek/track/session/background events. Render real frames in PlayerScreen and show an accessible labelled static or hidden state for every unsupported/denied/background/low-end condition per D-07. Add accessible preset/reset/enable controls and safe error copy; never animate random, stale, or synthetic data as live analysis.</action>
  <verify>
    <automated>cd mobile &amp;&amp; npm test -- --runInBand src/screens/__tests__/audioEffectsFlow.test.tsx</automated>
  </verify>
  <done>Foreground supported playback renders real synchronized frames; lifecycle and capability fallbacks are explicit, and effect failures or permission denial never interrupt audio.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Analyze complete cached media and apply safe fixed loudness gain</name>
  <files>mobile/android/app/src/main/java/com/listen2mobile/audiofx/LoudnessAnalyzer.kt, mobile/android/app/src/test/java/com/listen2mobile/audiofx/LoudnessAnalyzerTest.kt, mobile/android/app/src/main/java/com/listen2mobile/offline/OfflineCatalogRepository.kt, mobile/src/player/playerController.ts, mobile/src/player/__tests__/loudnessPlaybackIntegration.test.ts</files>
  <behavior>
    - Complete readable media decodes to PCM, calculates integrated LUFS and true peak against reference-vector tolerances, targets approximately -14 LUFS, and clamps predicted peak to -1 dBTP.
    - Metrics are reusable only when content hash, sample rate, codec, and analyzer version all match; any mismatch invalidates and returns unity gain.
    - First play starts immediately with unity/current valid gain; eligible analysis is scheduled asynchronously and a completed result applies only to the same active content generation.
    - Short, corrupt, unsupported-codec, decoder, worker, NaN, or storage failure records a bounded failure and preserves original volume.
  </behavior>
  <action>Implement a pure tested BS.1770/EBU-style loudness/true-peak core and a WorkManager-backed complete-media decoder using `MediaExtractor`/`MediaCodec`. Read only verified ready blobs through the offline repository, persist bounded metrics keyed by hash + sample rate + codec + analyzer version, and never persist PCM. Per D-08, return unity synchronously when metrics are absent/stale/failed, enqueue analysis without delaying initial playback, and apply completed gain only if the player content generation and identity still match. Compose normalization gain through the independent native gain seam without changing device volume, mute, or the user's application volume; clamp gain by the -1 dBTP ceiling and restore unity on every failure/track change.</action>
  <verify>
    <automated>JAVA_HOME=/opt/homebrew/opt/openjdk@17 ANDROID_HOME=/opt/homebrew/share/android-commandlinetools ANDROID_SDK_ROOT=/opt/homebrew/share/android-commandlinetools PATH=/opt/homebrew/opt/openjdk@17/bin:$PATH ./mobile/android/gradlew --offline --no-daemon -p mobile/android :app:testDebugUnitTest --tests 'com.listen2mobile.audiofx.LoudnessAnalyzerTest' &amp;&amp; cd mobile &amp;&amp; npm test -- --runInBand src/player/__tests__/loudnessPlaybackIntegration.test.ts</automated>
  </verify>
  <done>Reference vectors and invalidation pass, first play never waits, matching completed media receives peak-safe fixed gain, and every absent/failure case remains unity/original audio.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| RNTP service -> app native bridge | Privileged actual audio session lifecycle crosses a dependency seam. |
| JS -> effects module | UI requests effect/capture behavior but must not control session IDs or raw audio. |
| Cached media -> decoder | Potentially malformed media enters native codec and analysis work. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-07-03-01 | Elevation/Information disclosure | Audio session bridge | high | mitigate | Process-local native listener, actual nonzero session only, no JS/event/session identifier exposure. |
| T-07-03-02 | Information disclosure | Visualizer | high | mitigate | Explicit permission, player-session capture only, foreground lifecycle, bounded ephemeral frames, no PCM persistence. |
| T-07-03-03 | Denial of service | Effects/decoder | medium | mitigate | Capability checks, bounded frame/work input, release on lifecycle/errors, WorkManager constraints. |
| T-07-03-04 | Tampering | Loudness metric reuse | medium | mitigate | Hash/sample-rate/codec/analyzer-version identity and active-generation checks. |
| T-07-SC | Tampering | Dependency patch | high | mitigate | Modify only the pinned lockfile-matched patch and verify clean patch application; no new npm dependency. |
</threat_model>

<verification>
Run only the newly added effect, visualization, loudness, and playback-integration selectors above. The complete source/JVM gate is 07-05; per D-10, real codec/route/Bluetooth/headset/permission/device audio and measured LUFS evidence belong to Phase 8.
</verification>

<success_criteria>
- Preset lifecycle uses the real player session and never interrupts playback on error.
- Visualization contains only real current-generation frames or an explicit labelled fallback.
- Loudness analysis is complete-media-only, content-addressed, non-blocking, peak-limited, and unity on failure.
</success_criteria>

<output>
Create `.planning/phases/07-offline-advanced-playback/07-03-SUMMARY.md` when done.
</output>
