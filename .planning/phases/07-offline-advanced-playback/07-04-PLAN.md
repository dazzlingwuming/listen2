---
phase: 07-offline-advanced-playback
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - mobile/android/app/src/main/java/com/listen2mobile/deepseek/DeepSeekVault.kt
  - mobile/android/app/src/main/java/com/listen2mobile/deepseek/DeepSeekKeyActivity.kt
  - mobile/android/app/src/main/java/com/listen2mobile/deepseek/DeepSeekModule.kt
  - mobile/android/app/src/main/java/com/listen2mobile/deepseek/DeepSeekClient.kt
  - mobile/android/app/src/main/java/com/listen2mobile/deepseek/DeepSeekPolicy.kt
  - mobile/android/app/src/main/java/com/listen2mobile/deepseek/DeepSeekTranslationCache.kt
  - mobile/android/app/src/test/java/com/listen2mobile/deepseek/DeepSeekPrivacyContractTest.kt
  - mobile/android/app/src/test/java/com/listen2mobile/deepseek/DeepSeekContractTest.kt
  - mobile/src/deepseek/client.ts
  - mobile/src/deepseek/consent.ts
  - mobile/src/deepseek/types.ts
  - mobile/src/components/DeepSeekConsentSheet.tsx
  - mobile/src/screens/PlayerScreen.tsx
  - mobile/src/screens/SettingsScreen.tsx
  - mobile/src/screens/__tests__/playerTranslationBehavior.test.tsx
  - mobile/src/screens/__tests__/deepSeekFlow.test.tsx
  - mobile/src/screens/__tests__/deepSeekRevisionLifecycle.test.tsx
  - mobile/src/screens/__tests__/deepSeekSettingsStatus.test.tsx
  - mobile/src/deepseek/__tests__/client.test.ts
  - mobile/src/deepseek/__tests__/consent.test.ts
  - mobile/src/deepseek/__tests__/nativeWiring.test.js
autonomous: true
requirements: [AI-001, AI-002, AI-003, SEC-004]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Per D-09, unavailable/corrupt Android Keystore makes key/test/translate fail closed and no plaintext fallback exists."
    - "The user sees configured status/test outcome but never the key; clear removes ciphertext, alias, pending work, and safe status."
    - "Translation sends only after complete explicit consent, cancellation before consent makes zero network calls, and failure has no cache side effect."
    - "Only exact revision/schema/line/order/timeline/completeness-valid translations persist; raw request/provider result stays native-private."
  artifacts:
    - path: mobile/android/app/src/main/java/com/listen2mobile/deepseek/DeepSeekVault.kt
      provides: Keystore-only sealed key storage with fail-closed states
    - path: mobile/android/app/src/main/java/com/listen2mobile/deepseek/DeepSeekPolicy.kt
      provides: Native schema/alignment/revision/completeness validation
    - path: mobile/src/components/DeepSeekConsentSheet.tsx
      provides: Complete explicit disclosure and confirm/cancel behavior
  key_links:
    - from: mobile/src/deepseek/client.ts
      to: mobile/android/app/src/main/java/com/listen2mobile/deepseek/DeepSeekModule.kt
      via: exact one-use consent operation and safe status/projection response
      pattern: "consent|revision|operationId|status"
    - from: mobile/android/app/src/main/java/com/listen2mobile/deepseek/DeepSeekClient.kt
      to: mobile/android/app/src/main/java/com/listen2mobile/deepseek/DeepSeekPolicy.kt
      via: native raw response parsing before private-cache commit
      pattern: "validate|alignment|revision"
---

<objective>
Close the DeepSeek security and privacy boundary so keys are Keystore-only, consent is complete, and raw provider transactions remain native-private.

Purpose: Complete AI-001 through AI-003 and the DeepSeek portion of SEC-004 per D-09 without weakening the already-delivered translation UX.
Output: Fail-closed vault, native validation/cache transaction, exact safe JS contract, consent behavior, and canary/negative-path tests.
</objective>

<execution_context>
@/Users/fluenteng/.codex/gsd-core/workflows/execute-plan.md
@/Users/fluenteng/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-offline-advanced-playback/07-CONTEXT.md
@.planning/phases/07-offline-advanced-playback/07-RESEARCH.md
@mobile/android/app/src/main/java/com/listen2mobile/deepseek/DeepSeekVault.kt
@mobile/android/app/src/main/java/com/listen2mobile/deepseek/DeepSeekModule.kt
@mobile/src/deepseek/client.ts
@mobile/src/components/DeepSeekConsentSheet.tsx
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Configure, test, use, and clear one key through Keystore-only storage</name>
  <files>mobile/android/app/src/main/java/com/listen2mobile/deepseek/DeepSeekVault.kt, mobile/android/app/src/main/java/com/listen2mobile/deepseek/DeepSeekKeyActivity.kt, mobile/android/app/src/main/java/com/listen2mobile/deepseek/DeepSeekModule.kt, mobile/android/app/src/test/java/com/listen2mobile/deepseek/DeepSeekContractTest.kt, mobile/android/app/src/test/java/com/listen2mobile/deepseek/DeepSeekPrivacyContractTest.kt</files>
  <behavior>
    - Configure encrypts with an Android Keystore AES-GCM key and random IV; status/test return no key/ciphertext/alias/exception material.
    - Keystore key creation, encryption, decryption, invalidation, or authentication failure returns `keystore-unavailable` or `corrupt-cleared`, writes no plaintext, and does not call DeepSeek.
    - Clear cancels/invalidates active operations, deletes ciphertext/IV and alias where appropriate, and returns not-configured without echoing the prior key.
    - Test-only vault injection uses an in-memory fake cryptographic boundary and cannot enable a production plaintext SharedPreferences path.
  </behavior>
  <action>Refactor vault outcomes into a sealed fail-closed contract per D-09. Remove every production fallback that writes/reads a raw key when Android Keystore is absent, and separate test fakes from production constructors so a null/unavailable secret mechanism cannot downgrade storage. Keep key entry in the native Activity; expose only configured/not-configured/keystore-unavailable/corrupt-cleared and bounded test results. Sanitize all caught errors, clear corrupt ciphertext atomically, bind cancellation generations to test/translation, and ensure clear removes usable key material. Adapt existing native fixtures to the sealed result, add marker-secret/Keystore-failure methods to the new privacy selector, and run only those new methods here.</action>
  <verify>
    <automated>JAVA_HOME=/opt/homebrew/opt/openjdk@17 ANDROID_HOME=/opt/homebrew/share/android-commandlinetools ANDROID_SDK_ROOT=/opt/homebrew/share/android-commandlinetools PATH=/opt/homebrew/opt/openjdk@17/bin:$PATH ./mobile/android/gradlew --offline --no-daemon -p mobile/android :app:testDebugUnitTest --tests 'com.listen2mobile.deepseek.DeepSeekPrivacyContractTest.vault*'</automated>
  </verify>
  <done>The complete key lifecycle works only behind Keystore, all unavailable/corrupt cases fail closed with safe status, and no key value crosses the UI/bridge/log/storage contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Validate and persist the full translation transaction natively</name>
  <files>mobile/android/app/src/main/java/com/listen2mobile/deepseek/DeepSeekClient.kt, mobile/android/app/src/main/java/com/listen2mobile/deepseek/DeepSeekPolicy.kt, mobile/android/app/src/main/java/com/listen2mobile/deepseek/DeepSeekTranslationCache.kt, mobile/android/app/src/main/java/com/listen2mobile/deepseek/DeepSeekModule.kt, mobile/android/app/src/test/java/com/listen2mobile/deepseek/DeepSeekContractTest.kt, mobile/android/app/src/test/java/com/listen2mobile/deepseek/DeepSeekPrivacyContractTest.kt</files>
  <behavior>
    - Native constructs the request from bounded title/artist/whole-lyric/revision input and keeps authorization, prompt, serialized provider body, and raw response inside native memory.
    - Exact schema, full line count, original order, timeline/line mapping, non-truncation/text completeness, size, and current revision must pass before private cache commit.
    - Missing/extra/reordered/misaligned/truncated/oversized/stale output, HTTP/provider error, cancellation, or key loss produces no cache record and no raw text in the result/error.
    - A valid cache hit is revision-addressed and returns only the validated translated-line projection required to render the current lyrics.
  </behavior>
  <action>Move provider request construction, raw response parsing, schema/alignment/completeness validation, revision comparison, and private cache commit into one native transaction. Per D-09, make the bridge result an exact bounded status/progress/error plus validated render projection; never return the prompt, serialized request, authorization, raw provider response, or validation exception. Make operation IDs one-use, cap line/text/body counts, cancel the native call when requested, and recheck vault state and lyric revision immediately before commit. Store validated translations only in the native no-backup cache with revision/model/prompt-schema identity and bounded growth/corruption recovery.</action>
  <verify>
    <automated>JAVA_HOME=/opt/homebrew/opt/openjdk@17 ANDROID_HOME=/opt/homebrew/share/android-commandlinetools ANDROID_SDK_ROOT=/opt/homebrew/share/android-commandlinetools PATH=/opt/homebrew/opt/openjdk@17/bin:$PATH ./mobile/android/gradlew --offline --no-daemon -p mobile/android :app:testDebugUnitTest --tests 'com.listen2mobile.deepseek.DeepSeekPrivacyContractTest.transaction*'</automated>
  </verify>
  <done>Only exact current aligned complete translations commit, every invalid/cancel/failure path is side-effect-free, and raw provider transaction material remains native-private.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Enforce complete consent and a persistence-safe JS projection</name>
  <files>mobile/src/deepseek/client.ts, mobile/src/deepseek/consent.ts, mobile/src/deepseek/types.ts, mobile/src/components/DeepSeekConsentSheet.tsx, mobile/src/screens/PlayerScreen.tsx, mobile/src/screens/SettingsScreen.tsx, mobile/src/deepseek/__tests__/client.test.ts, mobile/src/deepseek/__tests__/consent.test.ts, mobile/src/deepseek/__tests__/nativeWiring.test.js, mobile/src/screens/__tests__/playerTranslationBehavior.test.tsx, mobile/src/screens/__tests__/deepSeekFlow.test.tsx, mobile/src/screens/__tests__/deepSeekRevisionLifecycle.test.tsx, mobile/src/screens/__tests__/deepSeekSettingsStatus.test.tsx</files>
  <behavior>
    - Consent explicitly names current full lyrics, title, artist, possible provider cost, cancellation behavior, and failure/no-cache consequences before confirm is enabled.
    - Closing, rejecting, stale revision, or cancelling before confirmation makes zero native/network calls; in-flight cancellation invalidates the operation and returns no persisted result.
    - JS exact validation accepts only safe status/error/progress and validated line projection; Redux, AsyncStorage, navigation, debug snapshots, and logs contain no key, raw request body, or raw model response.
    - Key UI displays only configuration/test status and clear action; no code path requests or renders the stored key value.
    - Player track/lyric revision switch cancels the old operation, rejects its late callback, retains the current track's translation state, and never commits the old result.
    - Settings consumes only the status/test adapter; Keystore unavailable is rendered as disabled with no insecure continuation, and clear/configure/test refresh status without reading a key.
  </behavior>
  <action>Make the consent model bind one current track ID + semantic lyric revision to one native operation and render every AI-002 disclosure before confirm. In `PlayerScreen`, abort on dismiss, lyric revision change, track switch, unmount, or user cancel; fence callbacks by operation/track/revision so a late old result cannot render or persist for the new track. Adapt the existing DeepSeek client/consent/native-wiring and player translation-flow fixtures to the exact safe projection without running their complete suites in this task, then add a focused switch/cancel/revision selector. Validate safe response shapes and keep transaction fields out of JS types/global persistence. In Settings, consume only configure/test/clear/status adapter results, render Keystore unavailable as fail-closed, refresh status after each action, and add a focused status selector; no screen requests or echoes a stored key.</action>
  <verify>
    <automated>cd mobile &amp;&amp; npm test -- --runInBand src/screens/__tests__/deepSeekRevisionLifecycle.test.tsx src/screens/__tests__/deepSeekSettingsStatus.test.tsx</automated>
  </verify>
  <done>Consent is complete and cancellable, no-confirm produces no call, only valid current projections render, and JS-visible/persisted state contains no key or raw provider transaction.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Native key Activity/vault -> Keystore | User secret crosses into hardware/OS-backed key protection. |
| JS consent -> native transaction | User-approved lyric/title/artist input crosses into privileged network code. |
| DeepSeek response -> validator/cache/UI | Untrusted model output may contain malformed, injected, oversized, or sensitive content. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-07-04-01 | Information disclosure | Vault fallback/status | critical | mitigate | Keystore-only AES-GCM, no plaintext fallback/echo, sanitized sealed status. |
| T-07-04-02 | Spoofing/Repudiation | Consent operation | high | mitigate | One-use operation bound to lyric revision and explicit confirm; cancellation/no-confirm call counters. |
| T-07-04-03 | Tampering | Model response/cache | high | mitigate | Exact schema/line/order/timeline/completeness/revision validation before atomic private commit. |
| T-07-04-04 | Denial of service | Prompt/response/cache | medium | mitigate | Bounded lines/text/body/deadline/cancellation and cache growth/corruption recovery. |
| T-07-SC | Tampering | Package installs | high | mitigate | No new package install is required. |
</threat_model>

<verification>
Run only the newly added privacy, revision-lifecycle, and Settings-status selectors above. The complete source/JVM gate is 07-05; Phase 8 scans the integrated APK/backup/runtime logs per D-10.
</verification>

<success_criteria>
- Keystore absence/corruption never downgrades to plaintext or sends a request.
- Consent/no-call/cancel and full native validation matrices pass.
- Key, raw provider request, raw model response, and unvalidated full output are absent from all JS-visible/persisted/error contracts.
</success_criteria>

<output>
Create `.planning/phases/07-offline-advanced-playback/07-04-SUMMARY.md` when done.
</output>
