---
phase: 01-verified-bilibili-startup-slice
plan: "05"
type: execute
wave: 2
depends_on:
  - 01-02
files_modified:
  - android/app/build.gradle
  - android/app/src/main/AndroidManifest.xml
  - android/app/src/main/java/com/dazzlingwuming/listen2/MainActivity.java
  - android/app/src/main/java/com/dazzlingwuming/listen2/NavigationPolicy.java
  - android/app/src/test/java/com/dazzlingwuming/listen2/NavigationPolicyTest.java
  - android/app/src/androidTest/java/com/dazzlingwuming/listen2/Phase01WebViewInstrumentationTest.java
autonomous: true
requirements:
  - NET-001
  - NET-002
  - SEC-001
  - SEC-002
  - SEC-003
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "SEC-001: only normalized packaged `/assets/listen1/` HTTPS content stays inside the WebView; cross-origin, iframe, stale-page, and unsafe-scheme messages/navigation cannot reach current app state."
    - "Page start/reload and Activity/WebView destroy cancel native work and detach the one listener before the old page can post a late terminal result."
    - "SEC-002: debug and release-like runtime keep file/content/universal-file access, mixed content, geolocation, automatic windows, unnecessary multiple windows, third-party cookies, and cleartext provider traffic disabled."
    - "Approved external HTTP(S) navigation is handed to an Android browsable system intent without WebView cookies/headers or sensitive userinfo/query/fragment material; dangerous or untrusted targets remain blocked."
    - "Runtime policy is proven by pure-Java navigation fixtures plus an API-35 instrumentation handshake/settings/lifecycle test; source inspection or APK assembly alone is not counted as runtime evidence."
    - statement: "SEC-002 concurrent configuration ordering remains fail-closed: no correctness claim depends on parallel setting writes; the completed WebView must expose the entire hardened setting set atomically before loading appassets."
      verification: backstop
  artifacts:
    - path: "android/app/src/androidTest/java/com/dazzlingwuming/listen2/Phase01WebViewInstrumentationTest.java"
      provides: "No-new-library API-35 runtime assertions for appassets, bridge, WebView settings, navigation handoff, reload, and destroy"
    - path: "android/app/src/main/java/com/dazzlingwuming/listen2/NavigationPolicy.java"
      provides: "Normalized packaged URL decision and sanitized external-navigation decision"
      contains: "ExternalNavigationDecision"
    - path: "android/app/src/main/java/com/dazzlingwuming/listen2/MainActivity.java"
      provides: "Page lifecycle cancellation, one bridge install/destroy point, and secure external intent handoff"
  key_links:
    - from: "android/app/src/main/java/com/dazzlingwuming/listen2/MainActivity.java"
      to: "android/app/src/main/java/com/dazzlingwuming/listen2/AndroidHttpBridge.java"
      via: "install before load, notify page start, cancel/destroy before WebView destruction"
      pattern: "AndroidHttpBridge"
    - from: "android/app/src/main/java/com/dazzlingwuming/listen2/MainActivity.java"
      to: "android/app/src/main/java/com/dazzlingwuming/listen2/NavigationPolicy.java"
      via: "every WebView navigation receives a packaged/external/blocked decision"
      pattern: "ExternalNavigationDecision"
  assumptions:
    - id: "AVD-01"
      statement: "A supported API-35 emulator is required for the connected instrumentation task; if unavailable, the plan is blocked and its runtime requirement remains pending."
      status: unresolved
      flagged: true
---

## Phase Goal

**As a** Listen2 Android user, **I want to** open the phone home and complete a safe Bilibili search → selected part → foreground audio → primary-lyrics-entry journey, **so that** I can reliably start listening on my phone within my actual authorization.

<objective>
Harden and runtime-test the Android WebView/origin/navigation/lifecycle boundary that hosts the typed Bilibili slice.

Purpose: Prove that secure source settings survive actual API-35 execution and that page/reload/destroy/navigation cannot escape or outlive the packaged surface.
Output: Exact navigation decisions, page-lifecycle bridge cancellation, hardened build/runtime configuration, and a dependency-free instrumentation test.
</objective>

<execution_context>
@/Users/fluenteng/.codex/gsd-core/workflows/execute-plan.md
@/Users/fluenteng/.codex/gsd-core/templates/summary.md
</execution_context>

<execution_gate>
Before every executor commit or push, invoke the user-level `$run-local-ci` skill against the exact changed worktree and HEAD intended for publication. Deterministic JS/JVM tests, Android builds, instrumentation, and emulator evidence complement but never replace this gate. A failure, timeout, missing required tool/dependency, absent authoritative CI command, or result that does not cover the exact changed worktree blocks that commit/push; do not reduce the command set or publish an unverified change.
</execution_gate>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-verified-bilibili-startup-slice/01-CONTEXT.md
@.planning/phases/01-verified-bilibili-startup-slice/01-RESEARCH.md
@.planning/phases/01-verified-bilibili-startup-slice/01-PATTERNS.md
@.planning/phases/01-verified-bilibili-startup-slice/01-VALIDATION.md
@.planning/phases/01-verified-bilibili-startup-slice/01-UI-SPEC.md
@.planning/phases/01-verified-bilibili-startup-slice/01-02-SUMMARY.md
@android/app/src/main/java/com/dazzlingwuming/listen2/MainActivity.java
@android/app/src/main/java/com/dazzlingwuming/listen2/NavigationPolicy.java
@android/app/src/main/AndroidManifest.xml
@android/app/build.gradle
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make packaged, external, blocked, reload, and destroy paths exact</name>
  <read_first>
    - `01-CONTEXT.md` D-02, D-03, D-11, D-13, D-14
    - `01-UI-SPEC.md` navigation/system-back table and safe external-navigation behavior
    - `MainActivity.java` secure settings, bridge install, client callbacks, back, and destroy
    - `NavigationPolicy.java` current prefix/external decisions and `NavigationPolicyTest.java`
    - Registry-aware lifecycle hooks recorded in `01-02-SUMMARY.md`
  </read_first>
  <files>android/app/src/main/java/com/dazzlingwuming/listen2/MainActivity.java, android/app/src/main/java/com/dazzlingwuming/listen2/NavigationPolicy.java, android/app/src/test/java/com/dazzlingwuming/listen2/NavigationPolicyTest.java, android/app/src/main/AndroidManifest.xml</files>
  <behavior>
    - Exact normalized appassets `/assets/listen1/` paths and safe hash navigation stay in WebView; wrong host/port/userinfo, encoded traversal, adjacent prefix, query-bearing asset, and file/content/javascript/intent/custom schemes are blocked.
    - External HTTP(S) handoff requires normal ports, no userinfo, no sensitive token/cookie/signature query names, and a resolvable browsable system intent; the WebView never loads it.
    - Page start/reload cancels all old-generation native requests and resets current epoch acceptance; page commit hides the native loading overlay only for the packaged page.
    - Back uses the most local safe WebView/app history and loading cancellation path; it does not stop foreground audio.
    - Destroy cancels registry work, removes the listener, detaches clients, stops loading, clears/destroys the WebView, and tolerates late callbacks without reply/UI mutation.
    - Manifest retains only INTERNET permission, no backup, and cleartext disabled.
  </behavior>
  <action>
Replace boolean-only navigation decisions with a pure `NavigationPolicy.ExternalNavigationDecision` (packaged, external with sanitized URI, blocked) while keeping compatibility helpers if tests/callers need them. Normalize path segments and reject encoded traversal, userinfo, nonstandard ports, query-bearing packaged assets, untrusted appassets paths, and dangerous schemes. For external HTTP(S), return a sanitized decision only when the URI is normal-port, browsable, and contains no known secret-bearing userinfo/query/fragment keys; do not attach WebView headers, cookie extras, referrer extras, or provider state to the intent.

In `MainActivity.PackagedUiClient`, call the bridge page-start hook before a packaged reload can reuse old requests, and only dismiss the native loading surface after the packaged commit. Use the decision object for system handoff/blocking. Harden teardown order: stop loading, cancel/destroy bridge, detach clients, clear WebView state needed to suppress callbacks, remove from parent, then destroy. Keep secure settings applied before listener installation/page load in both build types and retain the manifest's no-backup/cleartext boundary.

Write `NavigationPolicyTest` cases first for exact/adjacent/traversal/port/userinfo/sensitive-query/scheme targets and lifecycle decision data. Ensure errors/blocked inputs are not logged or displayed with their raw URI.
  </action>
  <acceptance_criteria>
    - Packaged appassets path checks are normalization-safe and exact; unsafe/adjacent inputs have explicit JVM cases.
    - Approved external navigation leaves the WebView through a sanitized browsable intent and contains no attached WebView credentials or secret-bearing URI material.
    - Reload/page start invalidates old native work; destroy removes listener/work before WebView destruction and late callbacks are inert.
    - Manifest and code keep all SEC-002 hardening values in debug and release source paths with no build-type override.
  </acceptance_criteria>
  <verify>
    <automated>(cd android &amp;&amp; gradle --no-daemon :app:testDebugUnitTest --tests com.dazzlingwuming.listen2.NavigationPolicyTest --tests com.dazzlingwuming.listen2.HttpBridgePolicyTest :app:assembleDebug :app:assembleRelease)</automated>
  </verify>
  <done>The host admits only normalized packaged content, sanitizes external system handoff, blocks unsafe navigation, invalidates old page work, tears down safely, and builds with the hardened manifest/settings in debug and release-like variants.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove the real API-35 WebView settings, origin handshake, navigation, and lifecycle</name>
  <precondition>An API-35 emulator is booted, `adb devices` reports it as `device`, and its WebView provider is installed and enabled.</precondition>
  <read_first>
    - `01-VALIDATION.md` runtime safety row and Wave-0 instrumentation/ADB contract
    - `01-01-SUMMARY.md` completed API-35 packaged-page tracer and `--wave0-verify` evidence identity
    - `MainActivity.java` completed Task-1 settings, client, and teardown
    - `AndroidHttpBridge.java` exact listener object/origin rules and typed request handshake
    - `android/app/build.gradle` existing instrumentation-runner declaration and dependency set
  </read_first>
  <files>android/app/build.gradle, android/app/src/androidTest/java/com/dazzlingwuming/listen2/Phase01WebViewInstrumentationTest.java, android/app/src/main/java/com/dazzlingwuming/listen2/MainActivity.java</files>
  <behavior>
    - Instrumentation launches `MainActivity` on API 35 and waits for exact appassets commit plus local shell marker.
    - On the UI thread, actual WebSettings report file/content/universal-file access false, mixed content never, geolocation false, automatic/multiple windows false, media user gesture true, safe browsing true, and third-party cookies false.
    - Page JavaScript sees one `Listen2AndroidHttp` object only at the packaged main frame and completes one bounded typed invalid-input handshake without network.
    - Representative unsafe navigation remains in the app/blocked, approved external HTTPS emits a sanitized browsable intent, and appassets adjacent/traversal does not commit.
    - Reload/destroy during an in-flight fixture request clears the old request and produces no second reply or crash.
  </behavior>
  <action>
Use the platform `android.test.InstrumentationTestRunner` and platform instrumentation APIs already present in the SDK so this plan adds no external testing library. Expand the Wave-0 `Phase01WebViewInstrumentationTest` in place: retain its packaged-page/typed-terminal tracer, then add view-hierarchy lookup, main-thread settings inspection, constant JavaScript origin probes, external-intent monitoring, unsafe navigation, and reload/destroy assertions around deterministic invalid/local bridge messages. Always restore/finish Activity state. Do not call the live Bilibili network in this test; Plan 01-07 owns that gate.

Configure the test runner accordingly without changing production dependencies. Ensure every latch has a finite timeout and failures identify only step/status, API, and bounded request identity. First run must fail before the runtime hooks/assertions exist, then pass on the supported emulator.
  </action>
  <acceptance_criteria>
    - `connectedDebugAndroidTest` runs the real Activity/WebView on API 35 without downloading or declaring a new test package.
    - Assertions read actual runtime settings and origin object; they do not infer security from source constants.
    - Unsafe navigation, external handoff, reload, and destroy each have runtime assertions with bounded timeouts.
    - Test output contains no URL query values, cookie/header values, signed candidates, provider body, token, or raw exception text.
  </acceptance_criteria>
  <verify>
    <automated>(cd android &amp;&amp; gradle --no-daemon :app:connectedDebugAndroidTest)</automated>
  </verify>
  <done>The exact debug APK host passes real API-35 WebView setting, appassets/bridge, navigation, reload, and teardown instrumentation, with the AVD/WebView identity retained for final evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Android intent/WebView navigation | Page/user URLs choose in-app, external, or blocked handling. |
| Activity/page lifecycle → native registry | Reload/destroy changes which page and replies are current. |
| Build configuration → runtime WebView | Source/manifest settings must survive the installed APK and API-35 provider. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-17 | Spoofing / Elevation | Packaged URL/origin | high | mitigate | Normalized exact host/path/port/source/main-frame policy plus JVM and instrumentation negatives. |
| T-01-18 | Information Disclosure | External navigation | high | mitigate | Sanitized browsable intent, no headers/cookies/extras/sensitive URI fields, runtime intent monitor. |
| T-01-19 | Tampering | Reload/destroy race | high | mitigate | Cancel old generation, remove listener before WebView destroy, bounded runtime race test. |
| T-01-20 | Elevation / Disclosure | WebView settings/build drift | high | mitigate | Harden before load in shared code path, no cleartext/backup, inspect actual settings on API 35 and assemble release-like APK. |

ASVS L1 blocking rule: every high-severity host/runtime threat is blocking. Neither source assertions nor assembled APKs substitute for the connected API-35 test; any setting/origin/navigation/lifecycle mismatch leaves the plan incomplete.
</threat_model>

## Artifacts this phase produces

- Expanded path `android/app/src/androidTest/java/com/dazzlingwuming/listen2/Phase01WebViewInstrumentationTest.java`, retaining the Wave-0 tracer and adding settings/navigation/lifecycle cases.
- New `NavigationPolicy.ExternalNavigationDecision` and normalization/sensitive-URI validation symbols.
- New `AndroidHttpBridge` page-start/current-generation lifecycle hook consumed by `MainActivity`.
- New `MainActivity` secure navigation decision, reload cancellation, and ordered teardown behavior.
- Expanded platform instrumentation-runner configuration in `android/app/build.gradle` with no external dependency or second runner.
- Expanded `NavigationPolicyTest` exact/adjacent/traversal/secret/scheme cases.

<verification>
Run both task commands on the same API-35 AVD. Record the connected-test report path, emulator/API/ABI/WebView version, debug/release-like APK paths, and any unverified runtime surface for Plan 01-07. Static/JVM/build checks must be green, but do not mark live Bilibili, audio, layout, or lyric behavior from this plan.
</verification>

<success_criteria>
- SEC-001/002/003 host and navigation controls exist at the correct native boundary and survive the installed runtime.
- Old pages cannot retain request authority across reload/destroy.
- External navigation does not carry app credentials or raw sensitive data.
- API-35 connected instrumentation is green; otherwise AVD-01 remains a blocking unresolved assumption.
- The SEC-002 concurrency backstop remains structured and cannot silently pass without verifier evidence.
</success_criteria>

<output>
Create `.planning/phases/01-verified-bilibili-startup-slice/01-05-SUMMARY.md` when done.
</output>
