---
phase: 01-verified-bilibili-startup-slice
plan: "07"
type: execute
wave: 5
depends_on:
  - 01-04
  - 01-05
  - 01-06
files_modified:
  - android/scripts/phase01-api35-smoke.sh
  - android/scripts/phase01-webview-smoke.mjs
  - android/evidence/phase01/README.md
  - android/README.md
  - .planning/phases/01-verified-bilibili-startup-slice/01-API35-EVIDENCE.md
  - .planning/phases/01-verified-bilibili-startup-slice/evidence/01-home.png
  - .planning/phases/01-verified-bilibili-startup-slice/evidence/01-results.png
  - .planning/phases/01-verified-bilibili-startup-slice/evidence/01-part.png
  - .planning/phases/01-verified-bilibili-startup-slice/evidence/01-playing-lyrics.png
autonomous: true
requirements:
  - NET-001
  - NET-002
  - NET-003
  - SRCH-001
  - SRCH-002
  - SRCH-003
  - SEC-001
  - SEC-002
  - SEC-003
estimate:
  tokens: 43000
  raw_tokens: 43000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "D-15: one exact debug APK on a timestamped supported API-35 emulator completes cold local shell → live Bilibili search/cancel/resubmit → current labelled results → exact part → foreground AudioFlinger and UI progress beyond `0:00` → pause/resume → truthful primary lyric state."
    - "The same installed build proves retry/finalization under a network failure, old-epoch suppression, detail/back/re-entry without duplicate rows, and external/unsafe navigation safety."
    - "At 320px, portrait/landscape, 200% font scale, keyboard open, and gesture navigation, essential content is reachable, has no horizontal page overflow/obscured fixed bands, and primary controls measure at least 48dp."
    - "D-16: evidence records timestamp/timezone, git SHA, APK SHA-256, build variant, AVD/image/API/ABI/WebView, network, exact commands, timings, selected public BVID/CID/part, screenshots, sanitized log observations, pass/fail, uncovered behavior, and recovery path."
    - "No deterministic fixture, HTTP status, descriptor, build, signature check, instrumentation result, or CI output can substitute for the live provider/audio gate."
    - "If AVD, live provider, CDN, codec, or network is unavailable, evidence says `BLOCKED`/`not verified` and Phase 1 remains incomplete; no result is fabricated or downgraded to success."
  artifacts:
    - path: "android/scripts/phase01-api35-smoke.sh"
      provides: "Reproducible AVD preparation, APK identity/install, ADB/connected-test orchestration, environment capture, cleanup, and hard gate"
    - path: "android/scripts/phase01-webview-smoke.mjs"
      provides: "Dependency-free Chrome DevTools Protocol phone-flow driver and DOM/runtime assertions"
    - path: "android/evidence/phase01/README.md"
      provides: "Evidence schema, redaction rules, commands, failure states, and recovery procedure"
    - path: ".planning/phases/01-verified-bilibili-startup-slice/01-API35-EVIDENCE.md"
      provides: "Timestamped result tied to exact APK/git/emulator/provider part"
    - path: ".planning/phases/01-verified-bilibili-startup-slice/evidence/01-playing-lyrics.png"
      provides: "Redacted runtime proof of current foreground progress and truthful lyric state"
  key_links:
    - from: "android/scripts/phase01-api35-smoke.sh"
      to: "android/scripts/phase01-webview-smoke.mjs"
      via: "ADB launch/port-forward/environment invokes CDP driver against exact installed APK"
      pattern: "phase01-webview-smoke"
    - from: "android/scripts/phase01-api35-smoke.sh"
      to: ".planning/phases/01-verified-bilibili-startup-slice/01-API35-EVIDENCE.md"
      via: "fail-closed result writer records exact hashes/environment/steps without secrets"
      pattern: "01-API35-EVIDENCE"
    - from: ".planning/phases/01-verified-bilibili-startup-slice/01-API35-EVIDENCE.md"
      to: ".planning/phases/01-verified-bilibili-startup-slice/evidence/01-playing-lyrics.png"
      via: "evidence table links redacted screenshots for observable steps"
      pattern: "01-playing-lyrics\.png"
  assumptions:
    - id: "AVD-01"
      statement: "A supported API-35 system image can be installed/started and exposes a compatible WebView provider."
      status: unresolved
      flagged: true
    - id: "LIVE-01"
      statement: "A public anonymous Bilibili item remains permitted and available through the current API/CDN/codec at execution time."
      status: unresolved
      flagged: true
---

## Phase Goal

**As a** Listen2 Android user, **I want to** open the phone home and complete a safe Bilibili search → selected part → foreground audio → primary-lyrics-entry journey, **so that** I can reliably start listening on my phone within my actual authorization.

<objective>
Create and run the non-substitutable API-35 emulator gate against the exact APK, then retain a redacted, reproducible record proving or blocking the complete Phase-1 journey.

Purpose: Establish device truth for WebView, live provider, codec/CDN, audible foreground progress, phone layout, lifecycle, cancellation, and navigation.
Output: Reusable ADB/CDP harness, evidence contract, exact screenshots, and a pass-or-blocked evidence record.
</objective>

<execution_context>
@/Users/fluenteng/.codex/gsd-core/workflows/execute-plan.md
@/Users/fluenteng/.codex/gsd-core/templates/summary.md
</execution_context>

<execution_gate>
Before every executor commit or push, invoke the user-level `$run-local-ci` skill against the exact changed worktree and HEAD intended for publication. Deterministic JS/JVM tests, Android builds, instrumentation, and emulator evidence complement but never replace this gate. A failure, timeout, missing required tool/dependency, absent authoritative CI command, or result that does not cover the exact changed worktree blocks that commit/push; do not reduce the command set or publish an unverified change.
</execution_gate>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-verified-bilibili-startup-slice/01-CONTEXT.md
@.planning/phases/01-verified-bilibili-startup-slice/01-VALIDATION.md
@.planning/phases/01-verified-bilibili-startup-slice/01-UI-SPEC.md
@.planning/phases/01-verified-bilibili-startup-slice/COVERAGE.md
@.planning/phases/01-verified-bilibili-startup-slice/01-04-SUMMARY.md
@.planning/phases/01-verified-bilibili-startup-slice/01-05-SUMMARY.md
@.planning/phases/01-verified-bilibili-startup-slice/01-06-SUMMARY.md
@android/README.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build a fail-closed API-35 ADB/CDP harness and redacted evidence contract</name>
  <read_first>
    - `01-CONTEXT.md` D-14, D-15, D-16
    - `01-VALIDATION.md` Wave-0 harness/evidence gaps and both manual-only verification rows
    - `01-UI-SPEC.md` eight emulator-verifiable acceptance criteria
    - `Phase01WebViewInstrumentationTest` command/report from `01-05-SUMMARY.md`
    - Repository-authoritative Android build/install/signature commands in `android/README.md`
  </read_first>
  <files>android/scripts/phase01-api35-smoke.sh, android/scripts/phase01-webview-smoke.mjs, android/evidence/phase01/README.md, android/README.md</files>
  <behavior>
    - `--prepare` selects/creates a repository-named API-35 AVD from an installed compatible image, or installs the exact official system image when tooling/network permits, then waits boundedly for boot and WebView readiness.
    - The harness builds/tests the APK, computes git/APK SHA-256 before install, installs that exact file, clears app state, cold launches, captures environment, and refuses to continue if any identity changes.
    - A Node-24 dependency-free CDP client discovers only the debug app WebView through an ADB local forward, drives constant selectors/actions, evaluates bounded DOM state, and never prints page network/request/descriptor objects.
    - Every command has a timeout/nonzero failure, trap-based device-setting/forward cleanup, and explicit `PASS`, `FAIL`, `BLOCKED`, or `not verified`; missing live prerequisites never become pass.
    - Evidence writer allow-lists fields and rejects/redacts URL query values, signed media candidates, headers, cookies, credentials, raw provider bodies, arbitrary exceptions, and personal paths.
  </behavior>
  <action>
Expand the Wave-0 `phase01-api35-smoke.sh` in place as the single orchestrator, retaining `--wave0-verify`. Resolve JDK/SDK/Gradle from documented environment, select API 35 and host-compatible ABI, create/start a dedicated AVD when needed, wait with bounded polling, record image/API/ABI/WebView/network, run deterministic suites and connected instrumentation, assemble/signature-verify debug APK, compute hashes, install/clear/cold-launch, enable/forward the debug WebView socket, invoke the Node driver, capture approved screenshots/observations, and restore font scale, rotation, window size, network, port forward, and emulator ownership on exit. Refuse multiple ambiguous devices unless the script selected one explicitly.

Create dependency-free `phase01-webview-smoke.mjs` using Node built-in fetch/WebSocket and the Chrome DevTools Protocol. Discover the packaged appassets page, wait for fixed UI selectors, issue real click/type/back/rotation-state actions through CDP/ADB-coordinated callbacks, and return a small typed result object containing step, state, bounded identifiers/timings, dimensions, current progress, and pass/fail only. Never evaluate or serialize bridge pending maps, cookies, headers, candidate URLs, provider bodies, or storage dumps.

Document the exact invocation, environment prerequisites, evidence schema, redaction allow-list/deny-list, live-vs-fixture distinction, cleanup, rerun, and external-blocker behavior in `android/evidence/phase01/README.md` and link it from `android/README.md`. Add a harness self-test/dry-run mode using synthetic CDP responses and a redaction canary so implementation begins red and proves output rejects forbidden fields before live use.
  </action>
  <acceptance_criteria>
    - Dry-run/self-test proves AVD/device ambiguity, command timeout, build/hash drift, CDP discovery failure, step failure, cleanup, and redaction canary each exit nonzero or record the correct blocked state.
    - Harness invokes the repository's actual deterministic/connected/build/signature commands and never suppresses their errors.
    - CDP output schema contains only approved step/state/timing/dimension/bounded-id fields and cannot emit raw network/storage/page objects.
    - Documentation distinguishes deterministic, instrumentation, and live gates and says only the live gate can complete D-15.
  </acceptance_criteria>
  <verify>
    <automated>bash android/scripts/phase01-api35-smoke.sh --self-test &amp;&amp; node android/scripts/phase01-webview-smoke.mjs --self-test</automated>
  </verify>
  <done>The repository has a dependency-free, bounded, cleanup-safe, redaction-tested API-35 harness that can prepare the emulator, tie an exact APK to runtime steps, and fail closed on every missing prerequisite or failed assertion.</done>
</task>

<task type="auto">
  <name>Task 2: Run the exact live Bilibili phone journey and capture observable device evidence</name>
  <precondition>`bash android/scripts/phase01-api35-smoke.sh --prepare` succeeds and reports one ready API-35 AVD with a supported WebView provider and network.</precondition>
  <read_first>
    - `01-CONTEXT.md` D-05, D-06, D-09, D-10, D-15, D-16
    - `01-UI-SPEC.md` exact copy, navigation, phone geometry, playback, lyric, and emulator criteria
    - Completed harness contract from Task 1
    - Current deterministic/connected reports from Plans 01-01 through 01-06
  </read_first>
  <files>.planning/phases/01-verified-bilibili-startup-slice/01-API35-EVIDENCE.md, .planning/phases/01-verified-bilibili-startup-slice/evidence/01-home.png, .planning/phases/01-verified-bilibili-startup-slice/evidence/01-results.png, .planning/phases/01-verified-bilibili-startup-slice/evidence/01-part.png, .planning/phases/01-verified-bilibili-startup-slice/evidence/01-playing-lyrics.png</files>
  <action>
Run the harness against the exact freshly built/hashed debug APK and one public anonymous Bilibili query/item selected at execution time. Cold launch after clearing app data and measure native launch to local shell, first current result, selected part readiness, user-gesture playback start, and forward progress. Capture the local shell before remote completion; submit then immediately cancel one search, revise/resubmit, and prove only the current labelled rows remain. Open one supported result, capture its API-order parts, select an exact CID, return/re-enter/rotate without duplicate rows, and exercise an invalid explicit-CID fixture/runtime hook that yields the approved error instead of another part.

Start the selected permitted part by a real input gesture. Require both UI/Howler current position greater than `0:00` and an active app audio track in sanitized AudioFlinger observations; descriptor/URL/load/`onplay` is insufficient. Pause, assert no forward movement, resume, assert forward movement again, then open primary lyrics and capture one truthful current loading/content/unavailable/error terminal state while audio remains valid. Keep Media3/background expectations absent.

Exercise current cancellation/retry and safety: force one bounded offline network failure, confirm spinner finalizes/valid rows persist/one retry recovers after network restore; navigate back from loading/detail/lyrics and prove old replies/current audio behave correctly; run connected unsafe/external navigation assertions on the same APK. Repeat at 320px portrait, landscape, font scale 2.0, keyboard open, and gesture navigation, measuring viewport overflow/fixed-band occlusion and primary target rectangles. Write only allow-listed environment, selected BVID/CID/part, state/timing/dimension/audio-active observations, and redacted screenshots to the stable evidence paths.
  </action>
  <acceptance_criteria>
    - Evidence ties one git SHA and APK SHA-256 to the installed package, API-35 image/ABI/WebView/network, full command list, and timestamp/timezone.
    - Live current search can cancel/revise/resubmit without stale overwrite; result/detail/part fields and exact CID behavior are visible and correct.
    - Playback has both active-audio observation and current forward progress beyond zero; pause/resume and current lyric entry pass on the same part/build.
    - Offline/retry, back/teardown, external/unsafe navigation, 320px, rotation, 200% text, keyboard, gesture-safe area, no horizontal overflow, and 48dp target checks pass or remain explicit failed blockers.
    - Screenshots/evidence contain no signed URL, raw URL query, header, cookie, token, provider body, raw exception, credential, or personal absolute path.
  </acceptance_criteria>
  <verify>
    <automated>bash android/scripts/phase01-api35-smoke.sh --run --evidence .planning/phases/01-verified-bilibili-startup-slice/01-API35-EVIDENCE.md</automated>
  </verify>
  <done>The exact installed API-35 build has a timestamped pass record for the complete live public Bilibili phone journey and safety/layout checks; otherwise the evidence truthfully records a blocking failure and Phase 1 remains incomplete.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Fail closed on incomplete, mismatched, substituted, or secret-bearing evidence</name>
  <read_first>
    - `01-VALIDATION.md` validation sign-off and non-substitution rule
    - `COVERAGE.md` all nine requirement/source/edge/prohibition mappings
    - Evidence schema and generated record from Tasks 1–2
    - Git diff and deterministic/connected test reports for the exact recorded SHA/worktree
  </read_first>
  <files>android/scripts/phase01-api35-smoke.sh, android/evidence/phase01/README.md, .planning/phases/01-verified-bilibili-startup-slice/01-API35-EVIDENCE.md</files>
  <behavior>
    - Verification rejects missing required fields/steps/screenshots, non-API35 device, mismatched git/APK hash, wrong package/build, stale timestamp, nonterminal step, or any failed/not-verified required step.
    - Verification rejects a record that cites fixture/JVM/build/instrumentation output in place of live provider/audio progress.
    - Verification rejects forbidden secret/URL/header/cookie/provider-body/raw-exception/personal-path patterns in evidence and linked screenshot metadata.
    - Verification permits explicit external `BLOCKED` evidence but returns nonzero and leaves requirement/phase status incomplete.
    - All source-audit, edge backstop, unresolved assumption, descriptor-less prohibition, ASVS high threat, and API coverage items remain visible for final verifier review.
  </behavior>
  <action>
Add `--verify-evidence` to the harness. Parse the generated Markdown through a bounded, exact-section validator and independently recompute current git/APK hashes, API/package/WebView identity, required step terminals, screenshot existence, and redaction scan. Require the live markers for current provider search, exact BVID/CID selection, AudioFlinger-active plus progress greater than zero, pause/resume, lyric terminal state, cancellation/retry, navigation, and phone geometry. A `BLOCKED` record is honest output but verification must exit nonzero.

Add self-tests with known-bad evidence fixtures generated in a temporary directory for missing live marker, substituted fixture, stale/mismatched hashes, failed required step, absent screenshot, secret/query/cookie/signed-candidate/personal-path canaries, and a clean control. Do not commit raw logs or bad fixtures. Update the evidence README with the verifier command and recovery path. Run the full deterministic suite, connected test, live harness, evidence verifier, and secret/cleartext/config scans on the exact final content; if any source or APK changes afterward, recompute/rerun rather than reusing the record.
  </action>
  <acceptance_criteria>
    - Every known-bad evidence self-test fails and the clean exact live record passes non-vacuously.
    - Git/APK/device/step identity is recomputed, not trusted from the record alone.
    - Required live markers cannot be satisfied by deterministic/instrumentation/build results.
    - External provider/device failure produces a nonzero blocked result and cannot mark any Phase-1 requirement complete.
    - Final evidence and screenshots pass the explicit redaction scan and retain unresolved assumptions/backstops/prohibitions for honest verifier disposition.
  </acceptance_criteria>
  <verify>
    <automated>bash android/scripts/phase01-api35-smoke.sh --self-test &amp;&amp; bash android/scripts/phase01-api35-smoke.sh --verify-evidence .planning/phases/01-verified-bilibili-startup-slice/01-API35-EVIDENCE.md &amp;&amp; npm --prefix app/listen1_chrome_extension test &amp;&amp; (cd android &amp;&amp; gradle --no-daemon :app:testDebugUnitTest :app:connectedDebugAndroidTest :app:assembleDebug :app:assembleRelease)</automated>
  </verify>
  <done>The final exact worktree has green deterministic, runtime, live, and evidence-integrity gates with no secret-bearing artifact, or the phase remains explicitly blocked and incomplete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Host scripts/CDP/ADB → emulator app | Automation controls a real runtime and may observe sensitive transient state. |
| Emulator/provider observations → repository evidence | Runtime output is filtered into durable review artifacts. |
| Evidence record → phase completion | A report can falsely or stalely claim the live gate passed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-25 | Spoofing / Repudiation | Build/device/evidence identity | high | mitigate | Recompute git/APK/API/package/WebView identity, require current timestamp/steps, known-bad verifier fixtures. |
| T-01-26 | Information Disclosure | CDP/ADB/evidence output | high | mitigate | Allow-listed result schema, redaction canaries/scans, bounded sanitized observations, no raw logs/storage/network objects. |
| T-01-27 | Tampering | Live-gate substitution | high | mitigate | Separate live markers and reject fixture/build/instrumentation substitution; external blocker exits nonzero. |
| T-01-28 | Denial of Service | AVD/network/device settings | medium | mitigate | Bounded startup/commands, one selected device, trap cleanup/restore, explicit blocked recovery path. |

ASVS L1 blocking rule: identity spoofing, evidence disclosure, or live-gate substitution is high severity and blocks Phase 1. The evidence validator must fail first on each known-bad fixture and pass the exact clean live record; no manual prose override can turn a failed high threat green.
</threat_model>

## Artifacts this phase produces

- Extended executable path `android/scripts/phase01-api35-smoke.sh` retaining `--wave0-verify` and adding `--self-test`, `--prepare`, `--run`, and `--verify-evidence` modes.
- New path `android/scripts/phase01-webview-smoke.mjs` with dependency-free CDP client/self-test and bounded result schema.
- Extended path `android/evidence/phase01/README.md` and linked Android README commands.
- New exact evidence path `.planning/phases/01-verified-bilibili-startup-slice/01-API35-EVIDENCE.md`.
- New redacted screenshot paths `evidence/01-home.png`, `01-results.png`, `01-part.png`, and `01-playing-lyrics.png` under the phase directory.

<verification>
Run all three task commands against one exact final APK/worktree. Confirm the evidence verifier independently recomputes identity and requires live audio/progress markers. Then invoke the repository `run-local-ci` gate before any commit/push; local CI remains distinct from this emulator gate and cannot replace it.
</verification>

<success_criteria>
- All nine Phase-1 requirements have deterministic plus applicable connected/live evidence on one exact API-35 build.
- The user journey reaches current labelled search, exact selected part, active foreground audio and progress beyond zero, pause/resume, and truthful lyrics.
- Cancellation/retry/stale/back/navigation/security/phone-layout states pass on the same build.
- Evidence is complete, redacted, reproducible, and non-substitutable; any external blocker leaves the phase incomplete.
- No deferred Media3, full lyrics, persistent auth, cache/download, MV/effects/AI capability is claimed or exposed by this evidence.
</success_criteria>

<output>
Create `.planning/phases/01-verified-bilibili-startup-slice/01-07-SUMMARY.md` when done.
</output>
