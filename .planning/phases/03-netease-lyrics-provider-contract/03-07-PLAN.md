---
phase: 03-netease-lyrics-provider-contract
plan: "07"
type: execute
wave: 5
depends_on: [03-06]
files_modified:
  - android/app/src/androidTest/java/com/dazzlingwuming/listen2/Phase03ProviderInstrumentationTest.java
  - android/app/src/androidTest/java/com/dazzlingwuming/listen2/Phase03LyricInstrumentationTest.java
  - android/scripts/phase03-api35-smoke.sh
  - android/scripts/phase03-webview-smoke.mjs
  - .planning/phases/03-netease-lyrics-provider-contract/03-API35-DETERMINISTIC-EVIDENCE.md
  - .planning/phases/03-netease-lyrics-provider-contract/evidence/03-deterministic-result.json
  - .planning/phases/03-netease-lyrics-provider-contract/evidence/03-netease.png
  - .planning/phases/03-netease-lyrics-provider-contract/evidence/03-lyrics.png
  - .planning/phases/03-netease-lyrics-provider-contract/evidence/03-picker.png
autonomous: true
requirements: [NET-004, LYR-001, LYR-002, LYR-003]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The exact API-35 installed debug APK proves the packaged WebMessage, typed NetEase fixture, sole Media3 owner, advancing position, current lyric identity/clock, Room selection/offset, and phone UI as one deterministic path."
    - "Pause, seek, track change, rotation, renderer recreation, cancellation, stale reply, manual clear, and recovery retain current identity and never expose transport or disable valid audio."
    - "Deterministic evidence binds git/APK/device/WebView identity and explicitly states that it cannot complete NET-004 without the separate authorized live gate."
  artifacts:
    - path: "android/scripts/phase03-api35-smoke.sh"
      provides: "Fail-closed deterministic API-35 runner/verifier"
    - path: ".planning/phases/03-netease-lyrics-provider-contract/03-API35-DETERMINISTIC-EVIDENCE.md"
      provides: "Redacted exact-build deterministic evidence and uncovered live gate"
    - path: "android/app/src/androidTest/java/com/dazzlingwuming/listen2/Phase03LyricInstrumentationTest.java"
      provides: "Installed lifecycle, native-clock, Room, accessibility semantic proof"
  key_links:
    - from: "android/scripts/phase03-api35-smoke.sh"
      to: "android/app/src/androidTest/java/com/dazzlingwuming/listen2/Phase03ProviderInstrumentationTest.java"
      via: "exact serial/package/class instrumentation execution"
      pattern: "Phase03ProviderInstrumentationTest"
    - from: "android/scripts/phase03-webview-smoke.mjs"
      to: ".planning/phases/03-netease-lyrics-provider-contract/evidence/03-deterministic-result.json"
      via: "bounded CDP observations and redacted screenshot/result emission"
      pattern: "deterministic-result|screenshots"
---

<objective>
Prove the complete deterministic Phase 3 architecture on an exact API-35 installed APK while preserving the separate live-provider completion gate (D-05, D-06, D-07, D-08, D-09, D-10, D-14, D-15, D-16, D-17, D-18, D-19, D-20).

Purpose: Catch installed WebView/Media3/Room/UI/lifecycle failures before the external NetEase route is available, with fail-closed identity and redaction evidence.
Output: Two focused instrumentation classes, deterministic ADB/CDP harness, screenshots/result JSON, and a reproducible evidence record labelled deterministic-only.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-netease-lyrics-provider-contract/03-CONTEXT.md
@.planning/phases/03-netease-lyrics-provider-contract/03-UI-SPEC.md
@.planning/phases/03-netease-lyrics-provider-contract/03-VALIDATION.md
@.planning/phases/03-netease-lyrics-provider-contract/03-06-SUMMARY.md
@.planning/phases/01-verified-bilibili-startup-slice/01-API35-EVIDENCE.md
@.planning/phases/02-native-media3-playback-background-control/02-API35-EVIDENCE.md
@android/scripts/phase02-api35-smoke.sh

<interfaces>
- Reuse Phase 1's exact appassets/bridge instrumentation and Phase 2's installed service, process-death, identity, screenshot, and evidence-verifier conventions.
- Test transport/content is synthetic and test-only; it must traverse production operation/policy/mapper/resolver/UI seams without enabling a production live route.
- Evidence identity must include timestamp/timezone, git SHA, APK SHA-256, package/variant, API/ABI/AVD, WebView package/version, exact commands, results, uncovered items, and recovery path.
</interfaces>
</context>

<publication_gate>
Before every commit or push, invoke `$run-local-ci` for the exact worktree and record repository, branch, HEAD/worktree before/after, authoritative commands, timestamp/timezone, results, and excluded live/physical-device checks. Missing, stale, partial, or failing evidence blocks publication. Use one atomic commit per task, current-branch push only, and no merge/deploy/release/signing action.
</publication_gate>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Exercise the installed deterministic NetEase-to-Media3-to-lyric path</name>
  <files>android/app/src/androidTest/java/com/dazzlingwuming/listen2/Phase03ProviderInstrumentationTest.java, android/app/src/androidTest/java/com/dazzlingwuming/listen2/Phase03LyricInstrumentationTest.java, android/scripts/phase03-api35-smoke.sh</files>
  <behavior>
    - Test 1: packaged page posts typed NetEase search, receives source/capability-safe rows, opens deterministic directory/detail, and selects one default logical rendition.
    - Test 2: the existing single service/player/session consumes injected deterministic media and position visibly advances before lyric entry; page acknowledgement alone never counts as playing.
    - Test 3: primary timed lyric uses the same native identity/revision/position, and no snapshot/instrumentation output contains transient transport.
  </behavior>
  <action>Build an installed API-35 tracer around the real packaged Activity, exact appassets listener, Plan 03-01 parser/mapper, Plan 03-03 resolver/service/snapshot, and Plan 03-05/06 page. Inject only synthetic provider responses and deterministic Media3-readable test media through instrumentation seams; do not enable a production route or add a second player. Assert one service/player/session, source-labelled safe result/detail, one default prepared selection, advancing position above zero, and primary lyric entry from the accepted native clock per D-06/D-07/D-09. Implement `android/scripts/phase03-api35-smoke.sh --run-deterministic` as the sole Task-1 runner: it must fail closed unless exactly one online serial is selected into `PHASE03_ADB_SERIAL`, every ADB call uses `adb -s "$PHASE03_ADB_SERIAL"`, and that target reports API 35; bind the current git SHA to SHA-256 values for the exact main/test APK paths and require `apksigner verify --verbose --print-certs` to pass for both before install; run `adb -s "$PHASE03_ADB_SERIAL" install -r app/build/outputs/apk/debug/app-debug.apk` and `adb -s "$PHASE03_ADB_SERIAL" install -r -t app/build/outputs/apk/androidTest/debug/app-debug-androidTest.apk`; run exactly `Phase03ProviderInstrumentationTest` and `Phase03LyricInstrumentationTest`; recursively enforce output/evidence redaction; and reject stale git/APK/signature/device identity, ambiguous device or artifact selection, missing classes, and missing/nonterminal markers. Label every output deterministic-only per D-19.</action>
  <verify>
    <automated>bash android/scripts/phase03-api35-smoke.sh --run-deterministic</automated>
  </verify>
  <done>The exact installed APK proves one deterministic typed NetEase/default-Media3/primary-lyric path with real advancing service position and zero page-visible transport, explicitly without claiming live NET-004.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove lifecycle, stale cancellation, Room persistence, provider matrix, and accessibility semantics</name>
  <files>android/app/src/androidTest/java/com/dazzlingwuming/listen2/Phase03ProviderInstrumentationTest.java, android/app/src/androidTest/java/com/dazzlingwuming/listen2/Phase03LyricInstrumentationTest.java, android/scripts/phase03-webview-smoke.mjs</files>
  <behavior>
    - Test 1: cancel/revise/Back/rotation/renderer recreation and track change suppress old search/detail/lyric/manual replies and clear busy exactly once.
    - Test 2: pause, seek, transition, restore, offset, manual select/clear, Room reopen, text-only/missing/mismatch/timeout/error all retain usable audio and current identity.
    - Test 3: accessibility tree exposes current line/source/mode/offset/terminal state once; five unavailable providers have no action; 320px/200%/keyboard/safe-area/reduced-motion source states remain observable.
  </behavior>
  <action>Expand instrumentation and bounded CDP checks across D-08/D-10/D-14/D-16: current request identity, late reply suppression, player/detail Back order, native position after seek/pause/transition/restore, exact-key Room manual/offset restore, and each lyric degradation without playback failure. Inspect the accessibility tree/DOM semantics for one polite current-line/state announcement surface and labelled 48dp actions per D-15; do not claim acoustic TalkBack speech from static semantics. Assert QQ/Kugou/Kuwo/Migu/Taihe remain unavailable and non-clickable per D-05. Add deterministic CDP self-tests for stale/malformed/secret-bearing inputs and require no raw provider/transport/internal identity in captured output.</action>
  <verify>
    <automated>node android/scripts/phase03-webview-smoke.mjs --self-test &amp;&amp; bash android/scripts/phase03-api35-smoke.sh --run-deterministic</automated>
  </verify>
  <done>Installed deterministic lifecycle, Room, native-clock, degradation, capability, and accessibility-semantic gates pass; actual TalkBack speech and live authorization remain explicit later evidence.</done>
</task>

<task type="auto">
  <name>Task 3: Emit exact-build redacted deterministic evidence and reject substitution</name>
  <files>.planning/phases/03-netease-lyrics-provider-contract/03-API35-DETERMINISTIC-EVIDENCE.md, .planning/phases/03-netease-lyrics-provider-contract/evidence/03-deterministic-result.json, .planning/phases/03-netease-lyrics-provider-contract/evidence/03-netease.png, .planning/phases/03-netease-lyrics-provider-contract/evidence/03-lyrics.png, .planning/phases/03-netease-lyrics-provider-contract/evidence/03-picker.png</files>
  <action>Use the already-built harness to capture the required exact git/APK/package/API/ABI/AVD/WebView identity, exact commands, terminal markers, redacted bounded JSON, three distinct screenshots, uncovered live/TalkBack checks, and recovery command per D-18. Run its strict deterministic verifier, which rejects mismatched/stale identity, wrong package/variant/API, missing markers/screenshots, fixture-labelled-as-live claims, or any URL/query/signed candidate/header/cookie/provider body/credential/raw error/internal key/personal path per D-20. Evidence status may be PASS only as `deterministic gate`; overall NET-004 and Phase 3 remain not verified until Plan 03-08 live evidence passes per D-19.</action>
  <verify>
    <automated>bash android/scripts/phase03-api35-smoke.sh --verify-deterministic-evidence</automated>
  </verify>
  <done>Deterministic evidence is reproducible, exact-build-bound, redacted, substitution-resistant, and explicitly separates PASS deterministic architecture from BLOCKED/not-verified live completion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Instrumentation fixture → production seams | Test-controlled data must not enable production transport or bypass policy. |
| Installed app/CDP/ADB → evidence files | Runtime observations may contain provider, device, or path-sensitive data. |
| Evidence verifier → completion claim | A deterministic PASS must not impersonate authorized live proof. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-25 | Elevation of privilege | Instrumentation injection | high | mitigate | Test-only injection, production operation/policy path, debug test package only, no production capability enablement. |
| T-03-26 | Information disclosure | ADB/CDP/screenshots/evidence | high | mitigate | Bounded allow-listed capture, recursive forbidden-data scan, redacted screenshots, no raw logs/provider bodies. |
| T-03-27 | Spoofing | Deterministic evidence status | high | mitigate | Exact git/APK/device identity and explicit deterministic-only status; live markers cannot be synthesized by fixture mode. |
| T-03-28 | Tampering | Evidence artifacts | medium | mitigate | Fail closed on missing/mismatched identity, markers, screenshots, commands, timestamp, or recovery path. |
| T-03-SC | Tampering | Package installation | low | accept | No dependency installation; existing SDK/Gradle/test runner are reused. |
</threat_model>

<verification>
- Focused API-35 instrumentation, CDP self-test, deterministic run, and strict evidence verification pass.
- Evidence says deterministic-only and names live route/TalkBack acoustic coverage as not verified.
- `$run-local-ci` passes before every commit/push; no merge/deploy/release/signing action occurs.
</verification>

<success_criteria>
- The installed deterministic path touches the real packaged bridge, sole Media3, native clock, Room, and UI.
- Lifecycle/stale/error/accessibility semantics and the unavailable provider matrix are proven on API 35.
- Evidence is exact-build-bound and free of prohibited transport/secret/path data.
- NET-004 remains pending until the separate live gate passes.
</success_criteria>

<output>
Create `.planning/phases/03-netease-lyrics-provider-contract/03-07-SUMMARY.md` when done.
</output>
