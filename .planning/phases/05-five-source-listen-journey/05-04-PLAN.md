---
phase: 05-five-source-listen-journey
plan: 04
type: execute
wave: 4
depends_on:
  - 05-03
files_modified:
  - mobile/android/app/src/main/java/com/listen2mobile/qq/QqPlaybackPolicy.kt
  - mobile/android/app/src/main/java/com/listen2mobile/qq/QqPlaybackGateway.kt
  - mobile/android/app/src/main/java/com/listen2mobile/qq/QqPlaybackModule.kt
  - mobile/android/app/src/main/java/com/listen2mobile/qq/QqPlaybackPackage.kt
  - mobile/android/app/src/test/java/com/listen2mobile/qq/QqPlaybackContractTest.kt
autonomous: true
gap_closure: true
requirements:
  - NET-004
  - PLAY-001
  - SRCH-003
  - SEC-004
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A canonical QQ semantic track ID can be resolved by an unregistered native module through the one fixed musicu POST into a short-lived, probe-vetted media descriptor."
    - "QQ sends, receives, retains, and exposes no cookie; JavaScript cannot supply a URL, method, body, header, session value, or credential."
    - "Explicit QQ authorization/restriction outcomes remain typed, an ambiguous empty purl fails safely, and malformed/timeout/cancelled/stale operations never yield a descriptor."
    - "The assumed fixture CDN host is an exact fail-closed policy input, not live-host proof; live host and RNTP redirect behavior remain Phase 8 evidence."
    - "QQ playback/bootstrap capability is not registered or enabled by this plan."
  artifacts:
    - path: "mobile/android/app/src/main/java/com/listen2mobile/qq/QqPlaybackPolicy.kt"
      provides: "Pure semantic-ID, fixed-body, response, media-probe, lease, and safe-error policy."
    - path: "mobile/android/app/src/main/java/com/listen2mobile/qq/QqPlaybackGateway.kt"
      provides: "Cookie-free, bounded QQ metadata and probe transport owner."
    - path: "mobile/android/app/src/main/java/com/listen2mobile/qq/QqPlaybackModule.kt"
      provides: "Versioned semantic-ID-only React Native resolver, immutable policy-readiness constants, and cancellation boundary."
    - path: "mobile/android/app/src/test/java/com/listen2mobile/qq/QqPlaybackContractTest.kt"
      provides: "Deterministic route/body/schema/probe/error/cancellation fixtures."
  key_links:
    - from: "QqPlaybackModule"
      to: "QqPlaybackGateway"
      via: "Only version, requestId, and canonical qqtrack semantic ID cross the module boundary."
      pattern: "resolveAudio|cancel"
    - from: "QqPlaybackGateway"
      to: "https://u.y.qq.com/cgi-bin/musicu.fcg"
      via: "Native-built fixed POST body with redirects disabled and bounded response."
      pattern: "musicu.fcg|GetVkeyServer|CgiGetVkey"
    - from: "QqPlaybackPolicy"
      to: "probe-time media descriptor"
      via: "Exact assumed fixture host, HTTPS URI constraints, no-redirect Range probe, MIME/size check, and 60-second maximum lease."
      pattern: "isure.stream.qqmusic.qq.com"
---

<objective>
Create the cookie-free native QQ playback contract that Phase 05 needs, stopping at an unregistered module boundary.

Purpose: Prove the risky provider request and media-validation semantics independently before any shared JavaScript capability can invoke them. This plan neither registers the package nor changes QQ from search-only.

Output: Five QQ Kotlin/JVM files implementing and testing the fixed request, bounded probe-time validation, safe failures, and cancellation. Plan 05-05 owns registration and capability activation after both provider contracts exist.
</objective>

<execution_context>
@/Users/fluenteng/.codex/gsd-core/workflows/execute-plan.md
@/Users/fluenteng/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-five-source-listen-journey/05-CONTEXT.md
@.planning/phases/05-five-source-listen-journey/05-RESEARCH.md
@.planning/phases/05-five-source-listen-journey/05-PATTERNS.md
@.planning/phases/05-five-source-listen-journey/05-UI-SPEC.md
@.planning/phases/05-five-source-listen-journey/05-VALIDATION.md
@.planning/phases/05-five-source-listen-journey/05-REVIEW.md
@.planning/phases/05-five-source-listen-journey/05-VERIFICATION.md
@.planning/phases/05-five-source-listen-journey/05-01-SUMMARY.md
@.planning/phases/05-five-source-listen-journey/05-02-SUMMARY.md
@.planning/phases/05-five-source-listen-journey/05-03-SUMMARY.md

# Canonical semantic and error contracts
@mobile/src/api/client.ts
@mobile/src/api/providers.ts
@mobile/src/api/ids.ts
@mobile/src/types/provider.ts

# Native structural patterns
@mobile/android/app/src/main/java/com/listen2mobile/bilibili/BilibiliPolicy.kt
@mobile/android/app/src/main/java/com/listen2mobile/bilibili/BilibiliGateway.kt
@mobile/android/app/src/main/java/com/listen2mobile/bilibili/BilibiliModule.kt
@mobile/android/app/src/main/java/com/listen2mobile/bilibili/BilibiliPackage.kt
@mobile/android/app/src/test/java/com/listen2mobile/bilibili/BilibiliPolicyTest.kt

# Original-author request semantics are reference-only. Do not copy renderer cookie/header
# control, broad request interception, global Header mutation, CORS rewriting, or retries.
@app/listen1_chrome_extension/js/provider/qq.js
@app/main.js
</context>

<discovery>
Level 1 repository verification is complete: the original provider fixes the musicu route/body, and the mobile Bilibili module supplies the bounded native pattern. No package is added. `isure.stream.qqmusic.qq.com` is a sanitized deterministic-fixture assumption only; it is not asserted to be the exhaustive live QQ CDN set.
</discovery>

<dependency_graph>

| Task | Needs | Creates | Consumer |
| --- | --- | --- | --- |
| 1. QQ resolver tracer | Existing semantic ID and Bilibili transport patterns | Fixed cookie-free request/response/probe policy and one success path | Task 2 module lifecycle; Plan 05-05 integration |
| 2. QQ failure and lifecycle closure | Task 1 policy/gateway | Versioned unregistered module, typed denial/error map, deterministic cancellation | Plan 05-05 registration, adapter, capability, and player cancellation |
</dependency_graph>

<file_ownership>

- This plan exclusively owns the five listed `qq/` production/test files.
- `MainApplication.kt`, TypeScript API/player files, capability projection, legacy provider code, desktop request interception, and every existing planning/product document are read-only here.
- The package class may be created for Plan 05-05, but this plan must not add it to `MainApplication` or otherwise make the module discoverable at runtime.
</file_ownership>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Resolve one semantic QQ mid through the fixed native request and probe</name>
  <files>mobile/android/app/src/main/java/com/listen2mobile/qq/QqPlaybackPolicy.kt, mobile/android/app/src/main/java/com/listen2mobile/qq/QqPlaybackGateway.kt, mobile/android/app/src/main/java/com/listen2mobile/qq/QqPlaybackModule.kt, mobile/android/app/src/main/java/com/listen2mobile/qq/QqPlaybackPackage.kt, mobile/android/app/src/test/java/com/listen2mobile/qq/QqPlaybackContractTest.kt</files>
  <behavior>
    - RED: input `{version:1,requestId,trackId:'qqtrack_003fixtureMid'}` yields semantic `songmid=003fixtureMid`; the only metadata request is HTTPS POST `https://u.y.qq.com/cgi-bin/musicu.fcg`.
    - RED: native-built JSON contains root `req_1`, module `vkey.GetVkeyServer`, method `CgiGetVkey`, `songmid:['003fixtureMid']`, `filename:['M500003fixtureMid003fixtureMid.mp3']`, and the original provider's fixed `guid`, `uin`, loginflag, platform, and comm fields.
    - RED: a non-empty fixture purl is joined with fixture sip only inside native code, then accepted only as HTTPS with no userinfo/non-default port/fragment, exact assumed fixture host `isure.stream.qqmusic.qq.com`, and no redirect at resolution-time probe.
    - RED: the native-owned `Range: bytes=0-0` probe releases a descriptor only for the allowlisted audio MIME and a declared total in `1..536870912`; metadata bodies stop at 262144 bytes and the returned lease ends no later than 60 seconds after resolution.
    - RED: success returns only `version`, `requestId`, original `trackId`, `source:'qq'`, `url`, `mimeType`, `sizeBytes`, and `expiresAt`.
    - RED: neither metadata request nor media probe sends a Cookie header, and Set-Cookie values are ignored rather than stored or forwarded.
  </behavior>
  <action>
Follow RED -> GREEN -> REFACTOR. Use pure package-private policy types plus injected clock/transport seams so JVM fixtures assert the complete request and probe without live network. Parse only the existing canonical `qqtrack_` form, derive the semantic songmid, and build the fixed original-author musicu POST natively. Expose only `resolveAudio({version,requestId,trackId})` and `cancel({version,requestId})`; reject extra map keys before transport.

Implement `QqPlaybackGateway` with `HttpsURLConnection`, fixed native headers, explicit connect/read timeouts, disabled redirects, bounded response reads, and a one-byte native Range probe. QQ is deliberately stateless: do not create a CookieManager, do not retain Set-Cookie, and do not send Cookie on either request. Keep response joining and URL validation native.

Treat `isure.stream.qqmusic.qq.com` as the exact host approved only for sanitized deterministic fixtures. Unknown hosts fail closed with a stable unavailable/invalid-response code. The probe proves only what the endpoint returned at resolution time; it does not prove that RNTP will see identical redirect/MIME behavior later. Keep the module/package unregistered and leave capability projection untouched.
  </action>
  <verify>
    <automated>cd mobile/android &amp;&amp; ./gradlew --offline :app:testDebugUnitTest --tests 'com.listen2mobile.qq.QqPlaybackContractTest.qqSemanticMidBuildsFixedCookieFreeRequestAndVettedDescriptor'</automated>
  </verify>
  <done>The focused tracer passes from canonical QQ track ID through the exact cookie-free POST and probe-time policy to a bounded descriptor, while the package remains unregistered and capability unavailable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Close QQ authorization, malformed, timeout, and cancellation behavior</name>
  <files>mobile/android/app/src/main/java/com/listen2mobile/qq/QqPlaybackPolicy.kt, mobile/android/app/src/main/java/com/listen2mobile/qq/QqPlaybackGateway.kt, mobile/android/app/src/main/java/com/listen2mobile/qq/QqPlaybackModule.kt, mobile/android/app/src/test/java/com/listen2mobile/qq/QqPlaybackContractTest.kt</files>
  <behavior>
    - RED: malformed/extra request keys, invalid version/requestId/semantic ID, caller URL/method/body/header/cookie/session fields, and duplicate active request IDs return `INVALID_REQUEST` before network I/O.
    - RED: explicit provider fixtures map authentication, membership, DRM, and region restrictions to `LOGIN_REQUIRED`, `MEMBERSHIP_REQUIRED`, `DRM_RESTRICTED`, and `REGION_RESTRICTED`; empty purl without an explicit signal returns `PLAYBACK_UNAVAILABLE` rather than guessing.
    - RED: invalid schema, body overflow, unapproved host, redirect, URI violation, MIME mismatch, missing/oversized declared total, or stale lease returns a safe stable error with no raw body, URL query, headers, or exception text.
    - RED: connect/read expiry becomes `REQUEST_TIMEOUT`; `cancel(requestId)` disconnects metadata/probe transport, settles once as `CANCELLED`, and suppresses late success.
    - RED: module invalidation cancels every active request; a stale generation cannot resolve after cancellation, and the module/package exports no generic request, cookie, or header method.
    - RED: all network fixtures assert the complete outbound header set contains no Cookie and all response-cookie fixtures leave no state for the next request.
    - RED: immutable module constants identify provider `qq`, contract version 1, and policy readiness; readiness is false when the exact assumed-host set is empty/unapproved so Plan 05-05 cannot activate capability optimistically.
  </behavior>
  <action>
Expand the tracer with table-driven negative fixtures before implementation. Classify only explicit repository/fixture evidence; ambiguous denial remains unavailable, never an entitlement bypass. Use stable existing ProviderErrorCode strings and a sanitized native result schema. Do not expose raw messages, request/response headers, cookie state, signed query details, or retry controls.

Track active calls by bounded requestId, reject collisions, make promise settlement atomic/single-shot, disconnect both metadata and probe connections on cancel/invalidation, and discard late completions. Verify a second request is indistinguishable from a fresh process with respect to cookies. Export immutable provider/version/policy-readiness constants for Plan 05-05; readiness requires a nonempty exact assumed-host policy but does not claim live-host proof. Keep `QqPlaybackPackage` absent from application registration and do not edit JavaScript capability/bootstrap code.
  </action>
  <verify>
    <automated>cd mobile/android &amp;&amp; ./gradlew --offline :app:testDebugUnitTest --tests 'com.listen2mobile.qq.QqPlaybackContractTest'</automated>
  </verify>
  <done>The complete QQ JVM contract passes fixed-route, no-cookie, safe-denial, malformed, bounded-policy, timeout, cancellation, teardown, and late-generation cases; no runtime registration or capability activation exists yet.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Test/direct module caller -> QQ native resolver | Untrusted maps are reduced to version, requestId, and semantic QQ track ID; the module remains unregistered in this plan. |
| Native resolver -> QQ metadata API | Native owns the exact POST/body/headers, has no cookie store, disables redirects, and bounds time and bytes. |
| QQ response -> media descriptor | Untrusted schema/URL crosses exact assumed-host and probe-time HTTPS/MIME/declared-size/lease validation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-05-04-01 | Spoofing | Native request identity | high | mitigate | Exact version/key/requestId/semantic-ID parsing, active-ID collision rejection, and echoed correlation fields. |
| T-05-04-02 | Tampering | Fixed request and provider response | high | mitigate | Native-built exact body, injected transport fixtures, bounded parser, exact assumed host, no redirects, probe-time MIME/size validation, and short lease. |
| T-05-04-03 | Information Disclosure | Cookies and media metadata | high | mitigate | No QQ cookie manager/header/storage, ignored Set-Cookie, allowlisted success schema, and sanitized stable errors. |
| T-05-04-04 | Elevation of Privilege | Authorization/member/DRM/region denial | high | mitigate | Only explicit denial mapping, ambiguous empty purl fails unavailable, and caller cannot supply credentials or retry policy. |
| T-05-04-05 | Denial of Service | Metadata/probe operations | medium | mitigate | Timeouts, 256 KiB metadata bound, one-byte probe, 512 MiB declared ceiling, collision rejection, cancellation disconnect, and teardown. |
| T-05-04-06 | Tampering | Stale descriptor/completion | high | mitigate | 60-second maximum lease, atomic completion, cancellation generation, and late-success suppression. |
</threat_model>

<rollback>

Remove the unregistered QQ package and JVM test files. No application registration, JavaScript capability, player state, stored ID, persisted session, cache, or schema changes exist in this plan, so rollback is deletion of these five isolated files after confirming Plan 05-05 has not executed.
</rollback>

<verification>

1. `cd mobile/android && ./gradlew --offline :app:testDebugUnitTest --tests 'com.listen2mobile.qq.QqPlaybackContractTest'`.
2. `npm run mobile:test && npm run mobile:typecheck` to prove the unregistered native addition did not alter canonical JavaScript behavior.
3. `git diff --check` and `git status --short`; implementation diff is limited to the five owned files plus `05-04-SUMMARY.md`.
4. Confirm `MainApplication.kt` and `mobile/src/api/client.ts` are unchanged in this plan, so QQ playback/bootstrap remains unavailable.

Do not claim APK installation, API 35 native loading, live account/entitlement/CDN behavior, exact final production CDN hosts, RNTP redirect handling, or audible playback; those are Phase 8 evidence after Plan 05-05 integration.
</verification>

<source_coverage_audit>

| Source | Item | Status | Coverage |
| --- | --- | --- | --- |
| GOAL | Close QQ's part of the remaining five-source authorized-playback gap. | COVERED | Native QQ contract is proven here; activation is deliberately sequenced in 05-05. |
| REQ NET-004 | Bounded native support for provider network semantics. | COVERED | Fixed POST, bounds, timeouts, no redirects/cookies, probe, cancellation. |
| REQ PLAY-001 | Produce a safe media descriptor for the canonical player path. | COVERED | Probe-vetted short lease exists; handoff waits for 05-05. |
| REQ SRCH-003 | Preserve canonical semantic provider identity. | COVERED | Only `qqtrack_` semantic ID enters; no provider-specific UI route is introduced. |
| REQ SEC-004 | Do not expose credentials or bypass restrictions. | COVERED (QQ slice) | Explicit no-cookie contract, semantic-only input, typed denials, and sanitized errors. |
| RESEARCH | Native owns transport; deterministic fixtures precede capability. | COVERED | Both tasks are JVM-first and package stays unregistered. |
| CONTEXT | Canonical mobile boundary, bounded failure, non-bypass, Phase 8 live proof. | COVERED | Module is narrow/fail-closed and live/RNTP evidence is excluded explicitly. |

No deferred idea is included. Kuwo, JavaScript registration/capability, player cancellation propagation, and live/device evidence are sequenced into 05-05/Phase 8 rather than silently omitted.
</source_coverage_audit>

<success_criteria>

- The QQ contract test passes the exact fixed request/body and semantic M500 filename.
- QQ proves no Cookie request, no response-cookie retention, no caller cookie/header/URL surface, and safe explicit/ambiguous denial behavior.
- Redirects, unapproved host, malformed schema, probe MIME/size failure, timeout, cancel, teardown, and stale completion fail closed.
- The fixture host is documented as an assumption and validation as probe-time only; live exact-host/RNTP redirect proof stays Phase 8.
- `MainApplication.kt` and TypeScript capability/bootstrap remain unchanged.
</success_criteria>

<output>
Create `.planning/phases/05-five-source-listen-journey/05-04-SUMMARY.md` when done.
</output>
