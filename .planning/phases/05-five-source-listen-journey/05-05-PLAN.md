---
phase: 05-five-source-listen-journey
plan: 05
type: execute
wave: 5
depends_on:
  - 05-04
files_modified:
  - mobile/android/app/src/main/java/com/listen2mobile/kuwo/KuwoPlaybackPolicy.kt
  - mobile/android/app/src/main/java/com/listen2mobile/kuwo/KuwoPlaybackGateway.kt
  - mobile/android/app/src/main/java/com/listen2mobile/kuwo/KuwoPlaybackModule.kt
  - mobile/android/app/src/main/java/com/listen2mobile/kuwo/KuwoPlaybackPackage.kt
  - mobile/android/app/src/test/java/com/listen2mobile/kuwo/KuwoPlaybackContractTest.kt
  - mobile/android/app/src/main/java/com/listen2mobile/MainApplication.kt
  - mobile/src/api/nativePlayback.ts
  - mobile/src/api/client.ts
  - mobile/src/api/__tests__/client.test.ts
  - mobile/src/player/playerController.ts
  - mobile/src/player/__tests__/playerController.test.ts
autonomous: true
gap_closure: true
requirements:
  - NET-004
  - PLAY-001
  - SRCH-003
  - SEC-004
estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 4
  confidence: low
must_haves:
  truths:
    - "The exact repository Kuwo cookie-derived Secret contract resolves a canonical Kuwo semantic ID through native-owned session state and one fixed playUrl request; JavaScript never receives or supplies Cookie, Secret, headers, or URLs."
    - "QQ and Kuwo playback/bootstrap capabilities become available only when their registered native modules advertise the exact contract version and a ready exact-host policy; missing, mismatched, or unapproved policy fails closed."
    - "Every playback operation propagates one AbortSignal through PlayerController, ProviderClient, the native adapter, and native cancel, so superseded or teardown-stale work cannot reach RNTP."
    - "Authorized QQ and Kuwo fixtures reach the canonical PlayerController/RNTP transaction, while authorization, entitlement, malformed, timeout, cancellation, and unapproved-host failures preserve current playback and occurrence queue."
    - "Fixture CDN hosts and resolution-time probes are deterministic assumptions, not live/RNTP proof; final exact hosts, redirects, accounts, CDN, installed APK, and API 35 remain Phase 8 evidence."
  artifacts:
    - path: "mobile/android/app/src/main/java/com/listen2mobile/kuwo/KuwoPlaybackPolicy.kt"
      provides: "Exact numeric-nonce Secret port, fixed-route and probe-time media policy, and safe error mapping."
    - path: "mobile/android/app/src/main/java/com/listen2mobile/kuwo/KuwoPlaybackGateway.kt"
      provides: "Native-only fixed-cookie session owner with one bounded refresh."
    - path: "mobile/android/app/src/main/java/com/listen2mobile/kuwo/KuwoPlaybackModule.kt"
      provides: "Versioned semantic-ID-only resolve/cancel module and fail-closed policy-readiness constants."
    - path: "mobile/src/api/nativePlayback.ts"
      provides: "Exact QQ/Kuwo native-contract validation, semantic bootstrap, and AbortSignal-to-native cancellation."
    - path: "mobile/src/api/client.ts"
      provides: "Fail-closed capability projection and QQ/Kuwo canonical bootstrap routing."
    - path: "mobile/src/player/playerController.ts"
      provides: "Playback-operation AbortController/generation guard through the sole RNTP owner."
    - path: "mobile/src/api/__tests__/client.test.ts"
      provides: "Native readiness, schema, route, safe-error, and cancellation fixtures."
    - path: "mobile/src/player/__tests__/playerController.test.ts"
      provides: "QQ/Kuwo journey and stale/teardown non-mutation proof."
  key_links:
    - from: "KuwoPlaybackGateway"
      to: "www.kuwo.cn homepage and /api/v1/www/music/playUrl"
      via: "Native-owned fixed cookie name, numeric-nonce Secret, exact query, no redirects, bounds, and one refresh."
      pattern: "Hm_Iuvt_cdb524f42f23cer9b268564v7y735ewrq2324|playUrl"
    - from: "mobile/src/api/client.ts"
      to: "mobile/src/api/nativePlayback.ts"
      via: "Capability readiness and bootstrap accept only exact versioned QQ/Kuwo native modules."
      pattern: "bootstrapTrack|isNativePlaybackReady"
    - from: "mobile/src/player/playerController.ts"
      to: "ProviderClient -> native cancel -> RNTP"
      via: "One operation generation and AbortSignal gates every async boundary before queue mutation."
      pattern: "AbortController|bootstrapTrack"
    - from: "native QQ/Kuwo probe"
      to: "RNTP media load"
      via: "Probe-time descriptor is revalidated for identity/lease before RNTP, with live redirect behavior deferred to Phase 8."
      pattern: "expiresAt|requestId|trackId|source"
---

<objective>
Finish the Phase 05 gap by adding the fail-closed Kuwo native session contract, registering both QQ and Kuwo resolvers, activating their canonical capabilities, and carrying cancellation through the sole player owner.

Purpose: Pair the 05-04 cookie-free QQ resolver with an exact repository-derived Kuwo cookie/Secret owner, then prove both sources can reach RNTP without stale or teardown work mutating playback.

Output: Five Kuwo Kotlin/JVM files, two package registrations, one constrained native adapter, provider-client activation, PlayerController cancellation propagation, and deterministic native/Jest evidence. Live/device evidence remains Phase 8.
</objective>

<execution_context>
@/Users/fluenteng/.codex/gsd-core/workflows/execute-plan.md
@/Users/fluenteng/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-five-source-listen-journey/05-CONTEXT.md
@.planning/phases/05-five-source-listen-journey/05-RESEARCH.md
@.planning/phases/05-five-source-listen-journey/05-PATTERNS.md
@.planning/phases/05-five-source-listen-journey/05-UI-SPEC.md
@.planning/phases/05-five-source-listen-journey/05-VALIDATION.md
@.planning/phases/05-five-source-listen-journey/05-REVIEW.md
@.planning/phases/05-five-source-listen-journey/05-VERIFICATION.md
@.planning/phases/05-five-source-listen-journey/05-04-SUMMARY.md

# Canonical native/API/player contracts
@mobile/android/app/src/main/java/com/listen2mobile/MainApplication.kt
@mobile/android/app/src/main/java/com/listen2mobile/bilibili/BilibiliPolicy.kt
@mobile/android/app/src/main/java/com/listen2mobile/bilibili/BilibiliGateway.kt
@mobile/android/app/src/main/java/com/listen2mobile/bilibili/BilibiliModule.kt
@mobile/src/bilibili/client.ts
@mobile/src/api/client.ts
@mobile/src/api/providers.ts
@mobile/src/api/ids.ts
@mobile/src/api/errors.ts
@mobile/src/types/provider.ts
@mobile/src/player/playerController.ts
@mobile/src/api/__tests__/client.test.ts
@mobile/src/player/__tests__/playerController.test.ts

# Original-author cookie name, Secret algorithm, and route are reference-only. Do not
# copy raw URL/header/cookie APIs, global Header state, CORS workarounds, or broad retries.
@app/listen1_chrome_extension/js/provider/kuwo.js
@app/main.js
</context>

<discovery>
Level 1 repository verification is complete. The only permitted Kuwo implementation is the exact repository algorithm checked by the corrected fake numeric-nonce vector. No new dependency is needed. `er-sycdn.kuwo.cn` is a sanitized deterministic-fixture host assumption, not an exhaustive live CDN claim.
</discovery>

<dependency_graph>

| Task | Needs | Creates | Consumer |
| --- | --- | --- | --- |
| 1. Kuwo session tracer | Repository cookie/Secret/route evidence; native transport pattern | Exact Secret vector, fixed session acquisition/playUrl request, probe-vetted success | Task 2 module lifecycle |
| 2. Kuwo module closure | Task 1 policy/gateway | Versioned fail-closed module/package, safe errors and deterministic native cancellation | Task 3 registration/adapter |
| 3. Native registration and provider API | Passing 05-04 QQ and Tasks 1-2 Kuwo contracts | Both registered modules, readiness-gated capability and semantic bootstrap/cancel adapter | Task 4 PlayerController operation |
| 4. Player operation cancellation | Task 3 signal-aware ProviderClient | One operation AbortSignal/generation through native cancel; guarded RNTP transaction | Phase 05 verification; Phase 8 live/device evidence |
</dependency_graph>

<file_ownership>

- Tasks 1-2 exclusively own the five listed `kuwo/` files.
- Task 3 exclusively owns `MainApplication.kt`, `nativePlayback.ts`, `client.ts`, and `client.test.ts`.
- Task 4 exclusively owns `playerController.ts` and `playerController.test.ts`.
- Plan 05-04 owns QQ implementation files; this plan consumes them without modifying them. Legacy provider/desktop files and all existing plans/product documents are read-only.
</file_ownership>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove the exact Kuwo numeric-nonce Secret and native session route</name>
  <files>mobile/android/app/src/main/java/com/listen2mobile/kuwo/KuwoPlaybackPolicy.kt, mobile/android/app/src/main/java/com/listen2mobile/kuwo/KuwoPlaybackGateway.kt, mobile/android/app/src/test/java/com/listen2mobile/kuwo/KuwoPlaybackContractTest.kt</files>
  <behavior>
    - RED: canonical `kwtrack_123456` becomes only semantic `mid=123456`; native may GET only `https://www.kuwo.cn/` for session acquisition and `https://www.kuwo.cn/api/v1/www/music/playUrl?mid=123456&amp;type=music&amp;httpsStatus=1&amp;reqId=&amp;plat=web_www&amp;from=` for resolution.
    - RED: the exact repository algorithm with token `fixture-token`, cookie name `Hm_Iuvt_cdb524f42f23cer9b268564v7y735ewrq2324`, and numeric decimal nonce `1234567` returns exactly `452fda90010b117cb165a7af6d0012d687`.
    - RED: nonce input is an unsigned numeric value in `0..4294967295`, formatted inside the algorithm as exactly eight lowercase hexadecimal characters; the production SecureRandom adapter repeatedly proves this numeric range/format without signed, locale, or variable-width output.
    - RED: homepage parsing accepts only the fixed cookie name, stores its value only in the gateway's process-memory session, and the playUrl request receives only native-built Cookie and Secret values; neither appears in result/error fixtures.
    - RED: the repository-proven authorization-failure envelope clears session, reacquires once, and retries playUrl once; any second denial stops with a typed safe failure.
    - RED: a success `data.url` is accepted only as HTTPS with no userinfo/non-default port/fragment, exact assumed fixture host `er-sycdn.kuwo.cn`, and a no-redirect resolution-time Range probe proving allowed audio MIME and declared total `1..536870912`; metadata is capped at 262144 bytes and lease at 60 seconds.
  </behavior>
  <action>
Follow RED -> GREEN -> REFACTOR. Port only the original-author Kuwo `h(token,cookieName)` algorithm. Inject a numeric nonce source into pure policy code; use the exact corrected golden vector above as the first gate, and make production nonce generation a SecureRandom-backed unsigned 32-bit number whose eight-character lowercase hexadecimal rendering is tested separately. If the vector differs by even one character, stop with policy unavailable; do not reinterpret the nonce as a hexadecimal input, invent token logic, or import signing code.

Build a native-only session owner around the fixed homepage and cookie name. Keep cookie value and Secret in memory, never logs/results, build the exact playUrl query from semantic mid, and permit only one proven session refresh. Use injected transports for fixtures; production uses bounded `HttpsURLConnection`, fixed native headers, explicit connect/read timeouts, disabled redirects, and bounded reads.

Parse `data.url` in native and run the native-owned one-byte Range probe. Treat `er-sycdn.kuwo.cn` as an exact deterministic-fixture assumption; an unknown host fails closed. The probe validates only the resolution-time response and cannot establish what RNTP will observe later.
  </action>
  <verify>
    <automated>cd mobile/android &amp;&amp; ./gradlew --offline :app:testDebugUnitTest --tests 'com.listen2mobile.kuwo.KuwoPlaybackContractTest.secretUsesDecimalNumericNonceAndFixedRoute'</automated>
  </verify>
  <done>The corrected decimal-nonce vector, SecureRandom numeric/format contract, fixed native session/playUrl request, one refresh, and probe-vetted fixture descriptor pass without exposing session material.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Close the Kuwo versioned module, safe errors, and native cancellation</name>
  <files>mobile/android/app/src/main/java/com/listen2mobile/kuwo/KuwoPlaybackGateway.kt, mobile/android/app/src/main/java/com/listen2mobile/kuwo/KuwoPlaybackModule.kt, mobile/android/app/src/main/java/com/listen2mobile/kuwo/KuwoPlaybackPackage.kt, mobile/android/app/src/test/java/com/listen2mobile/kuwo/KuwoPlaybackContractTest.kt</files>
  <behavior>
    - RED: module exposes only `resolveAudio({version,requestId,trackId})`, `cancel({version,requestId})`, and immutable contract constants; extra keys or caller URL/method/body/header/cookie/Secret/session/retry fields fail before transport.
    - RED: constants identify provider `kuwo`, contract version 1, and policy readiness; readiness is false for missing/mismatched golden-vector policy or an empty/unapproved exact-host set.
    - RED: success echoes version/requestId/trackId/source and returns only URL/MIME/size/expiry; explicit login/member/DRM/region fixtures keep matching stable codes, ambiguous denial becomes `PLAYBACK_UNAVAILABLE`, malformed becomes `INVALID_RESPONSE`, and expiry becomes `REQUEST_TIMEOUT`.
    - RED: duplicate active IDs fail; cancel disconnects homepage/playUrl/probe transport, clears sensitive per-call values, settles once as `CANCELLED`, and suppresses late success; module invalidation cancels all requests and clears process-memory session.
    - RED: raw cookie/Secret/header/body/URL query/exception data is absent from every failure and no generic request/cookie/header method is exported.
  </behavior>
  <action>
Create the React Native module/package around Task 1 using the established Bilibili lifecycle pattern. Contract constants must let JavaScript synchronously verify exact provider/version/policy readiness without exposing session values. Policy readiness is true only when the corrected deterministic vector is satisfied and a nonempty exact assumed-host policy is compiled; otherwise all resolution fails closed and capability cannot activate.

Validate exact request keys, bounded IDs, and canonical `kwtrack_` form. Track connections per request, reject collisions, make settlement atomic, disconnect every stage on cancellation, and discard late completions. Clear cookie/Secret session on authorization refresh and module invalidation. Map only stable existing provider error codes and keep raw external material out of the bridge. Do not register the package in this task.
  </action>
  <verify>
    <automated>cd mobile/android &amp;&amp; ./gradlew --offline :app:testDebugUnitTest --tests 'com.listen2mobile.kuwo.KuwoPlaybackContractTest'</automated>
  </verify>
  <done>The full Kuwo JVM module contract passes exact schema/readiness, safe-denial/malformed/timeout, bounded transport, session clearing, cancellation, invalidation, and late-completion cases while remaining unregistered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Register both modules and activate fail-closed canonical bootstrap</name>
  <files>mobile/android/app/src/main/java/com/listen2mobile/MainApplication.kt, mobile/src/api/nativePlayback.ts, mobile/src/api/client.ts, mobile/src/api/__tests__/client.test.ts</files>
  <behavior>
    - RED: `MainApplication` registers both `QqPlaybackPackage` and `KuwoPlaybackPackage` exactly once without altering existing packages.
    - RED: QQ/Kuwo playback/bootstrap capability is available only when the matching NativeModule exists and immutable constants match provider, version 1, policyReady true, and nonempty exact assumed-host policy; absent/mismatched/unapproved policy remains unavailable.
    - RED: `providerClient.bootstrapTrack(track,signal)` routes `qqtrack_` only to QQ and `kwtrack_` only to Kuwo with version/requestId/trackId; no URL/body/header/cookie/Secret crosses into native.
    - RED: adapter accepts only exact echoed identity/schema plus approved HTTPS host, allowed MIME, positive bounded size, and current unexpired maximum-60-second lease; stale/mismatched/extra/malformed replies become sanitized `ProviderClientError`.
    - RED: AbortSignal invokes the matching native cancel once with the same version/requestId, returns `CANCELLED`, and discards late completion; module absence, readiness false, or response host outside the advertised exact policy fails closed before returning bootstrap.
    - RED: search/detail/lyrics and NetEase/Kugou/Bilibili capabilities/routes remain unchanged while QQ and Kuwo gain only playback/bootstrap.
  </behavior>
  <action>
After both native suites pass, register 05-04's QQ package and this plan's Kuwo package in `MainApplication.kt`. Ensure the QQ module exposes the same immutable provider/version/policy-readiness contract used here; do not add any QQ cookie handling. Create `nativePlayback.ts` as the only JavaScript consumer of these modules. It must synchronously validate module constants for capability projection, generate bounded unique request IDs, call the provider-specific resolve method with semantic fields only, forward AbortSignal to provider-specific cancel, and validate exact correlated results before returning the existing BootstrapTrack shape.

Update `client.ts` so capability projection and bootstrap use the adapter. Enable QQ/Kuwo playback/bootstrap only for ready exact contracts; do not fall back to generic fetch or optimistic capability when the module/policy is absent. A media response outside the compiled/advertised exact assumed-host set remains unavailable even though the provider module exists. Preserve all other provider operations.

Use Jest NativeModules fixtures to prove registration contract, readiness rejection, exact request/result schema, safe error mapping, current lease, and native cancellation/late-reply suppression. Capability activation is based on deterministic assumed-host contracts; it is not evidence those are the final production CDN hosts.
  </action>
  <verify>
    <automated>cd mobile &amp;&amp; npm test -- --runInBand src/api/__tests__/client.test.ts &amp;&amp; npm run typecheck</automated>
  </verify>
  <done>Both packages are registered; QQ/Kuwo capability and bootstrap activate only for exact ready contracts, use semantic input and typed output, and propagate AbortSignal to native cancel without exposing network/session controls.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Make player-operation cancellation guard every RNTP mutation</name>
  <files>mobile/src/player/playerController.ts, mobile/src/player/__tests__/playerController.test.ts</files>
  <behavior>
    - RED: each play selection owns one AbortController and monotonically current generation; its signal is passed to `providerClient.bootstrapTrack` for QQ, Kuwo, and existing providers.
    - RED: a replacement selection aborts the prior operation before starting the next; stop/reset/destroy/teardown lifecycle aborts the active operation and invalidates its generation.
    - RED: after every awaited bootstrap boundary, controller checks signal/generation before TrackPlayer reset/add/play or current/queue mutation; a stale or teardown completion performs zero RNTP calls and returns typed cancellation.
    - RED: authorized QQ and Kuwo search-result fixtures each reach the existing TrackPlayer add/play path once and preserve semantic source/track ID plus player-owned occurrence identity.
    - RED: login/member/DRM/region/unavailable/malformed/timeout/cancel failures leave prior RNTP current item, occurrence queue, controller current state, and other provider results unchanged.
    - RED: controller teardown causes native adapter cancel once, tolerates a late native success, and never creates an unhandled rejection.
  </behavior>
  <action>
Add operation-scoped AbortController/generation ownership inside the existing PlayerController rather than creating another player owner. Abort the active resolution on replacement and every existing stop/reset/destroy/teardown entrypoint that invalidates pending playback. Pass the signal into the existing providerClient bootstrap signature. Recheck both signal and generation immediately before each RNTP or controller-state mutation after an await; normalize superseded work as the existing typed `CANCELLED` result.

Extend tests with one QQ and one Kuwo canonical search-result journey and deferred native promises for replacement/teardown races. Assert native cancel receives the same request correlation via the adapter, stale completion never reaches RNTP, and failures roll back to the exact prior queue/current snapshot. Do not change TrackPlayer ownership, occurrence-ID rules, search-result storage, or unrelated retry policy.
  </action>
  <verify>
    <automated>cd mobile &amp;&amp; npm test -- --runInBand src/player/__tests__/playerController.test.ts src/api/__tests__/client.test.ts &amp;&amp; npm run typecheck &amp;&amp; npm run lint -- --quiet</automated>
  </verify>
  <done>One operation AbortSignal reaches native cancellation end to end, and deterministic replacement/teardown tests prove stale QQ/Kuwo work cannot invoke RNTP or mutate canonical playback state.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| JavaScript -> native QQ/Kuwo modules | Exact module constants gate capability; requests contain only version, requestId, and semantic trackId. |
| Kuwo native session -> fixed provider APIs | Cookie and derived Secret remain native/process-memory and accompany only fixed homepage/playUrl operations. |
| Provider response -> bootstrap descriptor | Exact assumed-host and resolution-time probe policy validates untrusted URL/schema/MIME/size/lease before JavaScript handoff. |
| ProviderClient -> PlayerController -> RNTP | One AbortSignal/generation must remain current across every await before RNTP/state mutation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-05-05-01 | Spoofing | Native module/readiness and request identity | high | mitigate | Exact provider/version/policy constants, semantic ID parsing, unique request IDs, echoed identity, and generation checks. |
| T-05-05-02 | Tampering | Kuwo Secret/request and media response | high | mitigate | Correct decimal-nonce golden vector, fixed route/headers, SecureRandom format test, exact assumed host, no redirects, bounded probe, and JS revalidation. |
| T-05-05-03 | Information Disclosure | Kuwo Cookie/Secret and signed media data | high | mitigate | Native process-memory owner, no caller header/cookie fields, allowlisted results, session clearing, and sanitized errors/tests. |
| T-05-05-04 | Elevation of Privilege | Account/member/DRM/region denial | high | mitigate | Explicit typed denial, ambiguous unavailable, one proven session refresh only, and no generic request/credential override. |
| T-05-05-05 | Denial of Service | Session/metadata/probe/player operations | medium | mitigate | Timeouts/body bounds, one-byte probe, one retry, active-ID rejection, AbortSignal disconnect, teardown, and single settlement. |
| T-05-05-06 | Tampering | Stale bootstrap reaching RNTP | high | mitigate | Short lease, request/source/track correlation, one player operation generation, post-await guards, and late-result suppression. |
| T-05-05-07 | Repudiation | Live CDN/RNTP behavior | low | accept | Phase 05 records deterministic status/correlation only; Phase 8 owns redacted device/live evidence because provider behavior can change after probe. |
</threat_model>

<rollback>

1. First make QQ/Kuwo playback/bootstrap capability unavailable and restore their typed unavailable bootstrap behavior; abort the active player operation so no pending native result reaches RNTP.
2. Unregister both packages from `MainApplication`, remove the TypeScript adapter wiring, cancel native requests, and clear Kuwo process-memory cookie/Secret state. QQ has no cookie/session state.
3. Revert only the operation AbortController/generation additions after no native bootstrap can be pending. Preserve PlayerController ownership, occurrence queue/current item, semantic IDs, search/detail/lyrics, and the other three provider paths.
4. This plan adds no database/cache migration or persisted credential. Plan 05-04's unregistered QQ contract may remain for diagnosis or be removed separately.
</rollback>

<external_evidence_boundary>

Phase 05 accepts the exact QQ and Kuwo media hosts only as sanitized deterministic-fixture assumptions and validates them only during the native resolution-time probe. Phase 8 must record, with cookies, Secrets, signed URLs, account data, and raw responses redacted:

- installed APK/commit/build identity, API 35 device/emulator, timestamp, and search-result-to-audible-playback action for both providers;
- final exact QQ and Kuwo CDN hosts observed from authorized and denied cases, resolution-time status/MIME/declared size/redirect chain, descriptor age, and any host-policy mismatch;
- RNTP's actual handling of the returned URL, including later CDN redirects or changed MIME/length, because the native probe cannot prove subsequent RNTP transport behavior;
- QQ fixed body-shape/hash and empty-purl classification, with confirmation that no Cookie is sent or retained;
- Kuwo fixed cookie name/security attributes, header names only, corrected fake golden-vector result, one session-refresh behavior, and playUrl route/query;
- live account/member/region/CDN availability, native-module loading, cancellation during network delay, system playback behavior, and audible output.

Any observed host not in the exact policy requires a reviewed, sanitized fixture and exact-host policy update. Capability/bootstrap must continue failing closed until approval; suffix/wildcard expansion is not acceptable.
</external_evidence_boundary>

<verification>

1. `cd mobile/android && ./gradlew --offline :app:testDebugUnitTest --tests 'com.listen2mobile.qq.QqPlaybackContractTest' --tests 'com.listen2mobile.kuwo.KuwoPlaybackContractTest'`.
2. `cd mobile && npm test -- --runInBand src/api/__tests__/client.test.ts src/player/__tests__/playerController.test.ts`.
3. `npm run mobile:test && npm run mobile:typecheck && npm --prefix mobile run lint -- --quiet`.
4. Run the repository formatter/check only on `mobile/src/api/nativePlayback.ts`, `mobile/src/api/client.ts`, `mobile/src/api/__tests__/client.test.ts`, `mobile/src/player/playerController.ts`, and `mobile/src/player/__tests__/playerController.test.ts`.
5. `git diff --check` and `git status --short`; implementation diff is limited to the 11 owned files plus `05-05-SUMMARY.md`.

Do not substitute APK assembly, API 35, live accounts/CDNs, or audible playback with JVM/Jest success. Report those checks `not verified` for Phase 8.
</verification>

<source_coverage_audit>

| Source | Item | Status | Coverage |
| --- | --- | --- | --- |
| GOAL | All five visible sources support canonical authorized search-to-playback. | COVERED | Kuwo contract plus QQ/Kuwo registration and PlayerController journeys close the sole verification gap. |
| REQ NET-004 | Bounded native provider support and typed transport failures. | COVERED | Tasks 1-3 fix routes, native session, limits, readiness, probe, and cancellation. |
| REQ PLAY-001 | Canonical player transaction and safe control lifecycle. | COVERED | Task 4 carries operation cancellation through sole RNTP ownership and proves rollback. |
| REQ SRCH-003 | Search results retain canonical semantic identity into playback. | COVERED | Tasks 3-4 route existing `qqtrack_`/`kwtrack_` results without side channels. |
| REQ SEC-004 | Protect credentials and respect authorization restrictions. | COVERED (QQ/Kuwo slice) | Native Kuwo Cookie/Secret, no QQ cookie, semantic-only JS, typed denial, no bypass, sanitized errors. |
| RESEARCH | Operation-level capability, deterministic contracts, generation/cancellation, non-destructive failure. | COVERED | Tasks 2-4 gate readiness and exercise stale/teardown races. |
| CONTEXT | Canonical mobile integration, fixed allowlists, native session ownership, no desktop interception port, Phase 8 live proof. | COVERED | All four tasks enforce the boundary and the evidence section states the remaining uncertainty. |

No deferred idea is included. Broader Phase 7 security closure and Phase 8 device/live/RNTP evidence remain explicit exclusions rather than unplanned Phase 05 work.
</source_coverage_audit>

<success_criteria>

- Kuwo produces the exact corrected Secret for decimal numeric nonce `1234567`: `452fda90010b117cb165a7af6d0012d687`, and SecureRandom numeric/format tests pass.
- Kuwo cookie/Secret remains native/process-memory, requests only fixed homepage/playUrl routes, refreshes at most once, and exposes no general network/session surface.
- Both native packages register, yet QQ/Kuwo playback/bootstrap capability remains fail-closed unless exact version/policy readiness and exact assumed-host validation pass.
- One AbortSignal/generation spans PlayerController -> ProviderClient -> native module; replacement/teardown and late results cannot reach RNTP.
- Authorized QQ/Kuwo fixtures reach RNTP and all typed failures preserve current/queue/results; full deterministic mobile regression passes.
- Final exact live hosts, post-probe RNTP redirects, accounts/CDN/API 35/audible behavior remain explicitly unverified for Phase 8.
</success_criteria>

<output>
Create `.planning/phases/05-five-source-listen-journey/05-05-SUMMARY.md` when done.
</output>
