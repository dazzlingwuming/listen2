---
phase: quick-260914-f3q-bilibili-account-session
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - mobile/android/app/build.gradle
  - mobile/android/app/src/main/java/com/listen2mobile/MainApplication.kt
  - mobile/android/app/src/main/java/com/listen2mobile/bilibili/BilibiliPolicy.kt
  - mobile/android/app/src/main/java/com/listen2mobile/bilibili/BilibiliVault.kt
  - mobile/android/app/src/main/java/com/listen2mobile/bilibili/BilibiliSession.kt
  - mobile/android/app/src/main/java/com/listen2mobile/bilibili/BilibiliGateway.kt
  - mobile/android/app/src/main/java/com/listen2mobile/bilibili/BilibiliQrRenderer.kt
  - mobile/android/app/src/main/java/com/listen2mobile/bilibili/BilibiliModule.kt
  - mobile/android/app/src/main/java/com/listen2mobile/bilibili/BilibiliPackage.kt
  - mobile/android/app/src/test/java/com/listen2mobile/bilibili/BilibiliContractTest.kt
  - mobile/src/bilibili/types.ts
  - mobile/src/bilibili/client.ts
  - mobile/src/types/provider.ts
  - mobile/src/api/client.ts
  - mobile/src/api/providers.ts
  - mobile/src/navigation/types.ts
  - mobile/src/navigation/RootNavigator.tsx
  - mobile/src/screens/SearchScreen.tsx
  - mobile/src/screens/SettingsScreen.tsx
  - mobile/src/screens/BilibiliDetailScreen.tsx
  - mobile/src/player/playerController.ts
  - mobile/src/bilibili/__tests__/client.test.ts
  - mobile/src/screens/__tests__/bilibiliFlow.test.tsx
  - mobile/src/player/__tests__/playerController.test.ts
autonomous: true
requirements:
  - QUICK-BILIBILI-001
estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: medium
must_haves:
  truths:
    - "A user can begin, poll, cancel, retry, and finish or expire a Bilibili QR login, inspect a truthful public account state, and sign out without any credential entering JavaScript, Redux, AsyncStorage, backup, or logs."
    - "A Bilibili search result opens a bounded video detail with exact parts; selecting a part creates an exact BVID/CID track identity and never silently falls back to another CID."
    - "Bilibili playback is resolved by a semantic native request using the native session and WBI policy; the UI cannot choose an endpoint, host, header, cookie, quality entitlement, or arbitrary media URL."
    - "The native boundary returns only public account/detail state, a non-authenticating opaque attempt ID, a bounded QR PNG data URI, stable redacted errors, and the minimum short-lived audio handoff required by TrackPlayer; Cookie, refresh token, CSRF, QR key, WBI material, raw bodies, and exception text never cross it."
    - "Anonymous playback remains available when Bilibili permits it, authenticated quality never exceeds the account entitlement, and login/member/region/DRM failures stay distinguishable and are not bypassed."
    - "Focused Jest and pure Kotlin contracts cover state, validation, stale replies, redaction, exact part selection, WBI construction, bounded transport, and transactional playback; live QR/account/device behavior remains explicitly not verified until final Android acceptance."
  artifacts:
    - path: "mobile/android/app/src/main/java/com/listen2mobile/bilibili/BilibiliModule.kt"
      provides: "Narrow semantic React Native account, detail, and audio-manifest surface"
      contains: "class BilibiliModule"
    - path: "mobile/android/app/src/main/java/com/listen2mobile/bilibili/BilibiliVault.kt"
      provides: "Android Keystore AES-GCM credential vault with no plaintext getter exposed to JS"
      contains: "class BilibiliVault"
    - path: "mobile/android/app/src/test/java/com/listen2mobile/bilibili/BilibiliContractTest.kt"
      provides: "Pure Kotlin state, policy, redaction, WBI, and response-mapping contracts"
      contains: "class BilibiliContractTest"
    - path: "mobile/src/bilibili/client.ts"
      provides: "Strict public DTO adapter for the native semantic module"
      exports: ["bilibiliClient"]
    - path: "mobile/src/screens/BilibiliDetailScreen.tsx"
      provides: "Phone-first bounded part selector and exact-track playback entry"
      contains: "BilibiliDetailScreen"
    - path: "mobile/src/screens/SettingsScreen.tsx"
      provides: "Bilibili QR account lifecycle and sign-out UI"
      contains: "Bilibili"
  key_links:
    - from: "mobile/src/screens/SettingsScreen.tsx"
      to: "mobile/src/bilibili/client.ts"
      via: "explicit QR begin/poll/cancel/logout actions and stale-session suppression"
      pattern: "bilibiliClient"
    - from: "mobile/src/bilibili/client.ts"
      to: "mobile/android/app/src/main/java/com/listen2mobile/bilibili/BilibiliModule.kt"
      via: "NativeModules.Listen2Bilibili exact DTO validation"
      pattern: "Listen2Bilibili"
    - from: "mobile/src/screens/BilibiliDetailScreen.tsx"
      to: "mobile/src/player/playerController.ts"
      via: "selected exact BVID/CID Track is passed through the existing success-gated player transaction"
      pattern: "playTracks|playTrack"
    - from: "mobile/src/player/playerController.ts"
      to: "mobile/src/bilibili/client.ts"
      via: "Bilibili semantic bootstrap delegates to the native session resolver"
      pattern: "resolveAudio"
---

<objective>
Deliver a coherent Bilibili account-to-audio slice in the canonical React Native Android app: safe QR session ownership, exact multipart selection, and native authenticated media resolution integrated with the existing transactional TrackPlayer controller.

Purpose: Close the largest functional gap between Android and the current desktop product without returning to the legacy WebView host or exposing privileged Bilibili session material to JavaScript.
Output: Kotlin account/vault/network/policy/module contracts, phone account and multipart UI, semantic playback integration, and focused automated evidence.
</objective>

<execution_context>
@/Users/fluenteng/.codex/gsd-core/workflows/execute-plan.md
@/Users/fluenteng/.codex/gsd-core/templates/summary.md
</execution_context>

<execution_gate>
The user approved concentrated source implementation before one final APK acceptance. Do not run repository-wide CI, npm install/update, APK assembly, install/run-android, emulator, signing, merge, or deploy in this task. The host lacks JDK 17, so run JavaScript gates and record the focused Kotlin command as not verified if Java remains unavailable. Preserve unrelated tracked and untracked files. Product changes belong only to canonical mobile/; legacy android/ and desktop app/ are read-only references.
</execution_gate>

<context>
@AGENTS.md
@.planning/STATE.md
@mobile/README.md
@mobile/src/types/provider.ts
@mobile/src/api/client.ts
@mobile/src/api/providers.ts
@mobile/src/player/playerController.ts
@mobile/src/navigation/RootNavigator.tsx
@mobile/src/screens/SettingsScreen.tsx
@mobile/android/app/src/main/java/com/listen2mobile/MainApplication.kt
@app/bilibiliService.js
@android/app/src/main/java/com/dazzlingwuming/listen2/provider/BilibiliAccountSession.java
@android/app/src/main/java/com/dazzlingwuming/listen2/platform/BilibiliQrGateway.java
@android/app/src/main/java/com/dazzlingwuming/listen2/platform/AndroidKeystoreCredentialVault.java
@android/app/src/main/java/com/dazzlingwuming/listen2/BilibiliPlaybackResolver.java
</context>

<locked_decisions source="approved audits and project constraints">
- D-01: Canonical implementation target is mobile/ React Native; legacy android/ and desktop code are behavioral references only.
- D-02: Native owns QR keys, Cookie jar, refresh material, CSRF, WBI signing, fixed endpoints, redirects, and entitlement decisions. None may be returned to or supplied by JavaScript.
- D-03: Public QR lifecycle supports begin, poll, cancel, expiry, retry, success, status, and logout with stale-session suppression and stable redacted states.
- D-04: BVID/CID/page identity is exact and bounded. A selected CID is never replaced by the first part or another response row.
- D-05: Playback accepts semantic Bilibili identity only, resolves through native fixed policy, and preserves the existing prepare-before-activate queue/history rollback contract.
- D-06: MV, PiP, DeepSeek, Bilibili offline download, and a generic URL/header bridge are outside this batch.
- D-07: Implement source and focused tests now; defer only live-account/JDK/device evidence to final acceptance and do not loop APK builds.
</locked_decisions>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Establish the native-only Bilibili session and media policy</name>
  <files>mobile/android/app/build.gradle, mobile/android/app/src/main/java/com/listen2mobile/MainApplication.kt, mobile/android/app/src/main/java/com/listen2mobile/bilibili/BilibiliPolicy.kt, mobile/android/app/src/main/java/com/listen2mobile/bilibili/BilibiliVault.kt, mobile/android/app/src/main/java/com/listen2mobile/bilibili/BilibiliSession.kt, mobile/android/app/src/main/java/com/listen2mobile/bilibili/BilibiliGateway.kt, mobile/android/app/src/main/java/com/listen2mobile/bilibili/BilibiliQrRenderer.kt, mobile/android/app/src/main/java/com/listen2mobile/bilibili/BilibiliModule.kt, mobile/android/app/src/main/java/com/listen2mobile/bilibili/BilibiliPackage.kt, mobile/android/app/src/test/java/com/listen2mobile/bilibili/BilibiliContractTest.kt</files>
  <action>Write the pure contract tests first, then port the proven behavior from the old Java and desktop implementations into focused Kotlin classes rather than copying their WebView/Electron boundaries. BilibiliPolicy validates canonical BVID, positive safe CID/page, fixed HTTPS api.bilibili.com QR/nav/view/WBI playurl/refresh routes, bounded query/body/response sizes, manual redirects, deadlines, MIME/codec, future media deadlines, and stable provider errors. BilibiliVault uses Android Keystore AES-GCM plus app-private SharedPreferences ciphertext with no plaintext or generic read method available to React Native; secure-storage failure is terminal and logout destroys alias-backed session data. BilibiliSession owns one active QR generation key and a separately generated random opaque attempt ID which cannot authenticate or derive the QR key, rejects stale poll/cancel replies, maps not-scanned/scanned/confirmed/expired/cancelled/error public states, owns cookies/CSRF/refresh/WBI material, refreshes only through fixed policy, and clears all session state on logout. Add exactly `com.google.zxing:core:3.5.3` and use BilibiliQrRenderer to encode the provider QR content entirely in native memory into a fixed-size PNG data URI capped at 192 KiB; if that pinned artifact is unavailable from existing Gradle caches, record the Kotlin evidence as not verified and do not substitute another version. The JS reply contains that bitmap and opaque attempt ID, never the QR URL, `qrcode_key`, `qr_key`, or query secret. BilibiliGateway performs bounded cancellable HTTPS requests, disables automatic redirects, supplies only native-owned headers/cookies, parses JSON defensively, and never logs raw bodies, URLs with query secrets, cookies, keys, or exceptions. BilibiliModule exposes only status, qrBegin, qrPoll, qrCancel, logout, videoDetail, and resolveAudio semantic methods with exact input key allow-lists and sanitized DTOs; register BilibiliPackage manually. For the TrackPlayer handoff, return exactly one URL whose scheme is HTTPS, host is bilivideo.com or a subdomain, length is at most 4096, username/password/fragment are absent, and `deadline` is 30 seconds to 24 hours in the future, plus exactly the fixed `Referer: https://www.bilibili.com/` header. Reject expired/excess-TTL URLs, other hosts, unknown headers, Cookie, Authorization, and alternate candidates. This compatibility value may enter one function-local TrackPlayer add call only; never persist, dispatch, cache, back up, or log it. Implement D-01 through D-07 without adding a generic networking bridge or bypassing entitlement.</action>
  <verify><automated>cd mobile/android &amp;&amp; ./gradlew --offline --no-daemon :app:testDebugUnitTest --tests 'com.listen2mobile.bilibili.BilibiliContractTest'</automated></verify>
  <done>Pure Kotlin contracts cover QR terminal and stale states, vault failure/clear behavior, exact semantic validation, WBI construction, fixed routes, bounds/redaction, response mapping, entitlement errors, and a minimal non-persisted audio handoff; when JDK is unavailable the summary records this command as not verified rather than claiming it passed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add strict React Native account and multipart user journeys</name>
  <files>mobile/src/bilibili/types.ts, mobile/src/bilibili/client.ts, mobile/src/types/provider.ts, mobile/src/api/client.ts, mobile/src/api/providers.ts, mobile/src/navigation/types.ts, mobile/src/navigation/RootNavigator.tsx, mobile/src/screens/SearchScreen.tsx, mobile/src/screens/SettingsScreen.tsx, mobile/src/screens/BilibiliDetailScreen.tsx, mobile/src/bilibili/__tests__/client.test.ts, mobile/src/screens/__tests__/bilibiliFlow.test.tsx</files>
  <action>Create an exact TypeScript adapter over NativeModules.Listen2Bilibili which rejects unknown keys, overlong text, noncanonical numeric values, malformed data URIs, extra headers, raw messages, and impossible state transitions. Public account state contains only login status, bounded display name/avatar, a random non-authenticating opaque attempt ID, expiry, retryability, and next action. Tests must prove `qrcode_key`, `qr_key`, cookies, tokens, URL query secrets, and the attempt ID never enter Redux, AsyncStorage, portable backup, or logs. Add a Settings Bilibili account section with begin, bounded polling, scanned/expired/error feedback, cancel/retry/logout, unmount cancellation, stale-attempt suppression, fixed Simplified Chinese copy, a bounded native-generated QR PNG data URI, and accessible 48dp controls. Clear the bitmap and opaque attempt ID on every terminal/unmount path and never persist either. Extend Track with optional transport-free Bilibili identity metadata or use an exact `bitrack_v_BVID-CID` ID while preserving backup compatibility. Search result press opens BilibiliDetail instead of immediately defaulting to the first page. BilibiliDetail fetches one bounded exact video detail, renders title/owner and ordered parts, preserves route identity across rerenders, handles stale/abort/error states, and constructs a selected track only from the exact returned BVID/CID/page. Remove direct JS Bilibili detail/playurl bootstrap from providers.ts after the native adapter is wired; anonymous search may remain on the fixed existing public search route. Implement D-01, D-03, D-04, D-06, and D-07.</action>
  <verify><automated>npm --prefix mobile test -- --runInBand src/bilibili/__tests__/client.test.ts src/screens/__tests__/bilibiliFlow.test.tsx &amp;&amp; npm run mobile:typecheck</automated></verify>
  <done>Focused tests prove the complete public QR lifecycle, stale response/cancellation behavior, strict DTO redaction, Bilibili search-to-detail navigation, exact ordered part selection, accessible recovery states, and absence of credentials or privileged transport fields from JS state and backup-facing track metadata.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Route semantic Bilibili playback through native resolution transactionally</name>
  <precondition>Task 2 has completed the strict bilibiliClient adapter and exact BVID/CID identity contract; execute Task 3 strictly after Task 2 because both modify api/client.ts and client.test.ts.</precondition>
  <files>mobile/src/api/client.ts, mobile/src/player/playerController.ts, mobile/src/player/__tests__/playerController.test.ts, mobile/src/bilibili/__tests__/client.test.ts</files>
  <action>After Task 2, make providerClient.bootstrapTrack delegate Bilibili exact identities to bilibiliClient.resolveAudio while leaving NetEase/Kugou behavior unchanged. Revalidate that the native reply matches the requested BVID/CID and obeys the exact Task 1 URL/header contract; keep the result function-local and pass it to exactly one TrackPlayer add attempt without dispatch, persistence, cache, backup, or logging. Expand safe player error mapping for LOGIN_REQUIRED, MEMBERSHIP_REQUIRED, REGION_RESTRICTED, DRM_RESTRICTED, REQUEST_TIMEOUT, and CANCELLED with fixed UI-safe codes. Preserve playerController's prepare-before-activate behavior: resolve before TrackPlayer reset where possible, capture/restore the current native snapshot on a target-load failure, and mutate now-playing, queue occurrence, and history only after TrackPlayer accepts and starts the exact part. Add focused tests for anonymous success, authenticated semantic call, exact CID mismatch rejection, expired URL, excess TTL, wrong host/scheme, unknown/Cookie/Authorization headers, alternate candidates, every entitlement error, cancellation, malformed native reply, TrackPlayer failure rollback, and proof that the signed URL/header occur only in the TrackPlayer mock call and never in Redux actions, serialized backup inputs, cache calls, or log spies. Implement D-02, D-04, D-05, and D-07.</action>
  <verify><automated>npm --prefix mobile test -- --runInBand src/bilibili/__tests__/client.test.ts src/player/__tests__/playerController.test.ts &amp;&amp; npm run mobile:typecheck &amp;&amp; npm --prefix mobile test -- --runInBand</automated></verify>
  <done>All mobile Jest suites and TypeScript pass, Bilibili playback uses only an exact semantic native request plus a transient validated TrackPlayer handoff, entitlement errors remain truthful, and a failed target never consumes queue/history or replaces the prior playable snapshot.</done>
</task>

</tasks>

<verification>
Run `npm --prefix mobile test -- --runInBand src/bilibili/__tests__/client.test.ts src/screens/__tests__/bilibiliFlow.test.tsx src/player/__tests__/playerController.test.ts`, `npm --prefix mobile test -- --runInBand`, `npm run mobile:typecheck`, `cd mobile &amp;&amp; npx --no-install eslint src/bilibili src/screens/BilibiliDetailScreen.tsx src/screens/SearchScreen.tsx src/screens/SettingsScreen.tsx src/api/client.ts src/api/providers.ts src/navigation/types.ts src/navigation/RootNavigator.tsx src/player/playerController.ts src/types/provider.ts`, and `cd mobile &amp;&amp; npx --no-install prettier --check src/bilibili/types.ts src/bilibili/client.ts src/bilibili/__tests__/client.test.ts src/types/provider.ts src/api/client.ts src/api/providers.ts src/navigation/types.ts src/navigation/RootNavigator.tsx src/screens/SearchScreen.tsx src/screens/SettingsScreen.tsx src/screens/BilibiliDetailScreen.tsx src/screens/__tests__/bilibiliFlow.test.tsx src/player/playerController.ts src/player/__tests__/playerController.test.ts`. Run the Metro check with `cd mobile &amp;&amp; BILI_METRO_DIR="$(mktemp -d /tmp/listen2-bili-metro.XXXXXX)" &amp;&amp; npx --no-install react-native bundle --platform android --dev false --entry-file index.js --bundle-output "$BILI_METRO_DIR/index.android.bundle" --assets-dest "$BILI_METRO_DIR/assets"`; do not assemble an APK. Attempt the named pure Kotlin test only when a usable JDK 17 exists; otherwise record it and all live QR/account/device paths as not verified. Inspect staged diffs for credentials, raw provider responses, generated files, legacy android/ edits, and unrelated untracked files before each atomic commit.
</verification>

<success_criteria>
- The account UI exposes a complete sanitized QR lifecycle and logout.
- Exact BVID/CID parts flow from search detail to transactional playback without first-part fallback.
- Credentials, WBI and provider session state stay native-only; the only media value crossing JS is a single validated ephemeral TrackPlayer compatibility handoff which is never stored.
- Focused and complete JavaScript gates pass; unavailable JVM/device evidence is reported accurately.
- Product changes are confined to mobile/ and committed in coherent atomic units without APK, merge, or deploy work.
</success_criteria>
